# A19 real-media byte-quota boundary pilot — 2026-09-25

Classification: historical live evidence. Current A19 capability wording and
product state come from `capabilities/windows-hvf.json`. A19 remains OPEN.

## Exact source and sealed inputs

Physical-Mac job `t21-51e00066-a19-quota-boundary-r1` ran the
`t21-a19-quota-refusal` tier from exact merged main
`51e000667c20128682b15ebc1d33f05125a21bc5`. All 46 main-push hosted
workflows succeeded, including core CI
[36107712072](https://github.com/Ketchio-dev/bridgevm/actions/runs/36107712072).
The exact-main [full project check](https://github.com/Ketchio-dev/bridgevm/actions/runs/36107735720)
passed with `checked_source_sha=51e000667c20128682b15ebc1d33f05125a21bc5`.
The [Release dry-run](https://github.com/Ketchio-dev/bridgevm/actions/runs/36107743843)
also succeeded on that SHA; its artifact ID was `10852048374` and GitHub
reported ZIP digest
`e62d882b6e35db1a42dd31f6e6b34acb0db93420e2c0090338a38aea944c1bea`.
This dry run did not publish a release.

The worker's sealed seven-input manifest SHA-256 was
`35a668ef7b01a1aa89dc72dfa449c5052992385f7d5c79eec713620293ce6c28`;
the sealed Venus probe SHA-256 was
`121181f043959589e6aa452c0af79a9554032660d78630c245ef5c246ec7aed2`.
The receipt binds both to the queue job record and immutable ledger. It also
binds the authenticated release app tree and packaged snapshot helper. The
installed Windows disk SHA-256 was
`5ad7a304cfec4fe9320784b26b4d6895885361ddef2675d2411a759cb54165f8`;
its matching UEFI vars SHA-256 was
`2f0e68923bf0e4cc1bcfd51a6bb67eb661d48b90aef97ea3b03d4b2805b33ca7`.
The worker exercised private APFS clones; canonical media was not modified.

## Physical observation

The job ran on Mac17,9, macOS 27.0 from 07:56:12 to 08:00:55 UTC. Queue
`result.env` records `result=pass` and exit code 0. The strict private and
public receipts were byte-identical and passed schema, exact-source and queue
seal verification. The [frozen path-free public receipt](a19-t21-r1-20260925-receipt.json)
SHA-256 is
`808cc5627c32c20133bbf6ee6fe74b1d87c4b873c2537967d3f6d414d4bb9ee0`.

The selected disk plus vars pair measured **68,786,585,600 bytes**. A helper
`create` request with quota **68,786,585,599 bytes** returned exit 1 and the
exact `quota_exceeded` error; the rejected destination and staging tree were
absent. The cloned input disk and vars hashes did not change. A second helper
`create` request with quota **68,786,585,600 bytes** succeeded. Helper `verify`
succeeded, and the snapshot manifest, disk and vars hashes matched the sealed
pair. The snapshot manifest SHA-256 was
`14b22ceb7a377d4a6c71131185ce9bea0977a9c0fc10125a27bf133145562a09`.
The final cloned input hashes still matched the sealed inputs.

The receipt records `quota_case_count=1`, `run_count=1`, `sample_count=0` and
`boots_attempted=0`. `worker_cleanup_verified=true`; no owned private clone,
snapshot or staging residue remained, and no queue cleanup fence was present.
The private run log remains outside git; its SHA-256 is
`d0c5257ad43d65eece826c74031a5342b295cc8dee1fc927879aca7e50058ea4`.

## Evidence limit

This is one helper-direct real-media byte-quota case on exact source
`51e00066…`. It did not exercise the product UI refusal path or boot a guest.
It does not prove every quota/refusal case, interrupted snapshot create or
restore, sudden physical power loss, or the remaining product lifecycle sample
count. The historical [mixed-pair failure](a19-restore-atomicity-defect-20260912.md)
remains in the record; earlier T20 lifecycle samples stay distinct. The receipt
has `claim_eligible=false`, `criterion_pass=false`,
`capability_promotion=false` and `three_d_injection=false`. A19 stays OPEN and
the product remains Engineering Preview.
