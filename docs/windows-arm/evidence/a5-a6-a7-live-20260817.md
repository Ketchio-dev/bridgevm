# A5, A6, A7: fresh live proof to replace the figures nobody could check (2026-08-17)

## Why

These three criteria cited an A9 receipt that never mentioned audio, clipboard
or file transfer, and the runs their original figures came from were lost. They
were dropped to OPEN with that disclosure. This session re-proved all three
against live boots of the canonical net-live image, with every figure below
re-readable from the retained run logs.

## A6 — clipboard round trip with Korean text

Run `~/BridgeVM/runs/a567-live-*`. The guest set its clipboard to a Korean
test string, read it back, and hashed it:

```
BVA6 match=True sha256=21c7abcad447cf3195122b12efd12ae129a1cefcebb65aa4243e99434db9850a
```

The host computed the SHA-256 of the identical expected string independently:
`21c7abca...` — byte-identical. Round trip and encoding both hold.

## A7 — file transfer, both directions, matching hashes

Same boot, over the agent share (`--agent-share-host` / `C:\BVA567`):

| direction | bytes | guest SHA-256 (Get-FileHash) | host SHA-256 (shasum) |
| --- | --- | --- | --- |
| host → guest | 74 | `656f9a38...53dce2` | identical |
| guest → host | 58 | `750b06d7...dc19c0` | identical |

The guest hashed the file the host wrote, and the host hashed the file the
guest wrote. Both directions match exactly.

## A5 — guest PCM reaches the host CoreAudio ring

Run `~/BridgeVM/runs/a5-live-*`, booted with `BRIDGEVM_HDA=1
BRIDGEVM_HDA_COREAUDIO=1` (the boot runner forwards both). The guest played
Windows Ding five times; on shutdown the sink printed:

```
hda CoreAudio stats: frames_rendered=400948 drops=0 dropped_bytes=0
                     format_drops=0 ring_full_drops=0 callback_errors=3
```

`frames_rendered>0 AND drops==0` — the criterion's exact statement — holds.
`callback_errors=3` matches the quality note the original A5 carried and stays
tracked as such: the errors occur at stream teardown, not during rendering,
and no frames were dropped.

One boot-cost lesson: the stats print on clean process shutdown (the sink's
Drop), so the guest must be shut down gracefully (`shutdown /s /t 3`) or the
counters are never emitted.
