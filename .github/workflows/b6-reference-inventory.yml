name: B6 reference inventory contract
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  contract:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: powershell
        run: tests/integration/b6-reference-inventory-contract.ps1; tests/integration/firstboot-readiness-diagnostic-contract.ps1
