# HANDOFF — 2026-08-09/11 프로덕션 클로즈

> **역사 기록.** 이 문서는 2026-08-09/11 시점의 인수인계 기록이며 그 시점의
> 상태를 보존한다. 당시 OPEN이던 A3/A11은 이후 PROVEN으로 닫혔고, 여기 적힌
> 653 같은 테스트 개수는 그 시점의 값이다. 현재 상태는 `STATUS.md`와 능력
> 레지스트리(`capabilities/windows-hvf.json`)를 보라.

## 2026-08-16 gate coverage and dead defaults (committed)

Where the next session should start, and what it must not re-discover.

**CI now runs every `check-project.sh` step.** Five ran nowhere but a
developer's machine when this session began, including the 659 shim tests of the
time. New
gates: shell-script defect classes, Python compilation, workflow parsing plus
required jobs, test reachability, Swift source hygiene, and whether cited
capability evidence contains its figures.

**Two disclosures are open and deliberately not closed.** Five criteria (A5,
A6, A7, A17, A19) state figures no cited document contains, and the legacy
`.qemuCompat` engine resolves swtpm, qemu and edk2 from fixed Homebrew paths.
Both are recorded in the gates that found them and in STATUS; removing either
record requires producing what is missing.

**The method that found most of this** is in `docs/development-system.md`:
mutate a gate in both directions before trusting it, measure the shipped code
before changing it, and ask of every absolute default whether the file is there
right now. That last question found two paths that had already stopped
resolving.

## 2026-08-11 all-vCPU holder diagnostic implementation (committed; follow-up planned)

- A3/A11은 여전히 OPEN이다. 아래 변경은 stop 후 read-only diagnostics뿐이며 guest/device/interrupt/watchdog/renderer/A3 3/3 정책을 바꾸지 않는다.
- secondary owner thread가 `hv_vcpu_run`을 벗어난 뒤 handle withdrawal/`HvVcpuGuard` destruction 전에 PC/CPSR/X0-X30, MPIDR, SP_EL0/SP_EL1, SCTLR/TCR/TTBR0/TTBR1/MAIR과 각 `hv_return_t`를 캡처한다. primary는 secondary join 후에만 snapshot과 stable guest RAM을 해석한다. failed HVF read는 explicit unavailable이며 zero로 위장하지 않는다.
- per-vCPU translated PC/LR bytes, PE candidate identity, bounded frame chain/stack을 출력한다. viogpu parser는 strict `x0 == x20 == x28 + 0x20` 관계에서만 동작하고 `VioGpuBuf` object/lock와 `m_InUseBufs`를 최대 64 nodes로 읽는다. sentinel/cycle/null/unreadable/truncated를 fail-closed 기록하고 `GPU_VBUFFER` callback/context/`auto_release` 및 command/response type을 bounded하게 보존한다.
- focused evidence: viogpu final-state 10 tests PASS, `vcpu_coordination::tests` 7 PASS, venus example `cargo check` PASS, installed P3 GPU policy smoke PASS, live policy smoke 70/70 PASS, structural budget PASS, `git diff --check` PASS. Harness deterministic `scripts/check-project.sh --fast`도 exact worktree에서 PASS. Full non-fast `scripts/check-project.sh`는 아직 실행 전이므로 A11/goal 완료 주장 금지.
- files: `hvf_gic_boot_probe.rs`, `final_report.rs`, `probe_runtime.rs`, `secondary_vcpu.rs`, `vcpu_coordination.rs`, new `vcpu_coordination_tests.rs`, new `vcpu_final_state.rs`, new `vcpu_final_state/viogpu.rs`, new `vcpu_final_state_tests.rs`, and `scripts/refactor-budgets.tsv`. Existing ceilings were lowered or retained; none raised. `HANDOFF.md` remains operator-owned and must not be staged.
- commit/push history: `33c5da9a892765d1ab499209db855dda3cabec70` (`hvf: preserve all-vCPU stop diagnostics`) reached `origin/main`, but hosted CI run `31502762450` found one real `clippy -D warnings` failure: `publish_final_state -> Result<(), VcpuFinalState>` had a 696-byte Err variant (`clippy::result_large_err`). Security `31502762451` passed. This failed SHA remains historical evidence and must not be called green.
- fix commit `a645fce75db3fa8f515c715c08deb84be6641e41` (`hvf: keep final-state publication compact`) is on `origin/main`. One-shot publication now returns `bool`, preserving the same no-overwrite ownership contract without boxing or lint suppression. Local exact-tree `cargo clippy --workspace --all-targets --locked -- -D warnings`, example 329/329, venus check, budgets and `scripts/check-project.sh --fast` all PASS.
- hosted runs for `a645fce75db3fa8f515c715c08deb84be6641e41` are green: CI `31503203557`, Security `31503203599`. Initial follow-up full-check job `20260811-104437-59421-24573` was canceled while queued because operator supplied the wrong non-existent SHA string `a645fce422…`; never use it as evidence. Exact job `20260811-104803-70181-25760` was refused fail-closed before test (`available_gib=99`, required 100) and produced no receipt. Only reproducible local `target/` and `apps/macos/.build` were removed, raising free space to 102 GiB; no evidence/canonical media was touched. Replacement exact-SHA job `20260811-105324-82736-9937` submitted.
- old full-check job `20260811-103954-91299-9614` completed FAIL at `33c5da9` with the same clippy errors as hosted CI; receipt preserved and must not be promoted.
- exact full check `20260811-105324-82736-9937` completed PASS at `a645fce75db3fa8f515c715c08deb84be6641e41`: `result=pass`, `exit_code=0`, receipt public SHA-256 `07aff482c41b84feaf1aa3c9528fc5f40ecbf91f89a5dd7062129d9d94d81332`, final `project check: PASS`. There are no active live jobs and free space is 102 GiB.
- exact release probe built from `a645fce` with release+venus and ad-hoc HVF entitlements: `~/BridgeVM/binaries/a645fce75db3fa8f515c715c08deb84be6641e41/hvf_gic_boot_probe`, SHA-256 `1b882fdc0c4ac0c49bb32f95691c400f49d5bdbd001ad3bcfb4927fa222c288e`; strict codesign, `virtio_gpu_3d_compiled=true`, exact virglrenderer linkage and hypervisor entitlement PASS. New 10-input manifest `~/BridgeVM/manifests/a3-title-a645fce-all-vcpu-diagnostic.tsv`, SHA-256 `5cae6c8f202c96291497e1d2c4c0931d15ddd977e7b63589b7bc61e3ee166205`.
- manifest revalidation found the local driver directory had drifted (`83f072…`, `viogpu3d.sys=087da7…`) from the prior sealed shipping tree. It was moved intact to `~/BridgeVM/work/quarantine-download-120.45-mutated-20260811-151215`; exact GHA artifact `8622591771` from builder run `30169476359` was re-downloaded and restored at the original path. Restored tree `94bfe4…` and shipping SYS `d95e427…`; all 10 manifest inputs now rehash PASS. No canonical/evidence bytes were deleted.
- exact-input diagnostic campaign `20260811-111346-32765-30509` completed FAIL, tier `t6-a3-title`, commit `a645fce75db3fa8f515c715c08deb84be6641e41`, manifest `5cae6c8f202c96291497e1d2c4c0931d15ddd977e7b63589b7bc61e3ee166205`, sealed binary `1b882fdc0c4ac0c49bb32f95691c400f49d5bdbd001ad3bcfb4927fa222c288e`. Worker `verified-inputs.tsv` records exact PASS for all ten keys. Receipt/public receipt SHA-256 is `80c4ba5f43eefa12c9f776ac4d6f622535aa286fe4896db74b21e1c25d270567`; `run_count=1`, `required_run_count=3`, samples 0, module identity false, pass false.
- run 1 was graphics-active (not the latest pre-login-idle shape): BVAGENT READY/service started; trace ended at 2,716 records with 2,435 commands, 285 `RESOURCE_CREATE_3D`, 353 `SUBMIT_3D`, 64/64/64 fence create/complete/deliver, contexts through 26 and fence id through 1170. Guest share never ACKed the first asset; overdue/retransmit shifted to ping and continued through guest t≈656s although periodic SERVICE alive and later GPU traffic continued. Title commands were correctly never appended (`agent.ctl` 0 bytes), so this is pre-title/share failure, not FPS evidence.
- **diagnostic capture failed at the host lifecycle boundary**: verifier reported `bvgpu-real-title-gate.ps1 sync timeout` after its unchanged 600-second share wait, then `cleanup` sent SIGTERM to the probe. The probe has no graceful signal handler, so `VCPU-FINAL` markers are 0, post-run harvest is missing, and no holder/list lifecycle was captured. This run does not support a driver root-cause claim or patch.
- focused follow-up is now implemented locally: an opt-in absolute host request path is polled once per second by a dedicated host-only watcher. Only a direct regular file latches the one-shot flag and calls `hv_vcpus_exit`; cancel attribution is checked even when an agent/ramfb/vblank wake merged into the same exit. SYSTEM_RESET does not disarm the run-wide watcher; only the terminal final-report boundary does. The A3 verifier atomically creates the request only from nonzero EXIT cleanup, waits at most the unchanged `DIAGNOSTIC_GRACE`, then retains existing TERM cleanup as fallback. Guest/device behavior before failure, 600s share timeout, boot watchdog, renderer, p50 threshold, and A3 3/3 policy are unchanged.
- fresh deterministic evidence on the exact local tree: host stop policy/one-shot tests 2/2 PASS; cancel attribution 2/2 PASS; full HVF probe example 331/331 PASS; venus check and focused clippy `-D warnings` PASS; live policy smoke 73/73 PASS; installed P3 GPU policy smoke PASS; structural budgets, rustfmt, `git diff --check`, and `scripts/check-project.sh --fast` PASS. New modules are ratcheted at actual 76/1, 37/0, and 25/0; no existing ceiling was raised.
- required full non-fast `scripts/check-project.sh` now PASS on the exact local tree (Rust/workspace checks, cross-compile stubs, Swift build/tests, release override checks, final `project check: PASS`). The one-shot host request test was strengthened to prove absent → direct regular file accepted once → sticky fired → duplicate rejected; tests still 2/2 PASS after extraction. `host_diagnostic_stop.rs` is now 76/1 and its test module 37/0, both registered at actual size; budgets and `git diff --check` remain PASS.
- product change committed/pushed as `c263bc69196d3f4dd653d6f5f13274f9f75850cb` (`hvf: preserve diagnostics after host gate failure`); `HANDOFF.md` remains the only unstaged operator-owned file. Reproducible `target/` and `apps/macos/.build` were removed after full PASS to restore the queue's immutable 100-GiB guard (101 GiB free; no evidence/media removed). Hosted exact-SHA CI `31512651135` and Security `31512651123` both completed success. Exact-SHA asynchronous full-check job `20260811-123012-87448-5112` completed PASS at `c263bc6`: `result=pass`, `exit_code=0`, final `project check: PASS`, private receipt SHA-256 `c7facd6e0dfdda9cd5736b5dd96730b8b8db4676fbbca25492d2228a990ffe06`, public receipt SHA-256 `c8c366269304315a52eb570a7d12701dee42c3d42da3dcd621c8a7a10badb562`. Built/sealed exact `c263bc6` release+venus probe at `~/BridgeVM/binaries/c263bc69196d3f4dd653d6f5f13274f9f75850cb/hvf_gic_boot_probe`, SHA-256 `5e98e559f652bde3ef16253a74f8804d2cd6e20ac909b647e8d4b727b9051add`; strict codesign, HVF entitlement, `virtio_gpu_3d_compiled=true`, and exact virglrenderer linkage PASS. Manifest `~/BridgeVM/manifests/a3-title-c263bc6-host-stop-diagnostic.tsv`, SHA-256 `9abf821fc0a6f40c92d321a8d4343d268715d05253733b2e6d07a3796381728e`; worker rehashed all ten exact inputs PASS.
- exact diagnostic job `20260811-124458-45491-7672` completed honest FAIL: receipt/public SHA-256 `df8a770f3681bf3f49581f07b2af17add82158493e164360e8f124fa5ffd0f12`, `run_count=1`, `required_run_count=3`, samples 0, module identity false. This was not a graphics lock. Assets synchronized and GPU reached 2,842 records/2,561 commands, but PREP returned exit 4 because `C:\BridgeVM\a2-title\ppsspp\PPSSPPWindowsARM64.exe` was absent while T6 forced `PRESTAGED_TITLE=1`.
- graceful stop evidence is now live-proven: host request observed, `VCPU-FINAL-STATUS: complete`, captured 4/4 with exact translated PC/LR bytes. All CPUs had PC/LR `0xfffff80015081cdc/15081cd0`, translated to `ntkrnlmp.pdb` RVA `0x481cdc` idle/WFI; all four strict viogpu waiter checks rejected. This supports neither the earlier lock-holder hypothesis nor a driver patch. Post-run read-only harvest succeeded.
- `PLAN.md` records the payload-staging correction. Implementation is now in the uncommitted product diff: the manifest `ppsspp` row seals a strict ZIP payload; `a3-title-payload.py` rejects traversal, symlinks, aliases/reserved paths, unsupported/encrypted compression, duplicate case-folded names, >4,096 entries, or >256 MiB compressed/expanded; each run splits it into 7 MiB parts under the restored 8 MiB share cap, guest reconstructs and hashes archive+ARM64 executable, expands to staging, and replaces `C:\BridgeVM\a2-title` only after both identities match. Receipts keep separate payload (`ppsspp_payload_sha256`) and executable (`ppsspp_sha256`/`ppsspp_executable_sha256`) identities. Real local payload evidence: archive `f664f060005427e42db3e090e76f8782543d22d1891fd7ab84224236477c29ef`, 19,391,499 bytes/214 entries/41,095,642 expanded/CRC clean; embedded executable remains `d5e587f086b96a2c1bed240f8aa3f5be50e10d68aa5f37e73fd4353265f43b3e`; reconstructed 3-part host test (`7,340,032`, `7,340,032`, `4,711,435`) rehashed to the archive identity.
- Payload staging is committed/pushed as `0c9e770ddeca1afafc85ba16c45cfd812dbd306d` (`graphics: seal and stage the A3 title payload`). Fresh deterministic evidence before commit: payload policy self-test PASS, bounded chunk self-test PASS, receipt/redaction self-tests PASS, live policy smoke 81 checks PASS (including complete-manifest unsafe-payload refusal), installed P3 policy PASS, CRLF PASS, structural budgets PASS, `git diff --check` PASS, `scripts/check-project.sh --fast` PASS, and full non-fast `scripts/check-project.sh` PASS. Full-check tool log: `/var/folders/_d/y_x7_nf52sdd0qlgtqfk04300000gn/T/pi-bash-2c5e98e8834e21c8.log`. Hosted exact-SHA workflows are now green: Security `31519068133` success and CI `31519068240` success. First exact-SHA T0 `20260811-134403-85652-6177` honestly refused at 98 GiB (<100 GiB guard), then regenerated local `target`/Swift build cache was removed (not evidence/media), free space restored to 101 GiB. Exact-SHA T0 `20260811-134502-86746-22088` completed PASS at `0c9e770`: `result=pass`, `exit_code=0`, final `project check: PASS`; private/public receipt SHA-256 `8df4ce4a8bb6e0c9280d57087642c20723f6e3505cc2c11cb916611801d4e9ce` / `25b884101662bd0bc7c7ebad6e565412071c6540069dc91b7dea9f71ae6281a1`.
- exact release+Venus `0c9e770` probe built/ad-hoc signed with HVF entitlement at `~/BridgeVM/binaries/0c9e770ddeca1afafc85ba16c45cfd812dbd306d/hvf_gic_boot_probe`, SHA-256 `e2853ec45a69566b3a678d79c5cbc6faa348f986e1367a5df10a115f1234e8a7`; strict codesign, `virtio_gpu_3d_compiled=true`, and exact virglrenderer linkage PASS. New 10-input manifest `~/BridgeVM/manifests/a3-title-0c9e770ddeca1afafc85ba16c45cfd812dbd306d-sealed-payload.tsv`, SHA-256 `e8fc51966c7391659d00822767519eb3a03ad96e81ca8238ecf23227f17f4965`, changes only `ppsspp` to sealed payload `f664f060…` and binary to `e2853ec4…`; all other eight identities retain the prior verified values. Exact-input T6 A3 campaign `20260811-135851-37514-3242` completed honest FAIL, receipt/public SHA-256 both `e0f8f5922f3400d5740f71c7a2998b009c73fd42a002131068acda26bb731117`: all ten inputs verified, run_count 1/required 3, samples 0, module identity false. It failed before agent/title/payload staging: after one SYSTEM_RESET the boot-progress watchdog saw only 3 exits in 120 s and stopped at guest generation 2; service never started and only 3 GPU init records existed. Therefore this run neither validates nor falsifies the new payload transfer.
- Final diagnostics were complete (4/4). CPU0 `pc=...2761e8`, LR `...2762b0`, code bytes and historical exact symbol map place it in the established `KeIpiGenericCall` ack loop (`+0xa8` from public RVA `0x276140`); CPUs 1/2 were at kernel RVA `0xc93e04`, CPU3 at `0x47b728`. All four strict viogpu waiter checks rejected, no title command/GPU command stream occurred. Classify as a pre-title boot/IPI-ack stall, not graphics/payload evidence and not permission for a driver patch. Read-only post-run harvest succeeded. A3 remains OPEN. Because the first failure was explicitly pre-title, one same-SHA retry `20260811-140847-85685-23616` ran with no code/threshold/timeout/input change. It also completed honest FAIL before service/title/payload staging: receipt SHA-256 `9f12af71e48ee309a021d9497d0708acf5f3c26b5673aff43cfd7b3807295705`, 1/3, samples 0. This time Windows initialized the legacy VIRGL context (97 GPU records, valid feature negotiation/capset info/CTX_CREATE/nonempty SUBMIT_3D), then the exact release probe exited by `Trace/BPT trap: 5` immediately after `FIXME: "Handle EOI ID is not the highest priority, EOIR1 0x80" line 220`; probe status 133, no graceful final state. This is a distinct host-process crash at the Apple in-kernel GIC EOI path, not FPS/payload or viogpu holder evidence. Post-run read-only harvest succeeded. Crash report `~/Library/Logs/DiagnosticReports/hvf_gic_boot_probe-2026-08-11-141303.ips` is decisive: EXC_BREAKPOINT/SIGTRAP (`brk 1`) on the main vCPU thread in Hypervisor.framework UUID `5ace59ba…`, stack `Base::report_fixme_and_trap` → `HvCore::Hypervisor::EmulatedVgic::write_end_of_interrupt_register` → `Arm::Emulator::write_icc_eoir1_el1` → `VcpuStateManager::handle_system_register_access_exit` → `Hv::Vcpu::run`. So it is an Apple framework deliberate trap on a guest EOIR1 ordering state, not BridgeVM panic/virgl. Do not submit another T6 blindly; next bounded work is to compare guest priority/active ordering and determine whether host interrupt injection violates the GIC contract, without changing A3 criteria.

## 2026-08-11 13:15 UTC 최신 상태 (이 섹션 최우선)

- 제품 head `54755bff24eccf0206ee9d6346684c10ee59ea4b` (`windows: preserve boot postmortems read-only`), `origin/main` 동기화. Hosted CI `31491094566`와 Security and quality `31491094649` 모두 success. tracked product tree에는 새 변경이 없고 이 `HANDOFF.md`만 operator-owned local modification이므로 stage 금지.
- exact-input A3 campaign `20260811-082821-26741-10121`은 완료됐고 active live job은 없다. commit `54755bf`, manifest SHA-256 `f06f86dfa588794689964b74dd2c787948a2dae43c816df087ae6eee4256726a`, sealed binary SHA-256 `085039d672a4e809e5c6c1306c4aaf9ebfd564ae49c829291482bbb87d2886c4`; ten inputs는 모두 exact match.
- campaign은 run 1에서 honest fail 후 run 2/3을 시작하지 않았다. receipt/public receipt SHA-256 모두 `45b782d5a2d4d757b12a33940cedf1664c6933ea7caab7aca0c2e0eaba280ea1`; `run_count=1`, `required_run_count=3`, `failures=1`, `sample_count=0`, `module_identity_verified=false`, `pass=false`.
- 이 실패는 이전 graphics spin-lock failure가 아니다. `virtio-gpu.jsonl`은 초기화 3건뿐이고 command/context/title/BVAGENT/FPS가 모두 0이다. final PC/LR `0xfffff80142681cdc`/`0xfffff80142681cd0`는 exact kernel public symbols에서 `HalProcessorIdle+0x1c/+0x10`; stack family는 `PpmIdleDefaultExecute`, `PpmIdleExecuteTransition`, `PoIdle`, `KiIdleLoop`다. 별도 pre-login idle/WFI failure로 분류하고 A3 FPS나 driver-race evidence로 재사용하지 않는다.
- read-only post-mortem은 실제 gate lifecycle에서 정상 동작했다. pre/post manifest와 copied evidence는 `~/BridgeVM/live-queue/done/20260811-082821-26741-10121/run-1/` 아래에 있다. agent log, task XML, Winlogon/Security/System EVTX의 size/mtime/SHA-256은 pre/post 동일하고 TaskScheduler Operational은 양쪽 모두 `missing`; mount flags는 `media_read_only=true`, `volume_read_only=true`. 따라서 이번 boot에는 persisted logon/task/agent update가 없으며 2026-08-08 agent tail은 stale historical evidence다.
- 가장 최근 graphics-active failure는 `20260811-061223-93257-13219`: final PC/LR `0xfffff80359e6d4ec`/`0xfffff80359e6d1d0` = `KxWaitForSpinLockAndAcquire` → `KeAcquireSpinLockRaiseToDpc`; GPU commands 1045, fences 64/64/64, NVMe reads 190, console `ready=true guest_open=false`. waiter ABI는 `VioGpuBuf::GetBuf(this, 0x48, 0x18, nullptr)`로 `RESOURCE_CREATE_3D` allocation과 일치하고 lock target은 `VioGpuBuf + 0x20`; 실제 concurrent holder는 아직 unknown이다.
- exact shipping driver source/PDB/machine code에서 real static hazard 두 개를 확인했다. `VioGpuQueue::ReleaseBuffer` RVA `0x2a60` 안에 inline된 `FreeBuf`는 acquire `0x2b04`, circular traversal `0x2b24..0x2b48`, release `0x2c2c`이며 sentinel 종료가 없어 absent/already-released buffer에서 lock을 보유한 채 순환 가능하다. `VioGpu3DDpcRoutine`은 callback `0x35bc` 뒤 `auto_release`를 `0x35c0`에서 다시 읽고 true이면 `0x35d0`에서 release하므로 event signal 뒤 waiter의 free/reuse와 lifecycle race가 가능하다. 이 ordering은 shipped bytes에 존재하지만 실제 holder/buffer reuse를 포착하기 전에는 current root cause 또는 shipping fix로 승격하지 않는다.
- 다음 concrete step은 behavior-neutral bounded holder capture다: watchdog stop 시 각 owning vCPU에서 final PC/register/stack을 보존하고, proven waiter의 exact lock address/word 및 `m_InUseBufs` sentinel/links/candidate `PGPU_VBUFFER` identity를 bounded read-only로 수집한다. holder가 `ReleaseBuffer`/inline `FreeBuf`에 있거나 callback/flag/lifecycle evidence가 직접 일치할 때만 separate builder repo에서 candidate driver fix와 새 PDB/package provenance를 만든다.
- formal A1 10/10 evidence는 이 다른 A3 lane의 단일 idle failure로 자동 철회하지 않는다. 다만 reliability signal은 보존한다. 같은 A2 disk/vars snapshot pair clone으로 다음 diagnostic job 하나만 실행하고, 새 campaign 전 live job 동시 실행·canonical mutation·threshold 완화는 금지한다.

## 2026-08-11 05:22 EDT 최신 상태 (이 섹션 최우선)

- 제품 head `a98c80f` (`graphics: keep A3 on the shipping renderer`), origin/main 동기화. Hosted CI `31477231879`와 Security `31477231662` 모두 success. `scripts/check-project.sh --fast`, policy smoke 56/56, receipt/redactor self-tests, budgets PASS.
- `dbaa08d` exact A2-lineage T6 `20260811-044019-83899-2440`은 run 1에서 25분간 BVAGENT 0/title 0/samples 0으로 FAIL. DWM/virtio-gpu는 계속 active: command 10,491, fence create/complete/deliver 67/67/67, 60–120초 framebuffer black. receipt는 의도대로 `run_count=1`, `required_run_count=3`, `failures=1`, relative evidence path를 기록했고 public receipt private-path leak 0; SHA-256 `d6133946...`.
- 실패 verifier는 host preflight와 guest watchdog을 둘 다 1500초로 잡아 outer wait가 먼저 launcher를 kill했고 guest logs/final state harvest가 유실됨. `1d38e3c`/`a98c80f`에서 preflight를 별도 600초 bound로 확인한 후 guest watchdog 1500초 + post-mortem grace 60초를 주도록 수정.
- **철회**: `balanced`가 제품 기본 renderer라는 판단은 잘못이었다. 앱 `HvfEngineConfig.performanceRisk`와 typed runtime은 shipping lane을 `aggressive`로 고정한다. `1d38e3c`의 balanced 변경으로 live run은 한 번도 제출하지 않았고, `a98c80f`에서 aggressive를 즉시 복원했다. A3는 계속 shipping renderer를 측정한다.
- 새 exact A3 job `20260811-052245-28752-9727`: SHA `a98c80f`, A2 current image/vars, 10-input manifest SHA-256 `f06f86dfa588794689964b74dd2c787948a2dae43c816df087ae6eee4256726a`, signed binary SHA-256 `085039d672a4e809e5c6c1306c4aaf9ebfd564ae49c829291482bbb87d2886c4`. 마지막 관측 시 queued; queue idle/free 101GiB.
- A2 canonical task audit: `BridgeVM Guest Agent` XML은 enabled ONLOGON, interactive user `bridge`, highest run level이고 payload 존재. canonical bytes/inode/size/mtime는 independent preflight 이후 불변.
- 이전 exact `dbaa08d` full check job `20260811-044020-83918-11526`은 project check PASS receipt를 남김.
- working tree는 `HANDOFF.md`만 operator-owned local modification; stage 금지.

## 2026-08-11 최신 상태 (이 섹션 우선)

- A1 **PROVEN**: `~/BridgeVM/runs/p1gate-a1release-0f02579-20260810-121804` 10/10 PASS. 모든 clone stage4/fresh=1, distinct firstboot hash, reset=3, zero stalls, clean SYSTEM_OFF. tracked evidence/registry commit `404cd27`; hosted CI `31461214983`/`31461214950` success.
- A3 queue/provenance commit `c70f9c7` push 완료; hosted CI `31468605766`/`31468605856` success. current verifier는 PPSSPP real title + title-local ARM64 DXVK `d3d11.dll`/`dxgi.dll` + loaded Venus ICD의 guest-side exact hashes + samples>0 + p50>=30을 요구하고 **세 독립 run 전부 PASS**해야 receipt PASS.
- A3 async job `20260811-032348-6726-15447`, exact SHA `c70f9c7`, manifest `7c365202c9dd1501ca25df572b5b3fca35047dc51b4e091acf93d354200b13da`, copied signed binary `085039d672a4e809e5c6c1306c4aaf9ebfd564ae49c829291482bbb87d2886c4`. 10개 input 재해시 모두 PASS, first title run 진입 중. Job output under `~/BridgeVM/live-queue/{running,done}/20260811-032348-6726-15447`.
- A3 뒤 queue full-check job `20260811-032448-7775-11369` (`c70f9c7`) 대기 중. A3와 동시 실행 금지.
- A3 PASS 시 새 dated evidence + registry A3 PROVEN commit/push. 그 **최종 evidence head**에서 XCTest shim 653, full check, app package/deep-strict codesign/NOTICE gate를 다시 실행한 뒤 A11 PROVEN/24-of-24를 닫는다. A3 미달이면 threshold를 낮추지 않고 run별 summary/log/module hashes를 분석한다.
- tracked tree는 clean; `HANDOFF.md`만 operator-owned local modification이며 stage 금지.

## 0. 2026-08-10 정오 최신 상태

- 최신 제품 SHA `0f02579` (`windows: retry a lost staged reboot immediately`), origin/main push 완료.
- Hosted CI green: CI `31407167987`, Security and quality `31407167869`.
- A1 이전 원인 주장 정정: `84538cd` pilot boot1은 stage1 로그에 지연 reboot 요청과 same-BootId 재진입이 모두 있었지만 40분간 reset=0, stage1/fresh FAIL. 따라서 “duplicate task 자체가 원인” 주장은 철회. 직접 실패는 Windows의 5초 shutdown timer가 reset까지 도달하지 못한 것.
- `0f02579`는 same-BootId 재진입에서 다른 shutdown을 내리는 대신 동일 목적의 `shutdown /r /t 0 /f`를 재요청. 새 injector `~/BridgeVM/injectors/inj-a1-0f02579.raw`, assets digest `709cffa7...`.
- 새 pilot `~/BridgeVM/runs/p1pilot-a1retry-0f02579-20260810-120911` **2/2 PASS**, 각 stage4/fresh=1, reset=3, clean SYSTEM_OFF, 약 4분.
- 공식 10-boot A1 gate 진행 중: PID `96065`, output `~/BridgeVM/runs/p1gate-a1release-0f02579-20260810-121804`, 현재 2/2 PASS 후 boot3. 9/10 전에는 A1 PROVEN 금지.
- A3 verifier를 self-authored smoke에서 PPSSPP+real PSP content+side-loaded ARM64 DXVK로 교체한 SHA `70aba03`. `d3d11.dll` title-local load, title frame samples, p50>=30을 요구. A1 gate와 live 중첩 금지라 아직 실행 전.
- NOTICE 앱 최신 bundle: `~/BridgeVM/packages/BridgeVMControl-0f02579.app`; deep/strict codesign PASS, Rust closure/license text 54 packages, LGPL dylib 6개 `otool -L` dynamic consumer, `third_party_notice_gate=pass`.
- A11 최신 head: `scripts/run-xctest-shim-suites.sh` 403+188(+1 skip)+62=653 PASS. full `scripts/check-project.sh` PASS, log `~/BridgeVM/runs/check-project-0f02579.log`. 최종 registry commit 후 재확인 전에는 A11 OPEN.

## 1. Goal & status

GOAL.md 기준 프로덕션 클로즈 (레지스트리 24/24 + 패키징 + NOTICE + CI). 레지스트리 21/24, OPEN: **A1, A3, A11**.

**지금 되는 것**
- **A2 (Vulkan 58.82fps)**: PROVEN + 이 세션에서 2회 재검증 PASS (`vulkan-fps-20260809-125219`, `-213437`; canonical-a2-staged-20260807 + IK GIC + release 프로브). usgic에서는 여전히 main-window-not-observed (venus wedge, 기존 알려진 결함).
- **usgic+3D 부팅**: fence-pending 게이트(643fe0e) 후 **소크 20/20 연속** (이전 최고 8/10). canonical-a2-noparking-20260809 이미지.
- **앱 패키징**: `package-hvf-control-app.sh` → 31M .app, `codesign --verify --deep --strict` PASS. Frameworks 서드파티 dylib 13종 = THIRD-PARTY-NOTICES.md와 대조 완료.
- **THIRD-PARTY-NOTICES.md** 작성됨 (LGPL=dylib 동적링크 진술 포함, D3DMetal 배제 명시). 상용화 가능 구조 확인.
- **에이전트 재전송** (9cace27): lost-reply wedge에 3-overdue 후 재큐 (Ctl 명령 제외 — 장시간 게이트 명령은 재전송 금지).
- **게스트 커널 심볼 파이프라인**: `/tmp/ntoskrnl.exe` + `/tmp/pdb/ntkrnlmp.pdb` (msdl에서 받음) + `/tmp/pdb-publics.txt` + 매핑 스크립트(파이썬 인라인, 섹션 테이블 하드코딩) — park PC를 심볼로 즉석 해석 가능.

**안 되는 것**
- **A1 p1-boot-gate**: 최신 3라운드 8/10 → (async 실험 오염) → **6/10** (기준 9/10). 실패는 전부 `last_stage=stage1, reboots=00` — firstboot stage1에서 wedge. 세부는 §5.
- **IK GIC park**: 여전히 랜덤 발생 (A2 게이트 세션 3회 살해, 화면 체크섬 0x4ad2e258aaa5b103). fence-fix로도 안 사라짐 — IK는 A1 경로로 부적합, usgic가 유일한 길.
- **A3**: mesa-d3d10 UMD 경로 p50 24.39 (목표 30). DXVK 사이드로드 경로 배선 완료(`/tmp/a3-dxvk.sh`, gate ps1 `-ExtraArgs "--backend=DIRECT3D11"`)했으나 IK park로 실측 미완.
- **A11**: full check 1회 FAIL (vtpm flaky + 소크 경합 의심), 재실행 대기.

## 2. Decisions & why

- **V1은 구 스택(120.45 KMD + mesa venus + 우리 vendored virglrenderer)으로 닫는다.** UTM/Neptune/Triton 스택은 검증됐지만(아래) 구 UMD와 프로토콜 비호환 → V2 트랙. `~/BridgeVM/3d/prefix`는 구 스택으로 복원됨 (UTM 빌드는 `prefix-utm`, `prefix-utm-copy-20260809`에 보존).
- **상용화 라이선스 전략**: QEMU(GPL) 미배포(자체 VMM이 기본 경로이며, Compatibility Engine은 사용자가 설치한 qemu-system 바이너리를 실행할 뿐 링크·번들하지 않음) + DXMT는 미배포(향후 dylib 분리 배포 가능) + D3DMetal 금지(Apple 약관) + 나머지 MIT/BSD/Zlib + swtpm 계열 LGPL은 dylib 동적링크. → 클로즈드소스 유료 앱 가능.
- **fence 폴링은 동기 유지 + pending-비면-스킵 게이트만** (a0a4424가 최종). 근거: 폴은 pending_fenced 있을 때만 도는데, 그 창에서 게스트 KMD가 fence MSI 대기 WFI → async 완료는 소비할 exit가 없어 영원 지연 (boot-1 create3d=281/flush=0 wedge 2회 재현). idle 결합 제거는 게이트만으로 충분 (20/20 소크).
- **usgic kick은 line-edge에서만** (kick_if_line_changed, 26a762c): 이미 선 line에 재kick하면 hv_vcpus_exit가 게스트 STXR/exclusive monitor를 계속 깨서 livelock.
- **vtimer synth** (pre_run에서 CNTV 만기 감지 시 PPI 합성, 26a762c): Apple 커널이 부하에서 EXIT_VTIMER를 잃음 (258k recovery 호출에도 미도착 실측). vtimer_recovery는 IK 전용으로 격리 (usgic에서 rearm이 synth와 경합).
- **p1 게이트는 --release 필수** (p1-boot-gate.sh에 patch 완료, 2곳). --skip-build 단독은 debug 바이너리.
- **에이전트 Ctl 명령은 재전송 금지**: 단일 스레드 게스트 에이전트가 긴 명령 실행 중 무응답이 정상이라 재전송하면 이중 실행.
- **드라이버 라이브 교체 절차 확립**: WHQL 서명은 `pnputil /add-driver` + `remove-device` + `delete-driver`(비활성 후) + `scan-devices`. `/install`/`certutil`은 활성 GPU에서 hang (기존 지식 재확인).

## 3. Files modified (이 세션, 커밋 순)

리포 커밋 (모두 main, 최신이 아래):
- `26a762c`: usgic kick-edge 억제 + vtimer synth + local GDI blt live-ctx 허용(신형 KMD용, virtio_gpu_3d/submit_3d.rs+resource_3d.rs) + verify-*-fps.sh --release.
- `d9446c4`: bvinject.cmd + build-hvf-windows-driver-injector.sh에 REMOVE_DRIVER_OEMNAMES(오프라인 드라이버 제거); THIRD-PARTY-NOTICES.md.
- `9cace27`: agent_console (resident_service.rs, state.rs, harness_protocol.rs, agent_console_tests.rs) 재전송; bvgpu-real-title-gate.ps1 `-ExtraArgs`.
- `a718edf`: usgic_bridge.rs stall 리포트 (guest-relative now, voff, synth 카운터, per-vCPU cpsr/pc).
- `643fe0e`: virtio_gpu/fence.rs pending_fenced 게이트 (**핵심 픽스**).
- `72510dc`→`07ace60`→`851b3f1`→`330580b`→`a0a4424`: async fence 실험과 완전 롤백 (최종 = 643fe0e 상태 + fence_rx/tx 채널 잔존하나 동기 경로로만 사용).
- GOAL.md (신규), HANDOFF.md (이 파일).

리포 밖:
- `~/BridgeVM/work/canonical-a2-noparking-20260809.raw/-vars.fd`: powercfg CPMINCORES/CPMAXCORES=100, IDLEDISABLE=1 적용판 (core-parking 가설용이었으나 효과는 fence-fix가 냄 — 이미지 자체는 무해).
- `~/BridgeVM/work/triton-kmd-v0.2.1/`: osy v0.2.1 WHQL KMD (Neptune-era, DEV_1050 전용).
- `~/BridgeVM/3d/virglrenderer-utm/` (UTM macos-next + 우리 venus host-pointer-import 패치 병합, build-b2), `~/BridgeVM/3d/prefix-utm/`.
- `~/BridgeVM/3d/virglrenderer`: 브랜치 `bridgevm-vendor-snapshot-20260809`에 워킹트리 스냅샷 커밋 (a20cbff).
- `~/BridgeVM/injectors/inj-utm021.raw` (v0.2.1 + oem1/oem4 제거 마커).
- /tmp 스크립트: `soak-nopark.sh`(10회 usgic 소크), `p1gate-final*.log`, `a3-dxvk.sh`, `park-repro.sh`, `mk-noparking.sh`, ntoskrnl/PDB 도구.

## 4. Remaining work (다음 스텝 순)

1. **84538cd A1 2-boot pilot 결과 확인**: PID `3563`, output `~/BridgeVM/runs/p1pilot-a1base-84538cd-20260810-112142`, launcher log는 같은 경로에 `.launcher.log`. A1 전용 `wall-c8-clean-12041.raw` + `wall-c8-clean-inject-vars.fd`와 새 injector `~/BridgeVM/injectors/inj-a1-84538cd.raw`로 prepared-cache miss/injector pass가 진행 중. foreground 대기 금지.
   - 먼저 제출했던 `p1pilot-84538cd-20260810-090330`은 잘못된 canonical-a2 vars가 Boot0003 injector를 태우지 않아 `injector_boot_observed=false-booted-*`로 실패. 코드 검증 결과가 아니며 실패 기록은 보존. 미완성 cache만 안전 확인 후 삭제.
2. 이전 실패의 실제 공통 원인은 stage1이 ONSTART task 생성 직후 reboot를 예약했을 때 task가 같은 BootId에서 즉시 재진입하여 stage2 gate 실패 + 두 번째 shutdown을 예약한 경합이었다. `84538cd`는 same-BootId 재진입을 status 2(benign pending reboot)로 처리. 실패 PC 심볼은 boot3/7 `HalProcessorIdle+0x1c`(WFI 직후), boot4 `PpmEstimateIdleDuration+0x5c`; fence outstanding=0이라 GPU/fence 원인 주장은 철회.
3. pilot이 통과하면 새 injector로 **p1 10-boot 게이트** → 9/10 시 A1 PROVEN 플립 (`capabilities/windows-hvf.json` state + evidence, `render-capability-status.py` 재렌더).
3. **A3**: usgic로 (IK park 회피) `/tmp/a3-dxvk.sh` 재실행 — 단 usgic venus wedge(main-window-not-observed)가 D3D11(DXVK→vk) 경로에도 걸리는지 판정이 선행. 걸리면 A3도 IK에서 park-반복-시도 전략.
4. **A11**: 조용한 시간에 `scripts/check-project.sh` full + `run-xctest-shim-suites.sh` (653 기대). vtpm flaky 재발 시 원인(병렬 리소스 경합) 조사.
5. push + hosted CI 2 워크플로 그린 확인 (`sleep 240; gh run list --branch main --limit 2`).
6. 전부 그린이면 GOAL.md 증거 채우고 registry 렌더 + 최종 커밋.

## 5. Failed attempts & dead ends (반복 금지)

- **async fence 폴링 (72510dc, 07ace60)**: firstboot stage1 create3d=281/flush=0 wedge 2회 재현 → 완전 롤백. 이유는 §2. 재시도하려면 "완료 시 vCPU kick" 배선이 선행돼야 함 (poll_watchdog의 observe-only를 recovery kick으로 승격).
- **Group0/FIQ 가설**: 게스트가 IGROUPR0에 Group0을 요청하는지 계측 — 0건. 전부 Group1. 폐기.
- **core-parking-off 이미지 (noparking)**: 만들었고 무해하지만 park의 원인이 아니었음 (fence 결합이 원인). BPR 미반영도 검토 — threshold가 보수적이라 원인 아님.
- **kick 억제/vtimer synth만으로는 park 미해결**: 소크 8/10 유지. fence 게이트가 결정타.
- **인젝터로 v0.2.1 드라이버 스왑**: BootOrder가 인젝터를 안 태움 (a2 vars). p1-boot-gate의 prepared-cache 방식만 인젝터 부팅 성공. 라이브 스왑 절차가 더 빠름.
- **UTM virglrenderer를 구 UMD와 조합**: venus-win32 ctx SUBMIT ret=22 (프로토콜 비호환). 신구 혼용 불가 — 스택 전체를 세트로 교체해야 함.
- **VK_DRIVER_FILES 게스트 오버라이드**: 새 로더가 장치 레지스트리 ICD를 우선. DriverStore json의 bare-name LoadLibrary error 87은 System32 복사로 우회 가능 (v0.2.1 스택 실험 시).
- **vulkaninfo 없는 게스트에서 PPSSPP를 ICD 판정에 쓰기**: PPSSPP 기본 백엔드가 D3D11이라 `--backend=VULKAN` 필수. GUI-blocking 명령은 반드시 `cmd /c start`로 분리 (에이전트 워커 점유 → share/ctl 전면 마비).
- **A2를 usgic에서**: venus wedge (2nd vkCreateInstance 계열) 상존 — IK에서만 측정 가능. 반대로 부팅 신뢰성은 usgic에서만. **A1과 A2/A3가 서로 다른 GIC를 요구하는 상태가 현 최대 모순** — stage1 wedge를 잡으면 usgic로 통일 가능한지 재평가.
- **Neptune/Triton 전체 스택 검증 결과** (V2 참고): v0.2.1 WHQL KMD는 인증서 없이 클린 바인딩; 데스크톱 GDI blt는 우리 local copy 픽스(26a762c)로 동작; venus ctx도 생성됨 (capset=4, blob+submit OK — 7bcc456 핸드오프에 기록). UMD(vulkan_virtio) enumerate까지 갔고 physical device 0개는 shmem/escape 협상 이슈로 추정 — B2(레퍼런스 QEMU 재현) 없이 더 파지 말 것.
- **verify-vulkan-title-fps.sh 기본 TARGET(net-live)**: 드라이버 스테이징이 없어 module-not-loaded로 실패. canonical-a2-staged TARGET override 필수.
- **p1 게이트와 소크/풀체크 동시 실행**: 리소스 경합으로 결과 오염 (한 번 당함). 게이트류는 단독 실행.
## 2026-08-11 23:17 UTC — A3 exact-symbol qualification payload queued

- Current product head remains exact `8868b0ce4a0b7f40d4ff492a83a6b55c3690b2de`; hosted CI `31538278359`, Security `31538278358`, and exact-SHA T0 `20260811-173130-29229-4193` are PASS. The only tracked-worktree change remains this operator-owned `HANDOFF.md`; do not stage it.
- Latest shipping-payload A3 campaign `20260811-174845-78042-7768` remains immutable honest FAIL at 1/3, samples 0, receipt/public SHA-256 `283cfaf7f3fdaa93cc344461e4c045dec4a411fd6212e1d6bdf5a39db6844042`. Event 1000 record 316 reports exact official website executable `d5e587f086b96a2c1bed240f8aa3f5be50e10d68aa5f37e73fd4353265f43b3e`, module `PPSSPPWindowsARM64.exe`, exception `c0000005`, RVA `0x2eba0`. Host GPU trace/fences/P3 were healthy through the crash; no GIC trap recurred. This is not A3 FPS evidence.
- The website executable's CodeView GUID `d12885a8-23c2-424a-b84d-46dfae36d29b` has no exact PDB found. GitHub Actions tag run `25962002688` is a distinct but source-identical v1.20.4 build: executable `3e2834a0152aac5f242013cbf141e7d8fb51c9b618f75c9d4d66d8768e4c9303`, PDB `9fdd66fe67382df53388698652790e1d16ec20e3f8f841e73313e22a70344b05`, GUID `fcb1d8b5-fe27-4ccd-b397-fafb6af7b531`. Do not use that PDB for the website binary.
- Fresh source/log analysis found a concrete mixed-factory boundary. The same DXVK DLLs pass the self-authored ARM64 present smoke and log `Presenter: Actual swapchain properties`, but PPSSPP's DXVK log begins `D3D11InternalCreateDevice: Adapter is not a DXVK adapter`, creates the device through fallback, and ends before presenter creation. PPSSPP first enumerates an adapter from one `dxgi.dll` factory, passes it to `d3d11.dll`, then derives `dxgiFactory` back from that retained adapter. If the first factory is native/system while D3D11 fallback uses title-local DXVK, the derived factory cannot query DXVK's `IDXGIVkSwapChainFactory`, so `CreateSwapChain` fails and PPSSPP ignores the HRESULT before dereferencing null `swapChain_`. This is a strong, testable mechanism; the exact failing HRESULT is not yet live-captured.
- Built a private diagnostic/qualification payload from the public tag artifact with its PDB omitted (shared-file cap) and all runtime assets under the required `ppsspp/` root: `~/BridgeVM/apps/ppsspp-a3-gha-symbols-20260811.zip`, mode 0600, archive SHA-256 `b591163fde0e5150680e36c8650b5f9ee00dfaafc8d5ca73166440cab7ed0d6f`, 30,710,234 bytes, 196 entries, 69,306,516 expanded, CRC clean; embedded executable exact SHA-256 `3e2834a0152aac5f242013cbf141e7d8fb51c9b618f75c9d4d66d8768e4c9303`. The strict repository validator accepted it. No unsealed guest byte was added. The 118,190,080-byte PDB stays host-side as exact postmortem symbols at `/tmp/ppsspp-gha-arm64-unpack/PPSSPPWindowsARM64.pdb`.
- New private 10-input manifest `~/BridgeVM/manifests/a3-title-8868b0ce4a0b7f40d4ff492a83a6b55c3690b2de-gha-symbol-payload.tsv`, mode 0600, SHA-256 `76ee2f91e8f4e215925b4ffdf8cd65345fe75eac74ece5b09646f631775ca920`; only the PPSSPP payload row differs from the previous sealed manifest. First submission `20260811-191708-3564-22022` was correctly refused before input hashing/VM launch by the immutable free-space guard (`result=refused-free-space`, `available_gib=99`, no receipt/guest evidence). Only regenerated `apps/macos/.build`, generic pip/go build caches, and temporary downloaded/extracted PPSSPP work copies were removed; canonical/evidence/private payload/manifest bytes were untouched, restoring 101 GiB. Replacement T6 `20260811-192313-10177-13675` was submitted with the same exact commit, manifest and signed binary. Do not submit another live gate concurrently.
- This run is the one allowed diagnostic-value repeat: it uses a different exact executable with matching symbols. If run 1 has samples > 0, exact module identity, and p50 >= 30, allow the existing worker to continue independent runs 2/3 and 3/3. If run 1 fails, preserve the honest 1/3 receipt and classify with exact PDB; do not weaken A3 or repeat unchanged bytes.
- Exact PPSSPP fix build submitted (2026-08-11 23:30 UTC): static import inspection and retained logs support a concrete loader-order correction: PPSSPP loads `d3d11.dll` before its explicit `dxgi.dll`; the side-by-side D3D11 imports DXGI, so Windows can bind system DXGI first, yielding the observed `Adapter is not a DXVK adapter` fallback and later mixed factory. A public fork branch `Ketchio-dev/ppsspp@979fd873c58ac36704929adefb05be06ae07ad96` based on exact v1.20.4 changes only `D3D11Loader.cpp` to load DXGI before D3D11 and adds a pinned hosted ARM64 build workflow. GitHub-hosted run `31546664604` (`build-windows-arm64`, source SHA `979fd873…`) is in progress; it packages exact executable, PDB and assets. Separately, replacement exact-symbol baseline T6 `20260811-192313-10177-13675` passed all 10 worker-side input hashes and reached live run 1; this only proves sealed input acceptance so far, not title/FPS success. This is not a shipping or A3 pass claim until exact output hashes are sealed and the live 3/3 gate passes.
- The same PPSSPP diagnostic branch now includes fail-closed swap-chain handling at exact source SHA `ff0033544`: if no device DXGI factory is obtained, or `CreateSwapChain` fails/returns no object, `Init` returns false with the HRESULT in its bounded error string instead of constructing draw state and dereferencing `swapChain_`. This is independently justified by the live `0xc0000005` and does not claim to solve the underlying mixed-factory failure. Hosted build `31546664604` remains sealed to predecessor `979fd873…`; do not conflate its output with `ff0033544`. A hosted exact-source build of `ff0033544ab7c01c70457755ecda5ef20013683c` was submitted as run `31547160614` and is queued; its output will include both the loader-order candidate and fail-closed HRESULT path. Do not seal or run either artifact until its originating hosted run is green and exact hashes are recorded.
- A3 loader-order fix build sealed with exact symbols (2026-08-11 23:45 UTC) → hosted fork run `31546664604` (`Ketchio-dev/ppsspp`, source SHA `979fd873c58ac36704929adefb05be06ae07ad96`) completed success and produced artifact `9123023890` (55,616,921 bytes, ZIP SHA-256 `da04e500ce0d5049a56e500955e13b9558deab0666d4afe89380e84cd62e3f5d`). Its outputs are executable SHA-256 `e8d20718dfbd4fa0808f290667f5d1cf6cf4db69f32a188efaeff9d52dd7e3ed` (18,531,328 bytes) and PDB SHA-256 `6e4db39bca1177bff29679966ed4eda65fbf00f141749a14906feb71a0dca19d` (118,165,504 bytes). Unlike every prior PPSSPP binary in this campaign, these are an exact-symbol pair: the executable CodeView record is GUID `17EE9048-1834-4E06-839E-A8AAE71AF6F5`, age 1, `D:\a\ppsspp\ppsspp\PPSSPPWindowsARM64.pdb`, and the PDB reports the identical GUID/age. The build also cryptographically contains the intended change: PDB module 473 (`ARM64\Release\D3D11Loader.obj`) records source `D3D11Loader.cpp` SHA-256 `72466A845533DA8E016397E22834BBE28CC61A7DCFDC4E3DED58E47895CA9D98`, which is exactly the CRLF SHA-256 of the patched file at fork SHA `979fd873c` (its LF form is `539f94d6…`), so the shipped machine code was compiled from the DXGI-before-D3D11 loader order rather than upstream ordering. A strict-policy payload was sealed from that artifact with the oversized PDB kept host-side: `~/BridgeVM/apps/ppsspp-a3-dxgi-loader-fix-20260811.zip`, mode 0600, archive SHA-256 `8d3e325ebc600e2e60a450def6cd9c811473d07156ae237eb72db214a0a8be07`, 19,312,588 bytes, 189 entries, 41,020,784 expanded, CRC clean, embedded executable exactly `e8d20718…`; the repository validator accepted it and reported that same executable hash. This is a justified candidate fix with proven identity, not an A3 pass: it becomes evidence only if the unchanged 3/3 live gate passes samples>0, exact module identity and p50>=30.
- A3 exact-symbol crash proof, run `20260811-192313-10177-13675` (2026-08-11 23:45 UTC) → the replacement exact-input T6 with the exact-symbol GitHub tag payload completed honest FAIL and stopped at 1/3: `result=fail`, `exit_code=1`, `pass=false`, `run_count=1`, `required_run_count=3`, `failures=1`, `sample_count=0`, `module_identity_verified=false`; private/public receipt SHA-256 both `f982ba76ccab4945c458820ea71afa7126ee09e87d24be203e6b7ed9d2555d4b`, sealed payload `b591163fde0e5150680e36c8650b5f9ee00dfaafc8d5ca73166440cab7ed0d6f` and executable `3e2834a0152aac5f242013cbf141e7d8fb51c9b618f75c9d4d66d8768e4c9303`. Payload staging and D3D11 prep passed again (`prep=PPSSPPPAYLOADOK`, `prep=D3D11OK`) and the title reached real title-local D3D11 before Application Error event 1000 record 320: `exceptioncode=c0000005`, `faultingoffset=000000000002ded8`, module/app `C:\BridgeVM\a2-title\ppsspp\PPSSPPWindowsARM64.exe`. This closes the previous symbol gap: the executable that faulted is CodeView GUID `FCB1D8B5-FE27-4CCD-B397-FAFB6AF7B531`, age 1, and the retained PDB `9fdd66fe…` reports the identical GUID/age, so exact symbols now apply to the exact faulting image. In that PDB, `D3D11Context::GotBackbuffer` is `0001:183992` with code size 248, i.e. section-relative `0x2ceb8..0x2cfb0`; `.text` VA is `0x1000`, so the live fault RVA `0x2ded8` is section-relative `0x2ced8` and lands exactly at `GotBackbuffer+0x20`. Module 39's line table maps that routine to `D3D11Context.cpp` (source SHA-256 `35DFD132…`, exactly the CRLF hash of unmodified v1.20.4 `Windows/GPU/D3D11Context.cpp`), lines 227/229/230, where line 229 is `swapChain_->GetBuffer(0, IID_PPV_ARGS(&bbRenderTargetTex_))`. The faulting word `f94002a8` is `ldr x8, [x21]`, preceded by `f9400a75` = `ldr x21, [x19, #0x10]` (`swapChain_`). Exact symbols therefore prove, rather than corroborate, that the crash is the invalid/null `swapChain_` dereference in `GotBackbuffer` after ignored swap-chain creation failure. The prior website-build mapping is confirmed by exact symbols on an equivalent build, and the campaign correctly refused to continue to runs 2/3 after the first failure. This remains a FAIL receipt and not FPS evidence; A3 stays OPEN.
- A3 loader-fix qualification run submitted (2026-08-11 23:51 UTC) → private ten-input manifest `~/BridgeVM/manifests/a3-title-8868b0ce4a0b7f40d4ff492a83a6b55c3690b2de-dxgi-loader-fix.tsv`, mode 0600, SHA-256 `3704e4cc37ffe21078f3d29125cee16a95e3ee078cb95d003165fdf653380014`, changes only the PPSSPP row to the proven loader-fix payload `8d3e325ebc600e2e60a450def6cd9c811473d07156ae237eb72db214a0a8be07`; the other nine identities, the exact head `8868b0ce4a0b7f40d4ff492a83a6b55c3690b2de`, and the signed probe `0bfa84c292c6a2dd8087907225b41bda5224624f761c628aff036bd71614f940` are unchanged. Queue was idle with 101 GiB free; exact-input T6 `20260811-195156-60898-16982` was submitted asynchronously with no concurrent live gate. This run carries genuine diagnostic/remedial value rather than repeating identical crashing bytes: its payload provably contains the DXGI-before-D3D11 loader order. A3 criteria, renderer, timeouts and the independent 3/3 policy are unchanged, so this is evidence only if run 1 passes and runs 2/3 then also pass.
- A3 loader-order candidate falsified with its own exact symbols, run `20260811-195156-60898-16982` (2026-08-12 00:12 UTC) → the loader-fix payload completed honest FAIL at 1/3: `result=fail`, `exit_code=1`, `pass=false`, `run_count=1`, `required_run_count=3`, `sample_count=0`, `module_identity_verified=false`, receipt SHA-256 `6d01ac9af1b5f3ad63baa9fac37f628328663c63da563eb8ac4b1697b0d0a2d9`, sealed payload `8d3e325ebc600e2e60a450def6cd9c811473d07156ae237eb72db214a0a8be07`, executable `e8d20718dfbd4fa0808f290667f5d1cf6cf4db69f32a188efaeff9d52dd7e3ed`. Staging and prep passed (`prep=PPSSPPPAYLOADOK`, `prep=D3D11OK`); the guest confirmed the launched process hash was exactly `E8D20718…`, showed a foreground main window, and loaded title-local `C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, then exited after 10,173 ms with `0xc0000005`, Application Error event 1000 record 316, `faultingoffset=000000000002eba0`. Mapped with this build's own exactly matching PDB (GUID `17EE9048-1834-4E06-839E-A8AAE71AF6F5`, age 1): `.text` VA `0x1000`, so section-relative `0x2dba0`; module 39 places `D3D11Context::GotBackbuffer` at `0x2db80..0x2dc78` with line entries 227/229/230, and the faulting address is exactly `GotBackbuffer+0x20`, line 229. The faulting word is again `f94002a8` (`ldr x8, [x21]`) preceded by `f9400a75` (`ldr x21, [x19, #0x10]`). Conclusion: loading DXGI before D3D11 does not prevent the failure, so the loader-order hypothesis is falsified as a sufficient fix while the null/invalid `swapChain_` dereference at line 229 is now confirmed on two independent exact-symbol builds. The next bounded step is the already-built fail-closed variant at fork SHA `ff0033544ab7c01c70457755ecda5ef20013683c` (hosted run `31547160614` success, artifact `9123181952`), which returns the real `CreateSwapChain` HRESULT instead of dereferencing null; that run is required to capture the exact HRESULT rather than to claim a pass. A3 remains OPEN and no criterion was relaxed.
- A3 fail-closed swap-chain build sealed and submitted (2026-08-12 00:12 UTC) → hosted fork run `31547160614` (source SHA `ff0033544ab7c01c70457755ecda5ef20013683c`) completed success; artifact `9123181952` ZIP SHA-256 `b2f25fa87ab29ea1566dba0088d2945cafc6027e6b90011946101b877d05b055` yields executable SHA-256 `1d264915b619e181c614471d9665cae3e09fa2ef392a7e016f863da2d1171277` (18,531,328 bytes) and PDB SHA-256 `b76d5d48c4d5e2be8b6340b1f73c49df112c7029a7ca121fffe5836873847cc6`, an exact-symbol pair at CodeView GUID `B9EE7E6E-B34C-400C-8B20-7BE84D223069`, age 1. Both intended changes are proven present in the shipped code by PDB source hashes: module 39 records `D3D11Context.cpp` SHA-256 `FC90D93DF9A412C1981EFA92340572D41134453CE3FF83D1E6196C6BCC28DC37`, exactly the CRLF hash of the fail-closed file at `ff0033544`, and module 473 records `D3D11Loader.cpp` `72466A84…`, the DXGI-before-D3D11 order. Payload `~/BridgeVM/apps/ppsspp-a3-swapchain-failclosed-20260811.zip`, mode 0600, archive SHA-256 `9500dff9b82bef1b6276d4faf77516a75f29656c3fd26e84662fc509539ddb57`, 19,312,582 bytes, 189 entries, 41,020,784 expanded, CRC clean, embedded executable exactly `1d264915…`; the strict validator accepted it. Private manifest `a3-title-8868b0ce4a0b7f40d4ff492a83a6b55c3690b2de-swapchain-failclosed.tsv` SHA-256 `4c5e10b2f4b4728441682619d49cd356abb1e2074782b7211390d2719923f9c4` changes only the PPSSPP row. With the queue idle and 101 GiB free after removing only regenerable browser/test caches and temporary artifact copies, exact-input T6 `20260811-201204-87183-6234` was submitted on the unchanged head and signed probe. Purpose: replace the access violation with the real `CreateSwapChain` HRESULT in a bounded error string. A3 thresholds, renderer, timeouts and the 3/3 policy are unchanged.
- A3 crash eliminated and true root cause proven live, run `20260811-201204-87183-6234` (2026-08-12 00:35 UTC) → the fail-closed swap-chain payload completed honest FAIL at 1/3 (`result=fail`, `exit_code=1`, `pass=false`, `run_count=1`, `sample_count=0`, `module_identity_verified=false`, receipt SHA-256 `903bd74f1e38ed7f03db1d5f37f701c2988c15839be7cfc09e8564c680fe3709`, executable `1d264915b619e181c614471d9665cae3e09fa2ef392a7e016f863da2d1171277`), but it changed the failure class decisively. For the first time in this campaign the real title did NOT crash: the guest recorded `process_started ... executable_sha256=1D264915…`, `main_window_observed ... foreground=True`, `required_module_loaded module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, then `status=PASS pid=4380 elapsed_ms=62600` and `BVGPU-REAL-TITLE-PASS`. The reproducible `0xc0000005` at `GotBackbuffer+0x20`, present in every previous title run, is gone, which confirms that the access violation was the unchecked null `swapChain_` dereference and that fail-closed handling removes it. The identity pass then proved the actual remaining defect directly: `identity module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll sha256=DAAC69E6…` (the sealed DXVK D3D11) but `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll sha256=3D1DF347…` — the process is running title-local DXVK D3D11 against Windows' system DXGI. That is exactly the mixed-factory condition predicted from the `Adapter is not a DXVK adapter` fallback, and it is now observed live rather than inferred: swap-chain creation cannot obtain DXVK's private swap-chain factory from a system-DXGI adapter. Loading DXGI first inside PPSSPP was insufficient because the executable statically imports `dwmapi.dll`, which pulls system DXGI into the process before any application code runs. The gate correctly still fails: FPS samples are 0 because no frames render without a swap chain, and module identity is false because `dxgi.dll` is not the sealed module. A3 remains OPEN with the unchanged 3/3 policy; the next bounded step is to make the title-local DXVK `dxgi.dll` win module resolution (so both modules are the sealed pair) rather than to relax any criterion.
- A3 DXGI pairing correction committed (2026-08-12 00:35 UTC) → because the fail-closed run proved the process paired sealed DXVK `d3d11.dll` with `C:\Windows\system32\dxgi.dll`, and because the executable statically imports `dwmapi.dll` (which loads system DXGI before any application code can call `LoadLibrary`), copying DXVK beside the title cannot win module resolution by itself. The A3 verifier's D3D11 prep now also creates the per-application `PPSSPPWindowsARM64.exe.local` redirection directory and places the same already-sealed `d3d11.dll`/`dxgi.dll` bytes in it, so side-by-side redirection applies to every load of those names in that process, including indirect ones. No new unsealed byte is introduced: both DLLs are the manifest-verified copies already staged for the run, and the gate still verifies their hashes and paths in the guest. Thresholds, renderer, timeouts, title command and the independent 3/3 policy are unchanged. Local evidence on the exact tree: `bash -n` syntax PASS, `git diff --check` clean, live gate policy smoke 83 checks PASS, installed P3 GPU policy smoke PASS, structural budgets PASS, and `scripts/check-project.sh --fast` final `project check: PASS`. Committed and pushed as exact `2cbfc818733e514e9137ef783250104048b233aa` (`graphics: pair the title with DXVK DXGI via DotLocal redirection`); hosted CI `31550668102` and Security `31550668074` were started on that SHA, and exact-SHA T0 `20260811-203502-39344-17683` was submitted asynchronously before any new live title run, per the standing rule that a T6 follows a passing T0 on the same SHA.
- A3 DotLocal qualification run submitted (2026-08-12 00:50 UTC) → hosted CI `31550668102` and Security `31550668074` completed success on exact head `2cbfc818733e514e9137ef783250104048b233aa`, and exact-SHA T0 `20260811-203502-39344-17683` completed PASS (`result=pass`, `exit_code=0`, final `project check: PASS`; private/public receipt SHA-256 `348c504378a7800542f12d078f51f976ecbda72df74eb600c1f4b4f071cb6f25`/`1557584cb41d1b5e97e2d9e9d90c080304bee7b102bd50b6f4b50755d750f737`, check log `601d544601483decb89722400d28dce6465ada516c99e3a32bd25ef8b33d81d6`). A fresh release+Venus probe was built from that exact SHA in a detached worktree, ad-hoc signed with the Hypervisor entitlement, and sealed at SHA-256 `d571309364b2b4f0ff3252f8c5d3eb15f428bc7446aa8d9f072213fb2d6d7623`; `codesign --verify --strict` PASS, `com.apple.security.hypervisor` present, `virtio_gpu_3d_compiled=true`, and linkage to the sealed `libvirglrenderer.1.dylib` confirmed. New private ten-input manifest `a3-title-2cbfc818733e514e9137ef783250104048b233aa-dxgi-dotlocal.tsv`, mode 0600, SHA-256 `8da93fe242e77283cc165656b93409cf1c5ba1348db5deeb2cd2da9e289765f5`, keeps the fail-closed payload `9500dff9…` and updates only the binary row. Exact-input T6 `20260811-205044-63603-14241` was submitted asynchronously with the queue idle and 102 GiB free. This run tests one specific prediction: with DotLocal redirection the guest identity should report the sealed DXVK `dxgi.dll` beside the title instead of `C:\Windows\system32\dxgi.dll`, allowing swap-chain creation and real frames. It is evidence only if the unchanged gate reports samples>0, exact module identity and p50>=30, and then repeats independently 3/3.
- A3 DotLocal attempt refuted, and the loader boundary isolated (2026-08-12 00:58 UTC) → exact-input T6 `20260811-205044-63603-14241` on head `2cbfc818…` with sealed probe `d571309364b2b4f0ff3252f8c5d3eb15f428bc7446aa8d9f072213fb2d6d7623` and manifest `8da93fe242e77283cc165656b93409cf1c5ba1348db5deeb2cd2da9e289765f5` completed honest FAIL at 1/3: `result=fail`, `exit_code=1`, `pass=false`, `sample_count=0`, `module_identity_verified=false`, receipt SHA-256 `5d593a10e698a48fad3656f6f14dceb3f306968512b4f291ea4d067e839dc340`. The DotLocal directory was created as intended (`prep=D3D11OK` for the extended command) and the title again survived without crashing (`status=PASS`, `elapsed_ms=47203`, foreground window, title-local `d3d11.dll` loaded), confirming the earlier no-crash result reproduces. However the identity pass still reported `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll`, so `.local` redirection did not take effect for this executable, which ships its own embedded side-by-side manifest; per Windows rules an application manifest suppresses DotLocal redirection. The result is therefore a clean refutation of that one mechanism, not a threshold or policy change, and it further narrows the cause to the process's earliest module resolution. The remaining reachable lever is the static `dwmapi.dll` import proven present in the executable's import table: it forces the loader to bind dwmapi, and with it system DXGI, before any application code executes. A fork commit `2a0ed17818d8e06858c910bd10fa3da6d488f37c` now calls `DwmSetWindowAttribute` dynamically via `LoadLibraryW`/`GetProcAddress` and removes `dwmapi.lib` from all six ARM64/x64/Win32 link lines, leaving behavior identical while eliminating the early static dependency; hosted build `31552666332` is running on that exact SHA. A3 remains OPEN at 1/3 with all criteria unchanged.
- A3 dynamic-dwmapi build sealed and submitted (2026-08-12 01:27 UTC) → hosted fork run `31552666332` on exact source SHA `2a0ed17818d8e06858c910bd10fa3da6d488f37c` completed success; artifact `9125072677` ZIP SHA-256 `528333dc8bd20ac1853769355ad5dab413449bcc4ad052675e370ba67b103941` yields executable SHA-256 `1daa219cf3b7745942df50afb1a8335dc4ff152eb29b4b6db56959cf455584d0` and PDB SHA-256 `7c3f79448b865495a641007cfcc73e13fb08b7b1f149043f4ed98a8a03eb16f0`. The intended effect is verified directly in the shipped bytes rather than assumed: the import table of the previous builds began `AVRT.dll`, `dwmapi.dll`, `UxTheme.dll`, while this executable's table is `AVRT.dll`, `UxTheme.dll`, `SHLWAPI.dll`, … with no `dwmapi` entry at all, so the early static dependency that dragged system DXGI into the process before application code is gone. It retains both previous corrections (DXGI loaded before D3D11, fail-closed swap-chain handling). Sealed payload `~/BridgeVM/apps/ppsspp-a3-dynamic-dwmapi-20260811.zip`, mode 0600, archive SHA-256 `3c81608de73acafd84519f4ab1e37f08f9c5001471df1950921696705cfaf0ae`, 189 entries, 41,020,784 expanded, CRC clean, embedded executable exactly `1daa219c…`, accepted by the strict validator. Private manifest `a3-title-2cbfc818733e514e9137ef783250104048b233aa-dynamic-dwmapi.tsv` SHA-256 `64d5b6015a5b70f3a78373ec669ed9f71b803641624bcef4485de211e810e15c` changes only the PPSSPP row from the T0-passed head's manifest. Exact-input T6 `20260811-212728-6939-12557` submitted with the queue idle. Prediction under test: guest identity should now report the sealed DXVK `dxgi.dll` beside the title instead of `C:\Windows\system32\dxgi.dll`, enabling swap-chain creation and real frames. Criteria, renderer, timeouts and 3/3 policy remain unchanged.
- A3 bare-name load refuted; absolute-path loader built (2026-08-12 01:40 UTC) → exact-input T6 `20260811-212728-6939-12557` with the dynamic-dwmapi payload completed honest FAIL at 1/3 (`result=fail`, `exit_code=1`, `sample_count=0`, `module_identity_verified=false`). The build behaved exactly as intended in every respect except module choice: the launched process hash was exactly `1DAA219C…`, the title again did not crash (`status=PASS`, `elapsed_ms=60917`, foreground window, title-local `d3d11.dll` loaded), and removing the static `dwmapi` import is confirmed in the shipped import table. Yet identity still reported `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll`. Combined with the earlier refutation of `.local` redirection, this isolates the cause conclusively: `dxgi.dll` is a Windows KnownDLL, so a bare-name `LoadLibrary(L"dxgi.dll")` resolves to the system copy regardless of the application directory or DotLocal, and no amount of load ordering or import removal changes that. Three independent mechanisms have now been tested and only this one explains all observations. The corresponding fix is to load by absolute path: fork commit `4a0ac23c6f12c3d624eca9bfd46960c513e7a006` adds a small helper that resolves `d3d11.dll`/`dxgi.dll` next to the running executable via `GetModuleFileNameW` and loads that exact path when present, falling back to the previous bare-name behavior otherwise, so unmodified installations are unaffected. Hosted build `31554797189` is running on that exact SHA. A3 remains OPEN at 1/3 with no criterion, threshold, renderer or policy changed.
- A3 absolute-path DXGI build sealed and submitted (2026-08-12 02:06 UTC) → hosted fork run `31554797189` on exact source SHA `4a0ac23c6f12c3d624eca9bfd46960c513e7a006` completed success; artifact `9125863383` ZIP SHA-256 `0faa42e6a146b14b902d89367b1bed990c99f0511f22eff86186a8795523d459` yields executable SHA-256 `a19e8c6ed64c30b9c2d1ce79631e0320fcec6ad8db064f4680314a2ec2a866eb` and PDB SHA-256 `683283af84c751660f5f74e2ee9d598ef99e654a08ba36eaee00649579802b2a`. The change is proven present in the shipped code, not assumed: PDB module 471 (`ARM64\Release\D3D11Loader.obj`) records source `D3D11Loader.cpp` SHA-256 `2BEA049684911113CF87C591D8ADC1136AF24D57D49C4A63FD7B6390F657AFFB`, exactly the CRLF hash of the file at fork SHA `4a0ac23c…`. Sealed payload `~/BridgeVM/apps/ppsspp-a3-abspath-dxgi-20260811.zip`, mode 0600, archive SHA-256 `bfb171b808b6a895859e7d19c22d29ac2bcc6a862c3a0cc325192c2121e1756a`, 189 entries, 41,021,296 expanded, CRC clean, embedded executable exactly `a19e8c6e…`, accepted by the strict validator. Private manifest `a3-title-2cbfc818733e514e9137ef783250104048b233aa-abspath-dxgi.tsv` SHA-256 `78071785a97f9a59b405ea02433cc4fd55e31da9c436c373b4736f5a2bd29894` changes only the PPSSPP row. Exact-input T6 `20260811-220618-52792-29760` submitted on the T0-passed head with the queue idle. This build carries all four corrections in one binary: absolute-path module loading, DXGI before D3D11, no static dwmapi import, and fail-closed swap-chain handling.
- A3 absolute-path run result, `20260811-220618-52792-29760` (2026-08-12 02:30 UTC) → completed honest FAIL at 1/3: `result=fail`, `exit_code=1`, `pass=false`, `sample_count=0`, `module_identity_verified=false`, receipt SHA-256 `35b68f43ab627a890885d271ba1d82cb60feb96450c89b42a7c28d122b629e34`. All ten inputs verified, payload staged (`prep=PPSSPPPAYLOADOK`), D3D11 prep passed (`prep=D3D11OK`), and the exact absolute-path executable started (`process_started ... executable_sha256=A19E8C6E…`). The failure mode changed again and is newly informative: the gate exited 5 with `status=FAIL reason=required-module-not-loaded module=d3d11.dll`, meaning the process stayed alive for the full window but never loaded any `d3d11.dll`, no main window appeared, and no DXVK `d3d11.log`/`dxgi.log` was produced at all. Host state stayed healthy: the diagnostic stop captured 4/4 vCPUs (`VCPU-FINAL-STATUS: complete`) at the established `ntkrnlmp.pdb` RVA `0x481cdc` idle/WFI with no GIC trap. Because a DXVK log is written as soon as either DXVK module initializes, its complete absence shows neither sealed module was mapped this time, i.e. the absolute-path load did not produce the DXVK modules and PPSSPP took its "D3D11 unavailable" path instead of reaching device creation. The most consistent explanation with all four runs is Windows base-name module resolution: once a module named `dxgi.dll` is already present in the process, a later `LoadLibraryW` of a different file with the same base name returns the existing module, and `dxgi.dll` is additionally a KnownDLL; removing the static `dwmapi` import was therefore necessary but not sufficient, since another early dependency still maps the system copy. This run neither weakens nor changes any A3 criterion. A3 remains OPEN at 1/3 and the next bounded observation should enumerate the guest process's loaded modules to identify which dependency maps system `dxgi.dll` first, rather than guessing another link-time change.
- A3 private-assembly manifest fix built (2026-08-12 02:35 UTC) → the four live runs together identify the exact Windows rule that defeats every previous attempt: `dxgi.dll` is a KnownDLL, so bare-name loads and application-directory search both resolve to the system module, and because the executable carries an application manifest, `.local` redirection is disabled by design. The documented way to override KnownDLL resolution for a specific application is to declare the module as a private assembly file inside that manifest. Fork commit `2956fcca883bbfda7f1e07d702cf09f303a35f72` adds `<file name="dxgi.dll"/>` and `<file name="d3d11.dll"/>` to `Windows/PPSSPP.manifest`, which is inert when no such file sits beside the executable and therefore safe for ordinary installations, while making the sealed DXVK pair win when it is present. It stacks with the previously proven corrections (absolute-path loading, DXGI before D3D11, no static dwmapi import, fail-closed swap chain). Hosted build `31557013575` is running on that exact SHA. No A3 criterion, threshold, renderer, timeout or the 3/3 policy changed; A3 remains OPEN at 1/3.
- A3 private-assembly payload sealed and submitted (2026-08-12 02:46 UTC) → hosted fork run `31557013575` on exact source SHA `2956fcca883bbfda7f1e07d702cf09f303a35f72` completed success; artifact `9126556944` ZIP SHA-256 `7f8b9d2f21e31f9e2735c69cf9316216069e25664d5ed8989bae71a56b545e37` yields executable SHA-256 `58596c6254f9e3897ba9915f227560e80d0d4e6971c54b63fff955d57d7971c4`. The intended effect is verified in the shipped bytes by parsing the executable's embedded manifest, which now contains exactly `<file name="dxgi.dll"/>` and `<file name="d3d11.dll"/>`. Sealed payload `~/BridgeVM/apps/ppsspp-a3-private-assembly-20260811.zip`, mode 0600, archive SHA-256 `9261af310a60036093e5f6612035df715e0e3e20cc6e722cd45c5d4a5dbac6eb`, 189 entries, 41,021,808 expanded, CRC clean, embedded executable exactly `58596c62…`, accepted by the strict validator. Private manifest `a3-title-2cbfc818733e514e9137ef783250104048b233aa-private-assembly.tsv` SHA-256 `2ce590fb1b8e87ce274aea1cd90886f6de2864fe83be26969fed76373775f2cf` changes only the PPSSPP row. Exact-input T6 `20260811-224623-99428-10774` submitted with the queue idle on the T0-passed head `2cbfc818…` and sealed probe `d5713093…`.
- A3 mixed-factory defect ELIMINATED, run `20260811-224623-99428-10774` (2026-08-12 03:00 UTC) → the private-assembly payload completed honest FAIL at 1/3 (receipt SHA-256 `be76373a7465962de42d90277722e9327e14b3da3f8a5781a4b021722529be7b`, `sample_count=0`), but it is the first run in which the D3D11 graphics path actually works end to end. Evidence gathered from this run: the exact private-assembly executable `58596C62…` started, showed a foreground main window, loaded title-local `C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, and survived the whole window (`status=PASS`, `elapsed_ms=45293`); `identity status=PASS` with `d3d11.dll` = sealed `DAAC69E6…` and the Venus ICD resolved from the DriverStore. Decisively, DXVK's own logs are now produced and show the swap chain was created: `Presenter: Actual swapchain properties: Format VK_FORMAT_B8G8R8A8_UNORM, Present mode VK_PRESENT_MODE_FIFO_KHR, Buffer size 960x544, Image count 2`, and the `D3D11InternalCreateDevice: Adapter is not a DXVK adapter` warning that preceded every earlier failure is absent. So the chain that produced the original `0xc0000005` — system DXGI adapter → DXVK D3D11 fallback → failed `CreateSwapChain` → ignored HRESULT → null `swapChain_` dereference — is fully broken, and PPSSPP now renders through DXVK on the virtio-GPU Venus device. Note the guest still reports `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll`: the process maps both, and the DXVK DXGI is the one actually serving the device/swap chain, which is why creation succeeds. The remaining and only blocker for A3 is measurement, not graphics: `guest_fps samples=0 reason=no-fps-lines-in-frame-log`, because this PPSSPP build emits neither `fps:` lines nor the `=sceDisplaySetFrameBuf(` DEBUG lines the sampler falls back to. A3 stays OPEN at 1/3 with every criterion unchanged; the next bounded step is to obtain real per-frame samples from the title itself rather than to relax the p50 requirement.
- A3 frame-sample attribution added (2026-08-12 03:10 UTC) → with the graphics path now proven working, the only unexplained quantity was the absence of FPS samples, so the guest gate now emits one bounded, fixed-field line when and only when samples are zero: `guest_fps_absent frame_log_bytes=<n> flip_lines=<n> fps_lines=0`. It records whether the title's own frame log exists, its size, and how many per-frame lines of each kind the sampler actually saw, so a future zero-sample run is attributable to a missing log, a silent log, or a parser mismatch instead of being ambiguous. It is observation-only, runs on the existing success path, and cannot convert a failure into a pass. Local evidence on the exact tree: CRLF consistency verified (350/350), viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, structural budgets PASS, `git diff --check` clean, and `scripts/check-project.sh --fast` final `project check: PASS`. Committed and pushed as exact `620ca33e7c0fd09ff7c426d7cb72418de1c06b70` (`graphics: attribute absent frame samples`).
- A3 frame-attribution qualification run submitted (2026-08-12 03:28 UTC) → hosted CI `31559448480` and Security `31559448448` completed success on exact head `620ca33e7c0fd09ff7c426d7cb72418de1c06b70`, and exact-SHA T0 `20260811-231500-58763-11170` completed PASS (`result=pass`, `exit_code=0`). A fresh release+Venus probe was built from that exact SHA in a detached worktree, ad-hoc signed with the Hypervisor entitlement, verified (`codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage) and sealed at SHA-256 `52c6fb750c9620a0617273cac9a14607787838529a634d8274ec2fd983a783cc`. Private manifest `a3-title-620ca33e7c0fd09ff7c426d7cb72418de1c06b70-frame-attribution.tsv` SHA-256 `104c8aeb1bcfa8fb7bd0b7429598b62f501d28e60916379d01c6c2e1b6a4d3c4` keeps the private-assembly payload `9261af31…` that produced the working swap chain and updates only the binary row. Exact-input T6 `20260811-232808-80751-3764` submitted with the queue idle. Purpose: re-run the now-working D3D11 path and, if samples are still zero, capture the new bounded `guest_fps_absent` fields that state exactly why.
- A3 zero-sample cause attributed, run `20260811-232808-80751-3764` (2026-08-12 03:40 UTC) → honest FAIL at 1/3 (receipt SHA-256 `cde7d604e809bef78a7d1d214e6a0d2708eec7d313377684b5e9ad1ff97ea853`, `sample_count=0`, `module_identity_verified=false`) on head `620ca33e…` with sealed probe `52c6fb75…` and manifest `104c8aeb…`. The title again started from the exact private-assembly executable `58596C62…`, survived the full window (`status=PASS`, `elapsed_ms=45196`) and loaded title-local `d3d11.dll`, reconfirming that the D3D11/DXVK graphics path stays healthy. The new bounded diagnostic did its job and removed the ambiguity: `guest_fps_absent frame_log_bytes=80214 flip_lines=1 fps_lines=0`. The title's own log is being written (80,214 bytes) but contains exactly one `sceDisplaySetFrameBuf` flip and no `fps:` lines, so the emulator is not advancing frames during the measured window; the sampler is correct and there is genuinely nothing to sample. That is a content/emulation-progress condition, not a renderer, DXVK, module-identity or host-GPU failure, and it is distinct from every earlier failure class in this campaign. This run also surfaced a second, independent defect in the identity step: `bvgpu-d3d11-identity.ps1` failed with `The property 'ModuleName' cannot be found on this object` and exited 1, so module identity could not be evaluated at all this time. Both issues are now precisely stated and neither is addressed by weakening A3. A3 remains OPEN at 1/3 with p50>=30, exact identity, samples>0 and the independent 3/3 policy unchanged.
- A3 identity-pass defect fixed within the existing budget (2026-08-12 03:50 UTC) → the zero-sample run also proved `bvgpu-d3d11-identity.ps1` could abort before reporting any module: enumerating a live process can yield entries whose `ModuleName` is unavailable, and under `Set-StrictMode -Version Latest` that raised `The property 'ModuleName' cannot be found on this object` and exited 1, so module identity was unevaluable rather than false. The script now refreshes the process snapshot once and filters to entries that actually expose `ModuleName` before matching, so a single unreadable entry can no longer abort the pass. The first attempt at this fix grew the file to 32 lines and the structural-budget gate correctly failed the run at its 24-line ceiling; rather than raise the ceiling, the code was compacted to exactly 24/24 with no ceiling changed. Fresh evidence after the correction: structural budgets PASS (24/24), CRLF consistency verified, viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, and `scripts/check-project.sh --fast` final `project check: PASS`. Committed and pushed as exact `0e82b8083f7affc71352db75fbdd0865c2bfc780` (`graphics: read module identity without strict-mode aborts`).
- A3 zero-sample log suffix capture committed (2026-08-12 04:05 UTC) → retained evidence from `20260811-232808-80751-3764` was exhausted before another live run: A2 and A3 used byte-identical `cube.iso` (`24d3aeaa…`), the RAMFB checkpoints show only the unchanged Windows desktop because the accelerated scanout is not RAMFB, and the isolated writable disk clone was intentionally removed after the run, so the existing 80,214-byte guest PPSSPP log cannot be recovered post hoc. The gate therefore reuses its already-established failure-only `Write-FrameLogTail` routine on the zero-sample success path: at most the last 64 KiB / 16 nonempty lines, each normalized and capped at 512 characters, are emitted after `guest_fps_absent`. The same helper remains used for process-exit diagnostics, so no cap or share convention changed; the observation cannot produce samples or turn FAIL into PASS. Fresh evidence: CRLF consistent, viogpu3d asset smoke PASS, live policy 83 checks PASS, structural budgets PASS, `git diff --check` clean, and `scripts/check-project.sh --fast` final `project check: PASS`. Committed/pushed as exact `f3974e650e6a8246335621c105713e13f05ee3d5` (`graphics: report bounded frame log on zero samples`). A3 remains OPEN at 1/3; after exact hosted/T0 validation, the next T6 is diagnostic-value-bearing because it will expose the title's final state while preserving all criteria.
- A3 bounded-frame-tail T6 submitted (2026-08-12 04:25 UTC) → hosted CI `31562342214` and Security `31562342253` completed success on exact `f3974e650e6a8246335621c105713e13f05ee3d5`; exact-SHA T0 `20260812-001000-81585-16545` completed PASS (`result=pass`, `exit_code=0`). A fresh release+Venus probe was built from that exact SHA in a detached worktree, signed with the Hypervisor entitlement, strict-verified, reported `virtio_gpu_3d_compiled=true`, linked the sealed virglrenderer, and was sealed SHA-256 `d49bc3912202dc2a693a452a9a2f769778fcf08fb4a2638734eea603f87eca21`. Private ten-input manifest `a3-title-f3974e650e6a8246335621c105713e13f05ee3d5-frame-tail.tsv`, mode 0600, SHA-256 `b459e10f924b88b4ea422eec27be6a1550419cfd132e219ab0218f98dbb73480`, keeps the private-assembly PPSSPP payload `9261af31…` and changes only the binary row. With the queue idle, exact-input T6 `20260812-002503-6054-27450` was submitted. Its first run remains subject to samples>0, exact identity, p50>=30; if zero, the new fixed-cap tail must expose the final title-log state before another hypothesis is considered.
- A3 bounded-frame-tail T6 result `20260812-002503-6054-27450` (2026-08-12 04:43 UTC) → completed honest FAIL at 1/3, receipt SHA-256 `7cf2ea4fc7e8e904559fc7e8f952011ed4f4621d8f10beb7b12f64550731638d`, `sample_count=0`, `module_identity_verified=false`. All ten sealed inputs verified, payload reconstruction and D3D11 prep passed (`prep=PPSSPPPAYLOADOK`, `prep=D3D11OK`), and exact executable `58596C62…` started. This run did not reproduce the preceding zero-sample state: PPSSPP stayed alive through 45 seconds but never mapped any `d3d11.dll`, so the guest exited 5 at `status=FAIL reason=required-module-not-loaded`; no DXVK D3D11/DXGI log exists and the later FPS/identity steps correctly did not run. Host diagnostic stop captured all 4 owner-thread vCPU states without a GIC trap, and the trace had nonempty successful SUBMIT_3D with no submit failure groups, though the P3 title trace gate was red because the title never established the required path. This is a distinct title-initialization failure and neither a repeat of the working-swapchain zero-sample result nor FPS evidence. The new tail helper was not reached because this failure precedes FPS parsing; the next smallest bounded correction is to invoke that same fixed-cap helper before the existing `required-module-not-loaded` / `main-window-not-observed` exits, without altering launch, timeouts, success behavior, criteria or receipts. A3 remains OPEN at 1/3.
- A3 early-alive title-log capture committed (2026-08-12 04:50 UTC) → exact run `20260812-002503-6054-27450` showed the bounded tail must be reached before FPS parsing when the process stays alive but never loads D3D11 or presents a main window. The two existing early-alive exits now invoke the same `Write-FrameLogTail` helper immediately before `Exit-Gate 5/6`: still at most 64 KiB / 16 lines / 512 characters per line, observation-only, no command or timeout change, and no way to convert failure to pass. Fresh evidence: CRLF 356/356, viogpu3d asset smoke PASS, live policy 83 checks PASS, structural budgets PASS, `git diff --check` clean, and `scripts/check-project.sh --fast` final `project check: PASS`. Committed/pushed as exact `9fcb8c087a1b712c2824ee1c8a3f3414a74115c0` (`graphics: report title log before module failure`). A3 remains OPEN; this makes the next run diagnostic rather than an unchanged repetition.
- A3 early-frame-tail T6 submitted (2026-08-12 05:03 UTC) → hosted CI `31564415877` and Security `31564415864` completed success on exact `9fcb8c087a1b712c2824ee1c8a3f3414a74115c0`; exact-SHA T0 `20260812-004836-57853-19216` completed PASS (`result=pass`, `exit_code=0`). A fresh exact-SHA release+Venus probe was signed and strict-verified, reported `virtio_gpu_3d_compiled=true`, linked the sealed virglrenderer, and was sealed SHA-256 `42c6a3b7a7ba569726d7041a015ff47945e267a08680c61fbbb13df05ce82b7d`. Private manifest `a3-title-9fcb8c087a1b712c2824ee1c8a3f3414a74115c0-early-frame-tail.tsv`, mode 0600, SHA-256 `4f16884fa83b17243a7a749e53a04ab86d361ec044a83be21506744558eacf12`, keeps the same private-assembly payload and updates only the binary row. Exact-input T6 `20260812-010342-81780-12430` was submitted with the queue idle. This is not an unchanged repeat: whether the title reaches the working swap chain or stays before D3D11, its bounded final log state will now be observed.
- A3 blocking-readback boundary proven, run `20260812-010342-81780-12430` (2026-08-12 05:20 UTC) → honest FAIL at 1/3, receipt SHA-256 `b0029688dd6c7b67262fedc4258fcbf8eb09fff17ec343df685e09829506fd6b`, `sample_count=0`, `module_identity_verified=false`. All inputs/staging/prep passed and exact title `58596C62…` reached title-local D3D11, a foreground main window, and the full gate (`status=PASS`, `venus_icd=...d3d11.dll`); the fixed-cap diagnostic captured `frame_log_bytes=80214`, one flip, and the final 16 lines. Those lines prove the emulator was not idle: the PSP user thread issued GE display-list stall updates and draw sync, waited one vblank, latched framebuffer `04088000`, then stopped immediately after `Reading framebuffer to mem, fb_address = 04000000, ptr=...`. Exact v1.20.4 source maps that final line to `FramebufferManagerCommon::ReadbackFramebufferSync` immediately before `D3D11DrawContext::CopyFramebufferToMemory(... ReadbackMode::BLOCK, "ReadbackFramebufferSync")`; D3D11 then copies into a CPU-readable staging texture and synchronously maps it. Concurrent host evidence is not healthy for this boundary: 154 Venus submissions returned renderer status 22 (`EINVAL`); the trace groups 94 `TRANSFER3D`, 58 `END_TRANSFERS`, one `COPY_TRANSFER3D` and one resource-specific transfer failure, all `ERR_UNSPEC`, while ordinary fenced submissions still complete and P3 remains PASS. PPSSPP later disappears before identity, and the guest could not export open DXVK logs, so exact module identity was unevaluable rather than passed. This narrows the blocker from generic "one flip" to the first synchronous GPU-to-guest framebuffer readback over Venus; it does not yet prove which Venus transfer encoding is rejected, and no renderer fix is claimed. A3 remains OPEN with unchanged criteria. Next action is to map renderer status 22 to the exact rejected Venus transfer command and source invariant before changing code.
- A3 readback conclusion corrected and sharpened (2026-08-12 05:30 UTC) → two corrections to the preceding entry, made before drawing any fix conclusion. First, the Venus `renderer_status=22` submissions do NOT correlate with the stall: runs `20260811-224623-99428-10774` and `20260811-232808-80751-3764` report `SUBMIT_3D failure groups: none`, yet all three runs stop at byte-identical `frame_log_bytes=80214` with exactly one flip, so the transfer failures seen only in `20260812-010342-81780-12430` are a separate artifact and are not the blocker. Second, those failure groups are printed with VirGL command names (`crates/bridgevm-cli/src/json_util/trace_report_print.rs:99` calls `virgl_command_name`) and the bounded diagnostic they come from is filled by the VirGL decoder (`vrend_bridgevm_take_submit_diagnostic`, `src/vrend/vrend_decode.c:74`), while these contexts are Venus (`debug_name=venus-win32`, submissions served by `vkr_context_submit_cmd`, `src/venus/vkr_context.c:164`), so the names `TRANSFER3D`/`END_TRANSFERS`/`COPY_TRANSFER3D` are not trustworthy labels for a Venus payload and must not be used as proof. What the evidence does establish is stronger and backend-specific: PPSSPP's `FramebufferManagerCommon::ReadbackFramebufferSync` is backend-agnostic common code, the identical `cube.iso` on the Vulkan backend produced `samples=1641 p50=58.82`, and on the D3D11 path the title deterministically stops at the first blocking readback, immediately after `Reading framebuffer to mem`, which in D3D11 becomes `D3D11DrawContext::CopyFramebufferToMemory(... ReadbackMode::BLOCK)` copying into a `D3D11_USAGE_STAGING` texture and then synchronously mapping it (`Common/GPU/D3D11/thin3d_d3d11.cpp:1724`, "BIG GPU STALL"). DXVK logs nothing after the successful swap chain, consistent with blocking inside the map wait rather than an error return. The remaining blocker is therefore a synchronous D3D11/DXVK staging readback that never completes on this stack, not emulator progress, module identity, renderer choice or measurement. A3 remains OPEN at 1/3 with all criteria unchanged; the next step is a minimal render → staging copy → `Map(D3D11_MAP_READ)` reproduction rather than another full-title run.
- A3 generic-readback hypothesis REFUTED by existing evidence (2026-08-12 05:35 UTC) → before proposing any change, the "synchronous D3D11 staging readback cannot complete on this stack" claim was tested against retained evidence and is false. `scripts/win-tests/bridgevm-d3d11-present-smoke.c` already performs exactly that sequence on the same DXVK/Venus stack — render, `CopyResource` into a `D3D11_USAGE_STAGING` texture with `D3D11_CPU_ACCESS_READ`, then `Map(..., D3D11_MAP_READ, ...)` and per-pixel verification — and the retained run `~/BridgeVM/runs/d3d11-fps-20260809-125753` reports `BV-D3D11-PRESENT-BACKBUFFER magenta_pixels=76800 bad_pixels=0`, `BV-D3D11-PRESENT-FPS samples=30 p50=748.98` and `BV-D3D11-PRESENT-PASS`. A blocking staging map therefore demonstrably completes here, so the blocker is not blocking readback per se, and no renderer/DXVK change is justified on that basis. What stays proven is narrower: PPSSPP deterministically stops at its first `ReadbackFramebufferSync` (identical `frame_log_bytes=80214`, one flip, across three independent runs) while the same content on the Vulkan backend yields `samples=1641 p50=58.82`. The difference must lie in what PPSSPP's D3D11 path does around that readback rather than in the ability to map staging memory, and the exact source exposes a directly relevant, already-supported control: `FramebufferManagerCommon::GetSkipGPUReadbackMode()` returns `g_Config.iSkipGPUReadbackMode` (`Core/Config.cpp:781`, `SkipGPUReadbackMode`), a stock user setting. No claim is made yet about which is correct for A3; the next bounded step is to determine from the exact code path whether the stall is inside PPSSPP's own readback bookkeeping, and to prefer a stock configuration change over any binary or renderer modification. A3 remains OPEN at 1/3, all criteria unchanged.
- A3 stock skip-readback configuration committed (2026-08-12 05:45 UTC) → since the stall is specific to PPSSPP's readback while staging maps demonstrably work on this stack, the correction uses PPSSPP's own shipped `[Graphics]` option rather than modifying the emulator, DXVK or the renderer: `SkipGPUReadbackMode = 1` is added to the sealed `bv-ppsspp-d3d11.ini` (stock setting, `Core/Config.cpp:781`, consumed by `FramebufferManagerCommon::GetSkipGPUReadbackMode()`), which skips the CPU framebuffer readback where the title deterministically stops. Nothing else changed: same title command, backend, renderer, timeouts, sample source, p50>=30, exact identity and independent 3/3 policy. The first edit exceeded this file's 15-line ceiling and the budget gate failed the run; the comments were compacted to exactly 15/15 instead of raising any ceiling. Fresh evidence: structural budgets PASS (15/15), CRLF consistent, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` final `project check: PASS`. Committed/pushed as exact `d5f6e709ca91452041dafe25b6c4666681f6f1a2` (`graphics: skip the stalling framebuffer readback`). This is a hypothesis under test, not a pass: it becomes A3 evidence only if the unchanged gate then reports samples>0, exact identity and p50>=30 three independent times.
- A3 skip-readback qualification run submitted (2026-08-12 06:02 UTC) → hosted CI `31567678710` and Security `31567678741` completed success on exact `d5f6e709ca91452041dafe25b6c4666681f6f1a2`; exact-SHA T0 `20260812-014724-63008-15414` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe built, entitled, `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `0427c47f42fdde3f44ac2df664f7662dcdecf1897d77c2aa7e7ede034445e00a`. Private manifest `a3-title-d5f6e709ca91452041dafe25b6c4666681f6f1a2-skip-readback.tsv`, mode 0600, SHA-256 `6187032e80a0258d5ab6c16088785ced3b829adb7c5cf786d3fd25dea6557d57`; only the binary row changed and the PPSSPP payload stays the proven private-assembly `9261af31…`. Exact-input T6 `20260812-020236-87146-25292` submitted with the queue idle. Prediction under test: with the stalling readback skipped, the title should advance past its first flip and the sampler should derive real per-frame samples; the run is evidence only if samples>0, exact identity and p50>=30 hold, then repeat independently 3/3.
- A3 skip-readback run `20260812-020236-87146-25292` → honest FAIL (2026-08-12 06:20 UTC), `result=fail`, `exit_code=1`, receipt `run_count=1 required_run_count=3 sample_count=0 module_identity_verified=false`, so A3 stays OPEN at 1/3 and no criterion is relaxed. The failure mode changed and is informative rather than a repeat. Staging and prep were clean (`prep=PPSSPPPAYLOADOK`, `prep=D3D11OK`), the exact sealed title ran (`executable_sha256=58596C62…`), `main_window_observed handle=262298 foreground=True`, and `required_module_loaded module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll` — then the gate script itself aborted under `Set-StrictMode -Version Latest` with `The property 'Count' cannot be found on this object`, before writing any `guest_fps` line, so the harness (not the title) ended the measurement. This is diagnostic, not merely a bug: the only `.Count` on that path is `$sorted.Count` inside the `if ($fpsSamples.Count -gt 0)` branch, and `Sort-Object` returns a bare scalar exactly when it is given one element. Reaching that throw therefore proves the sampler derived precisely one `setframebuf-intervals` sample, which requires two flips — strictly more frame progress than the previous three runs that stopped at one flip with `frame_log_bytes=80214`. `SkipGPUReadbackMode = 1` thus moved the title past the readback stall, but not yet to sustained frames. Fix committed as exact `bb9cde12553aa03fe66949d70616b8ad3e64aea2` (`graphics: report a single derived frame sample`): wrap the sort as `@($fpsSamples | Sort-Object)` so a one-sample run reports instead of aborting, and always call the bounded `Write-FrameLogTail` so a too-few-samples run shows where the title stopped. This only preserves reporting — it cannot turn a failure into a pass, and p50>=30, samples>0, exact identity and independent 3/3 are untouched. Evidence: budgets PASS, CRLF consistent, asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 single-sample-reporting run submitted (2026-08-12 06:45 UTC) → hosted CI `31569951368` and Security `31569951434` both completed success on exact `bb9cde12553aa03fe66949d70616b8ad3e64aea2`; exact-SHA T0 `20260812-022845-35161-25815` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe rebuilt from a detached worktree at that SHA: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `61df28b0f8ccc0195b2b1b25171f0ae43e1b052060b0d8f262081bdf1221935c`. Private manifest `a3-title-bb9cde12553aa03fe66949d70616b8ad3e64aea2-single-sample.tsv`, mode 0600, SHA-256 `b4478371cdee07781661712535a4b4ec0175fc96927024500cf7f9adf2d01579`; only the binary row changed, and the PPSSPP payload remains the proven private-assembly `9261af31…` with `SkipGPUReadbackMode = 1` still in effect. Exact-input T6 `20260812-024516-60508-31016` submitted with the queue idle. This run has genuine diagnostic value beyond the last: whatever the sample count, the gate can now report it instead of aborting, and the always-on bounded frame tail will show exactly where the title stops if samples remain too few. It counts as A3 evidence only if samples>0, exact module identity and p50>=30 all hold.
- A3 first real frame progress, run `20260812-030516-87321-27132` (2026-08-12 07:22 UTC) → honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `512ecd6d4d77babc354ce2821255782042e4adf96aa3587dad3c06b7dc42315f`, `sample_count=0`/`run_count=1`, so A3 stays OPEN at 1/3. This is the first run in the campaign where the title actually produced measured frames: the gate logged `guest_fps samples=1 p50=6.13 p05=6.13 mean=6.13 min=6.13 max=6.13 source=setframebuf-intervals`, which confirms the earlier strict-mode inference that `SkipGPUReadbackMode = 1` moved PPSSPP past the blocking `ReadbackFramebufferSync`. Everything up to measurement was clean and exact: `prep=PPSSPPPAYLOADOK`, `prep=D3D11OK`, `executable_sha256=58596C62…`, `main_window_observed handle=262330 foreground=True`, `required_module_loaded module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, `resource_flush_count=415`, `SUBMIT_3D failure groups: none`, `P3 Windows 3D trace gate: PASS`, and the process survived `elapsed_ms=79657`. Two facts keep it a fail and neither is waived: only one interval was derived (two flips) with `p50=6.13`, far below the unchanged `p50>=30` threshold, and the always-on frame tail now ends on `Displaying FBO 04000000` after `sceDisplayWaitVblankStart`, with all 16 tail lines stamped `20:56:02x`–`20:56:034`, i.e. the title stops advancing about nine seconds into a 45-second window rather than crashing. Separately, exact module identity was again unevaluable: `bvgpu-d3d11-identity.ps1` exited 1 with `Cannot bind argument to parameter 'LiteralPath'`, because it runs after the gate and `Get-Process | Select-Object -First 1` matched a process whose `Path` was null. So the remaining blocker moved from readback to presentation/frame pacing, and identity must be captured while the title is still alive. Interim per-run behaviour also shows this stage is intermittent: runs `20260812-002503-6054-27450` and `20260812-024516-60508-31016` failed earlier with `required-module-not-loaded` and no frame log at all, while `20260812-020236-87146-25292` and this run reached module load and frames.
- A3 present-path and identity corrections committed as exact `8ae702abb53ff03f658b705152401391861a32c1` (`graphics: present without vsync and read identity from the live title`, 2026-08-12 07:40 UTC). Two independently justified changes, neither of which can turn a failure into a pass. (1) `VerticalSync = False` added to the sealed `bv-ppsspp-d3d11.ini`. This is PPSSPP's own stock `[Graphics]` option (`Core/Config.cpp:771`, default true) and it selects the present path already proven on this stack: `FrameTiming::ComputePresentMode` (`Core/FrameTiming.cpp:81`) maps vsync-off to `PresentMode::IMMEDIATE`, which `D3D11DrawContext::Present` (`Common/GPU/D3D11/thin3d_d3d11.cpp:510-523`) issues as `Present(0)`, whereas the default FIFO path issues the blocking `Present(1)`. The retained smoke `bridgevm-d3d11-present-smoke.c` passes here only via `Present(0)` (`BV-D3D11-PRESENT-FPS samples=30 p50=748.98`, `BV-D3D11-PRESENT-PASS` in `~/BridgeVM/runs/d3d11-fps-20260809-125753`), so `Present(1)` on DXVK/Venus has never been demonstrated and is exactly where the last run stopped, one frame after `Displaying FBO`. (2) `bvgpu-d3d11-identity.ps1` now selects a live instance that exposes its image path (`-not $_.HasExited -and $_.Path`) and fails closed with `exit 2` otherwise, instead of blindly taking `Select-Object -First 1` and dying on `Cannot bind argument to parameter 'LiteralPath'`; this is what left `module_identity_verified=false` even on the run that produced frames. Both edits again hit fixed ceilings and were compacted rather than granted headroom: the ini stayed at 15/15 and the identity script was folded back to exactly 24/24 (guard merged, hash helper extracted). Evidence: budgets PASS, CRLF consistent for both files, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 immediate-present qualification run submitted (2026-08-12 07:50 UTC) → hosted CI `31574473515` and Security `31574473546` completed success on exact `8ae702abb53ff03f658b705152401391861a32c1`; exact-SHA T0 `20260812-033540-95376-9624` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe rebuilt from a detached worktree: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `c1d359c8ccad84b4611b7bb49328cef5a87e8654b281681d4ed68ca5cfe7d956`. Private manifest `a3-title-8ae702abb53ff03f658b705152401391861a32c1-immediate-present.tsv`, mode 0600, SHA-256 `669cd4de86528e05170655ff0b1e3a0690a80e55fcf4b195e55afb2bdd2f3da0`; only the binary row changed and the PPSSPP payload remains the proven private-assembly `9261af31…`. Exact-input T6 `20260812-035016-19781-28340` submitted with the queue idle. Prediction under test: with `Present(0)` instead of the blocking `Present(1)`, the title should keep flipping past the point where the previous run froze, yielding many `setframebuf-intervals` samples rather than one; the live identity fix should additionally make exact module identity evaluable in the same run. Evidence only if samples>0, exact identity and p50>=30 all hold, then independently 3/3.
- A3 vsync hypothesis REFUTED, run `20260812-035016-19781-28340` (2026-08-12 08:07 UTC) → honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `639f8e965ea83daf4b8e2e2b036a996042db68ed7d7e39621697a41f75fdd210`, A3 still OPEN at 1/3. With `VerticalSync = False` the title again reached exact-title launch (`executable_sha256=58596C62…`), `main_window_observed handle=655488 foreground=True`, `required_module_loaded module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, survived `elapsed_ms=45483`, with `SUBMIT_3D failure groups: none` and `P3 Windows 3D trace gate: PASS`. The present mode did change measurably — the single derived interval moved from `p50=6.13` (FIFO) to `guest_fps samples=1 p50=13.33 source=setframebuf-intervals` (IMMEDIATE) — but the sample count did not: still exactly one interval, i.e. two flips, and the bounded tail again ends on `Displaying FBO 04000000` with every tail line stamped within the same 28 ms. So `Present(1)` versus `Present(0)` is not what stops the title; switching to IMMEDIATE only shortened the one interval that does complete. The blocker is therefore whatever follows the first presented frame after `Displaying FBO`, not the swap interval, and `p50>=30` is not approached by either mode. Identity now fails closed as designed rather than crashing: `bvgpu-d3d11-identity.ps1 exit=2`, meaning no live PPSSPP with a readable image path remained when the post-gate identity step ran; that also explains the earlier null-`Path` abort, and it means exact module identity must be captured while the title is still running rather than afterwards. `VerticalSync = False` is retained: it is a stock setting, it is now measured rather than assumed, and it removes the never-demonstrated blocking present from the path.
- A3 freeze localized to the guest, not the host GPU path (2026-08-12 08:15 UTC, analysis of run `20260812-035016-19781-28340`). The run log does contain alarming Venus fence-poll records — `suspect=renderer-did-not-retire` with `outstanding_fences=1` and gaps up to `gap_ms=59599` — but they must not be attributed to the title: every one of them, and every `venus: submit_cmd` failure, occurs strictly before the line that launches PPSSPP. Counting only records after the launch command gives `fence-poll stalls after title launch: 0` and `venus submit failures after launch: 0`, matching `SUBMIT_3D failure groups: none` and `P3 Windows 3D trace gate: PASS` for the same run. So during the measured window the host renderer accepted every submission and retired every fence, while the guest title stopped flipping after its first present. Combined with the two refuted hypotheses (blocking staging readback, which the retained present smoke performs successfully on this stack, and vsync/`Present(1)`, which only changed the one interval from 6.13 to 13.33 fps), the remaining blocker is inside the guest after the first `Displaying FBO` and is not explained by renderer errors, submission failures, fence starvation or swap interval. Also note the bounded tail is capped at 16 lines by design, so it shows where the log ends but not how the last frame's work was distributed; any further step must widen guest-side attribution without weakening the sampler, the `p50>=30` threshold, exact identity or the independent 3/3 requirement.
- A3 frozen-title attribution and live identity capture committed as exact `5e3486a8fb27c686b195f172f29fb0344a70c43c` (`graphics: attribute the frozen title and capture identity live`, 2026-08-12 08:25 UTC). Two bounded, observation-only changes that cannot convert a failure into a pass. (1) When fewer than two samples are derived, the gate now records fixed fields about the still-running title: `guest_frozen_process cpu_s=<TotalProcessorTime> threads=<n> handles=<n> responding=<bool>` plus at most four `guest_frozen_threads state=<name> count=<n>` groups taken from thread wait reasons. This is the missing discriminator — CPU time separates a spin from a block, and the dominant wait reason names what the block is on — and it is capped at four fixed-format lines. (2) The identity probe now runs from inside the gate while the title is still alive, instead of afterwards: the previous ordering produced `bvgpu-d3d11-identity.ps1 exit=2` because the process was already gone, leaving `module_identity_verified=false` even on runs that produced frames. `scripts/verify-d3d11-title-fps.sh` no longer invokes it as a separate post-gate step, and its `^identity …` parsing is unchanged, so the exact-hash comparisons for `d3d11.dll`, `dxgi.dll`, `vulkan_virtio.dll`, the process and the content still gate the result exactly as before. Evidence: structural budgets PASS (no ceiling raised), CRLF consistent, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 frozen-attribution run submitted (2026-08-12 08:33 UTC) → hosted CI `31577660626` and Security `31577660699` completed success on exact `5e3486a8fb27c686b195f172f29fb0344a70c43c`; exact-SHA T0 `20260812-042012-78460-26548` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe rebuilt in a detached worktree: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `812e3f284ef2792d8b47164d572c9339048a58d79d21ca9a02b8488286cd267a`. Private manifest `a3-title-5e3486a8fb27c686b195f172f29fb0344a70c43c-frozen-attribution.tsv`, mode 0600, SHA-256 `86cffaffe66457933d5508edcd265c41aea7cc0e3b6017d7a3345e57513bb1ad`; only the binary row changed, payload remains `9261af31…`. Exact-input T6 `20260812-043257-589-25795` submitted with the queue idle. Diagnostic value regardless of outcome: if the title freezes again the run now reports whether it is spinning or blocked and on what, and it should also produce exact module identity for the first time in this campaign because the probe runs while the title is alive.
- A3 freeze CAUSE IDENTIFIED and exact identity finally PROVEN, run `20260812-043257-589-25795` (2026-08-12 08:45 UTC) → still an honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `5ba0765961bdb163e351e01967f53db3d4b991ec45c9404693419193a04c68fc`, `guest_fps samples=1 p50=5.85`), so A3 remains OPEN at 1/3 and nothing is relaxed. Two decisive results. First, the new bounded attribution answers what the title is doing when it stops: `guest_frozen_process cpu_s=2.53 threads=32 handles=472 responding=False`, with `guest_frozen_threads state=Suspended count=31` and `state=LpcReply count=1`. Only 2.53 s of CPU across ~45 s means it is not spinning, and 31 of 32 threads Suspended with exactly one thread in `LpcReply` is the Windows Error Reporting signature: WER suspends every thread of a faulting process and the surviving thread blocks in an LPC call to the WER service. The title is therefore not deadlocked in the GPU path at all — it faults after its first present and is then frozen in place by WER, which is precisely why the log always ends immediately after `Displaying FBO`, why exactly two flips are recorded, and why the process still appears alive to the gate for the rest of the window. This supersedes both refuted hypotheses (blocking staging readback, vsync/present interval) and is consistent with the host evidence that no fence stall or submit failure occurs after launch. Second, the identity probe moved inside the gate now succeeds for the first time in this campaign: `identity status=PASS` with `process_sha256=58596C62…` matching the sealed executable, `content_sha256=24D3AEAA…` matching the sealed `cube.iso`, `module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll sha256=DAAC69E6…` (title-local DXVK) and `module=vulkan_virtio.dll` from the viogpu3d DriverStore. Note `module=dxgi.dll path=C:\Windows\system32\dxgi.dll sha256=3D1DF347…` is reported, which the verifier requires to be the title-local copy, so identity would still gate this run even with samples; the private-assembly manifest makes DXVK's DXGI serve the device while the loader still lists the system module under that name, and that discrepancy must be resolved honestly rather than by loosening the check. Next step is to capture the actual fault (Application Error/WER record for the exact faulting module and offset) after the first present, not to retry the same payload.
- A3 WER hold disabled so the fault becomes observable, exact `c90769e0450b5fa466ab3dd77534c529abf05fa5` (`graphics: let a faulting title exit instead of being held`, 2026-08-12 08:55 UTC). Because Windows Error Reporting suspends all threads of a faulting process and holds it, the crash after the first present presented itself as a live-but-frozen title, which defeated the gate's existing fault diagnostics: those only run on the `process-exited` paths, and the process never exited. The step now sets `Disabled=1` and `DontShowUI=1` under `HKLM\SOFTWARE\Microsoft\Windows\Windows Error Reporting` before launch and fails closed unless the guest echoes `prep=WEROK`. It is written with `reg add` and double-quoted key paths through `cmd.exe /c` rather than PowerShell, honouring the standing constraint that guest commands must not depend on literal single quotes or assignable path expressions — the key contains spaces, which is exactly the case that has broken quoting before. This changes no threshold, sampler, renderer, timeout or identity requirement; it only stops the OS from masking a crash as a hang, so the already-proven `process_exit_ntstatus` and Application Error event 1000 fields (faulting module, offset, exception code) can identify the actual fault. Evidence: `bash -n` syntax OK, structural budgets PASS, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 fault-capture run submitted (2026-08-12 09:15 UTC) → hosted CI `31580728769` and Security `31580728685` completed success on exact `c90769e0450b5fa466ab3dd77534c529abf05fa5` (CI appeared after the initial listing lag and was confirmed green for this exact SHA before use). Exact-SHA T0 `20260812-050256-59242-29938` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `c8150f950468afd2423c3ff134b987a657d3db447475ab95d76c4736ad6e099f`. Private manifest `a3-title-c90769e0450b5fa466ab3dd77534c529abf05fa5-fault-capture.tsv`, mode 0600, SHA-256 `376d575f931100555cfca492fb7404554e4de5a06eb883d8549faf1a7547d0e6`; only the binary row changed, payload remains `9261af31…`. Exact-input T6 `20260812-051535-80694-11609` submitted with the queue idle. With the WER hold removed the faulting title should now terminate rather than sit suspended, which routes the run into the existing `process-exited` diagnostics and should yield `process_exit_ntstatus`, the Application Error record and the faulting module plus offset — the first direct identification of what fails after the first present.
- A3 WER-disable did NOT lift the hold, run `20260812-051535-80694-11609` (2026-08-12 09:26 UTC) → honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `2cbc0cafc9644be0038246eafc874f7373a1356b900849e4fdbc17e093ca5e93`, A3 still OPEN at 1/3. The guest confirmed the new step applied (`prep=WEROK`, so both `Disabled=1` and `DontShowUI=1` were written), and the run again produced exact identity (`identity status=PASS`) plus a single derived interval (`guest_fps samples=1 p50=8.13`). But the frozen-process signature is unchanged: `guest_frozen_process cpu_s=2.31 threads=32 handles=477 responding=False` with `guest_frozen_threads state=Suspended count=31` and `state=LpcReply count=1`. So disabling WER reporting does not stop the suspend-and-hold, the process still never exits, and the `process-exited` fault diagnostics (`process_exit_ntstatus`, Application Error event 1000) still never run. Two honest consequences: the intended fault capture failed and no faulting module/offset was obtained, and the earlier WER interpretation, while consistent with the thread signature, is not yet proven to be the mechanism — an unexited process suspended with one `LpcReply` thread could also be held by a different service-side hold. The three single-sample runs now show p50 varying 5.85/8.13/13.33 with the sample count pinned at exactly 1, which is consistent with one completed frame interval followed by an immediate stop rather than a slow-but-running title. Next step must determine what actually holds the process — inspecting the live guest for a debugger/collector attachment and the Application/WER event records while the title is still suspended — rather than submitting the same payload again.
- A3 held-title fault reporting committed as exact `58ecfc4e465aaff4058d7ef00e1126d4701f7f18` (`graphics: report the fault record of a held title`, 2026-08-12 09:35 UTC). Since disabling WER did not release the process, the gate no longer depends on the title exiting to learn why it stopped: in the same too-few-samples branch it now queries the Application log for events 1000/1001 raised since launch and emits at most two fixed-format `guest_frozen_fault provider=<name> event_id=<id> text=<bounded 512 chars>` lines, or `guest_frozen_fault status=none` / `status=query-failed`. This is sound because Application Error writes event 1000 when the fault occurs rather than when the process terminates, so a suspended-and-held process still has a record to report. The probe is observation-only, bounded (max 64 events scanned, max 2 reported, fixed fields, per-line cap) and runs only when fewer than two samples were derived, so it cannot turn a failure into a pass and cannot affect a healthy run. Evidence: structural budgets PASS with no ceiling raised, CRLF consistent, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 held-fault run submitted (2026-08-12 09:56 UTC) → hosted CI `31583934067` and Security `31583934006` completed success on exact `58ecfc4e465aaff4058d7ef00e1126d4701f7f18`; exact-SHA T0 `20260812-054329-30614-7783` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `8b2241cec408d62d9584e7cd55091a6e770829f294831ab94cf2a7d3bc7d6931`. Private manifest `a3-title-58ecfc4e465aaff4058d7ef00e1126d4701f7f18-held-fault.tsv`, mode 0600, SHA-256 `1b15f02772cbcf594dbfa7b360e85b05df1724382cd131ec276261ccc0d56165`; only the binary row changed, payload remains `9261af31…`. Exact-input T6 `20260812-055611-52240-20714` submitted with the queue idle. This run should finally name the fault: even though the title stays suspended, Application Error 1000/1001 is written at fault time, so `guest_frozen_fault` should report the faulting module and exception, or explicitly `status=none` if no fault was raised — which would itself refute the crash interpretation and point at a deliberate hold instead.
- A3 crash/WER interpretation REFUTED; title is alive and idle, run `20260812-055611-52240-20714` (2026-08-12 10:25 UTC) → honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `ef5a6f01cd63a0e94bef717eeba31b5f0282b6b67eb1508da74a71d887b519ed`, `guest_fps samples=1 p50=4.42`, `identity status=PASS`, A3 still OPEN at 1/3. The widened attribution overturns the previous reading: this run reports `guest_frozen_process cpu_s=1.64 threads=34 handles=467 responding=True`, with thread states `Unknown 22`, `EventPairLow 5`, `ExecutionDelay 4`, `UserRequest 3` — no `Suspended` threads at all, the window pumping messages (`responding=True`), and no Application fault record. So the earlier `31 Suspended + 1 LpcReply` signature was not a stable crash-and-hold state, and the WER explanation does not survive: PPSSPP has not faulted, is not being held by an error collector, and is simply not advancing frames while consuming almost no CPU (1.64 s over the whole window). `EventPairLow` and `ExecutionDelay` waits indicate threads parked on events/sleeps, i.e. the emulator is waiting for something that never arrives rather than spinning or dying. Two honest caveats recorded rather than glossed: the WER-disable step (`prep=WEROK`) remains applied and is harmless but was not the fix it was intended to be, and `guest_frozen_fault status=query-failed` means the Application log query itself failed — the just-committed change replaces the id filter with a bounded unfiltered read and adds a `guest_frozen_helpers` probe, which is exactly what is needed to distinguish "no fault record" from "cannot read the log". Combined with the host evidence (no post-launch fence stalls, no submit failures, `P3 … PASS`) and the three refuted hypotheses so far (blocking readback, vsync/present interval, WER hold), the surviving explanation is that the title blocks waiting on an event tied to frame progress; the next step is to identify that wait rather than to rerun the same payload.
- A3 unfiltered application-record probe committed as exact `ad4d4ec38182714cee4192442b39bf84830d6cce` (`graphics: read application records without an id filter`, 2026-08-12 10:32 UTC). The previous probe reported `guest_frozen_fault status=query-failed`, which conflates "the title raised no fault" with "the query itself failed" — `Get-WinEvent -FilterHashtable` throws when nothing matches instead of returning an empty set, so filtering on ids 1000/1001 guaranteed a throw whenever the title had not crashed. It now reads at most 16 recent Application records with no id filter, reports up to 3 as `guest_frozen_fault provider=<name> event_id=<id> text=<bounded 384 chars>`, distinguishes genuinely empty results as `status=none` from an unreadable log as `status=no-application-records`, and adds `guest_frozen_helpers count=<n> names=<bounded>` from `WerFault`/`wermgr` so a real error-collector hold would be named rather than inferred. This keeps event id 1002 ("application hang") visible, which matters precisely because the last run showed a responsive, non-faulted, idle title. Still observation-only, still gated behind fewer than two samples, still fixed-field and bounded, so it cannot alter a verdict. Evidence: structural budgets PASS with no ceiling raised, CRLF consistent, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 application-record run submitted (2026-08-12 10:44 UTC) → hosted CI `31587382788` and Security `31587382777` completed success on exact `ad4d4ec38182714cee4192442b39bf84830d6cce`. The first T0 attempt `20260812-062843-6970-1872` was correctly refused by the live worker guard (`result=refused-free-space`, `available_gib=96`); the guard was not relaxed. Space was reclaimed only from regenerable scratch: four stale content-addressed `prepared-*` directories under `~/BridgeVM/work/p1gate-cache`, which `scripts/p1-boot-gate.sh` rebuilds automatically from `base-image.raw` + `injector.raw` (`PREPARED_KEY`/`PREPARED_DIR`, lines 226-231) and which belong to the already-closed A1 gate. No canonical image, vars, receipt, live-queue evidence or manifest was touched; free space went 96 -> 101 GiB. Re-submitted T0 `20260812-063159-10554-15214` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe: `codesign --verify --strict` PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage confirmed, sealed SHA-256 `90cbdbe003cdddc02f4ca5ec8600af474fd0dc10846156c71b7661791253ebaa`. Private manifest `a3-title-ad4d4ec38182714cee4192442b39bf84830d6cce-app-records.tsv`, mode 0600, SHA-256 `bbae47bb0fcfc6e3cf0897d3dda6796f1fb87cbd4102bf8bfeaa8d6081aafcee`; only the binary row changed, payload remains `9261af31…`. Exact-input T6 `20260812-064406-31122-6723` submitted with the queue idle. It should now report actual Application records (including a 1002 hang record if Windows considers the window stuck) or an explicit `status=none`, plus whether any WER helper process exists, which decides between "idle waiting on an event" and "held by a collector".
- A3 no-fault/no-holder result and readback candidate (2026-08-12 13:00 UTC) → exact-input run `20260812-064406-31122-6723` completed honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `e4d182d64af33cf3fac9ea7c6bd25f2230548be131a5749863f047a1056903e0`, `guest_fps samples=1 p50=9.17`, `identity status=PASS`), so A3 remains OPEN at 1/3. It decisively rules out the crash/collector interpretation: `guest_frozen_process cpu_s=1.69 threads=33 handles=469 responding=True`; waits are `Unknown 22`, `ExecutionDelay 4`, `EventPairLow 4`, `UserRequest 3`; `guest_frozen_fault status=no-application-records`; `guest_frozen_helpers count=0`; after title launch there are still no submit failures or fence stalls and P3 is PASS. The process is responsive and almost entirely idle after exactly two flips — neither faulted, WER-held, spinning nor host-GPU-starved. Re-reading exact v1.20.4 source exposes the surviving semantic boundary: the cube invokes `ReadbackFramebufferSync` because it requires the resulting PSP framebuffer bytes before it submits its next frame. `SkipGPUReadbackMode=1` bypassed the blocking operation but also withheld those bytes, explaining why it advances through one present then idles rather than producing sustained frames. The retained D3D11 smoke proves a different readback sequence works on this stack: a size-matched `D3D11_USAGE_STAGING` texture, `CopyResource`, then `Map(D3D11_MAP_READ)`. PPSSPP instead reused a 512x512 staging texture, used `CopySubresourceRegion`, and for custom `bw x bh` staging textures incorrectly retained destination/map offsets `(bx,by)`. A narrow public-fork candidate now preserves the required readback bytes while matching the proven sequence: always allocate a size-matched staging texture, use `CopyResource` for a full-frame read, otherwise copy the requested source rectangle to destination `(0,0)`, and map from byte offset zero. No BridgeVM renderer, sampler, threshold, timeout or criterion changes. Source commit is `1d22775c07d7068577445deb919fb67831fbd0ac` on `bridgevm/v1.20.4-dxgi-pair`; cherry-picked public workflow source is exact `7ef47c487680f02265560647cf36672ffc30bf23`. Hosted Windows ARM64 run `31599168576` was dispatched on that exact SHA and is in progress. No artifact is sealed or run unless that hosted run completes green and its exact hashes are recorded.
- A3 skip-readback workaround retired, exact `8d6a0220e2d6687d578a512ec9b68a67312a6c69` (`graphics: keep the framebuffer readback the title needs`, 2026-08-12 13:10 UTC). `SkipGPUReadbackMode = 1` was removed from the sealed `bv-ppsspp-d3d11.ini`, so the sealed configuration once again asks PPSSPP to perform the real CPU framebuffer readback. Justification is the measured behaviour rather than preference: skipping it did move the title past the blocking call, but the three runs that followed all stopped after exactly two flips with a responsive, idle, fault-free process, which is what withholding the framebuffer bytes the title reads back would produce. The correct place to fix a broken readback is the readback itself, and that candidate now exists on the public fork. The unrelated stock `VerticalSync = False` stays because it was measured (`p50` moved 6.13 -> 13.33 for the one completed interval) and keeps the title on the `Present(0)` path proven by the retained smoke. The file stayed within its fixed 15-line ceiling by compacting comments; no ceiling was raised. Evidence: structural budgets PASS, CRLF consistent, viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 readback-fix build green and identity sealed (2026-08-12 13:35 UTC) → hosted fork run `31599168576` on exact source SHA `7ef47c487680f02265560647cf36672ffc30bf23` completed success; artifact `9142804411` (56,317,708 bytes) downloaded as ZIP SHA-256 `4e8f6cfc22bfa16a84dedcc3ac90ba9b8e6281cc1644599bbbac92a2b2e19d9b`, yielding executable SHA-256 `90b6ef56f0ec7fc99dd2a637810314ecf8baf53e37cac2ee11411e38106fe0d6` and PDB SHA-256 `68a255884bad09c72d332972d5a0f2a7e948c8dcd46fad714ec107e69e1bfb1e`. The intended change is proven present in the shipped bytes rather than assumed: the CRLF SHA-256 of the patched `Common/GPU/D3D11/thin3d_d3d11.cpp` is `cdfaaa315cba0d075d842b4ea06d8769f7fd41de2584775549c55c34502c83da` and that exact digest appears in the PDB source-hash table (its LF form does not), so the machine code was compiled from the size-matched staging-texture readback rather than upstream ordering. This build stacks on the already-proven corrections (private-assembly DXGI pairing, absolute-path loading, no static dwmapi import, fail-closed swap chain). Repo-side, exact `8d6a0220e2d6687d578a512ec9b68a67312a6c69` is green on hosted CI `31599388694` and Security `31599388651`, and its exact-SHA release+Venus probe is sealed at SHA-256 `e53ed5ccaccf5ea8116063ebbe6569c26ab4c54feceb93148ceeecae9dbbf6d6` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage). Nothing here is an A3 pass: no payload has been sealed from this artifact, no T6 has been submitted, and the criteria (samples>0, exact identity, p50>=30, independent 3/3) are unchanged. STOPPING POINT: exact-SHA T0 `20260812-090850-13326-28620` is still running on `8d6a0220…`; the next action is to confirm that T0 PASS, seal a strict-policy payload from executable `90b6ef56…` with the oversized PDB kept host-side, copy the prior manifest changing only the ppsspp and binary rows, and submit one T6 with the queue idle.
- A3 clean stopping point confirmed (2026-08-12 13:45 UTC) → exact-SHA T0 `20260812-090850-13326-28620` on head `8d6a0220e2d6687d578a512ec9b68a67312a6c69` completed PASS (`result=pass`, `exit_code=0`). The live queue is idle, no VM is running, the working tree carries only the intentionally uncommitted operator-owned `HANDOFF.md`, and every artifact needed to resume is sealed and hash-identified: head `8d6a0220…` (CI `31599388694`, Security `31599388651`, T0 PASS), probe `e53ed5ccaccf5ea8116063ebbe6569c26ab4c54feceb93148ceeecae9dbbf6d6`, PPSSPP readback-fix executable `90b6ef56f0ec7fc99dd2a637810314ecf8baf53e37cac2ee11411e38106fe0d6` with PDB `68a255884bad09c72d332972d5a0f2a7e948c8dcd46fad714ec107e69e1bfb1e` from green fork run `31599168576` (source `7ef47c487…`). A3 remains OPEN at 1/3 with all criteria unchanged; A11 and registry 24/24 remain OPEN behind it. RESUME HERE: seal a strict-policy payload from executable `90b6ef56…` (PDB stays host-side, ZIP under the 8 MiB share convention), copy manifest `a3-title-ad4d4ec38182714cee4192442b39bf84830d6cce-app-records.tsv` changing only the `ppsspp` and `binary` rows (mode 0600), then submit one `t6-a3-title` on `8d6a0220…` with the queue idle and >=100 GiB free. Expected discriminator: if the size-matched staging readback is the real defect, the title should keep flipping past the previous two-flip ceiling and report many `setframebuf-intervals` samples; anything less stays an honest FAIL.
- A3 readback-fix qualification run submitted (2026-08-12 13:24 UTC) → strict-policy payload sealed from the green fork artifact: `~/BridgeVM/apps/ppsspp-a3-readback-fix-20260812.zip`, mode 0600, archive SHA-256 `b1d105bd562d3b2334e3b2e4179354c664c0bbc3c2945df4ba98549d28d1ec7f`, 19,496,145 bytes, 208 entries, 41,412,558 expanded, root `ppsspp/`, oversized PDB deliberately kept host-side so the 8 MiB share convention is untouched. The repository validator accepted it and independently reported embedded executable `90b6ef56f0ec7fc99dd2a637810314ecf8baf53e37cac2ee11411e38106fe0d6`, matching the hosted build. Entry comparison against the previously proven payload shows no missing files (only-in-old is empty); the extra entries are explicit directory records plus assets that ship with this artifact. Private manifest `a3-title-8d6a0220e2d6687d578a512ec9b68a67312a6c69-readback-fix.tsv`, mode 0600, SHA-256 `4465f97012738cc267fc371fa641c4643f8ca6b93acc2441013507c30ff69cc2`, changes only the `ppsspp` and `binary` rows from the last verified manifest; all nine hashable rows were re-verified locally against their files before submission (image, vars, title, ppsspp, d3d11, dxgi, virglrenderer, moltenvk, binary all `ok`), with the viogpu tree left to the worker's own directory verification. Exact-input T6 `20260812-092407-37953-781` submitted on head `8d6a0220…` with the queue idle and 101 GiB free. Discriminator: the sealed configuration now performs the real CPU framebuffer readback again, so if the size-matched staging-texture path is the actual defect the title should flip past the previous two-flip ceiling and yield many `setframebuf-intervals` samples; fewer samples, missing identity or p50<30 remains an honest FAIL and A3 stays OPEN at 1/3.
- A3 readback-fix candidate REGRESSED and is not adopted, run `20260812-092407-37953-781` (2026-08-12 13:45 UTC) → honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `78e4b958689fbe4e0f919417099337fcf9a4a5a0cdbc9f0a40e62b23b66071f6`; A3 remains OPEN at 1/3. All ten inputs were independently re-verified by the worker (`verified-inputs.tsv`, 10 rows), payload staging and prep passed (`prep=D3D11OK`, `prep=WEROK`), and the guest confirms the exact new build ran: `process_started pid=4228 executable_sha256=90B6EF56F0EC7FC99DD2A637810314ECF8BAF53E37CAC2EE11411E38106FE0D6` followed by `required_module_loaded module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll`. The outcome is strictly worse than the build it replaced: `status=FAIL reason=process-exited exit_code=1 elapsed_ms=5262`, `process_exit_ntstatus=0x00000001` (a clean error exit, not an access violation), `process_exit_event status=not-observed-within-5s`, and `frame_tail status=absent` — the title exited after 5.2 s without writing any frame log, versus ~45-80 s of life and two flips before. The trace gate also reports `P3 Windows 3D trace gate: FAIL` with blockers `missing successful RESOURCE_CREATE_BLOB` and `missing successful GET_CAPSET for VENUS capset id 4`, plus 83 failing `SUBMIT_3D` on ctx 7. That P3 failure is attributed as a consequence, not a cause: host P3 readiness passed before boot (`host_preflight=PASS`, `host_expected_capset_ok=true`, `PASS: P3 Windows GPU readiness`), `GET_CAPSET` was 13/13 successful, and `RESOURCE_CREATE_BLOB` count is 0 here versus 15 successful in the immediately preceding run on the same host, i.e. the title died before it ever allocated blob resources. Conclusion: the size-matched staging-texture readback is refuted as written — it does not restore frame progress and it introduces an early clean exit. It is therefore NOT adopted; the sealed `bv-ppsspp-d3d11.ini` change that restored the real readback stands on its own merit, and the shipping payload remains the previously proven private-assembly build. Sealed identities retained for the record: payload `b1d105bd562d3b2334e3b2e4179354c664c0bbc3c2945df4ba98549d28d1ec7f`, executable `90b6ef56…`, manifest `4465f97012738cc267fc371fa641c4643f8ca6b93acc2441013507c30ff69cc2`, probe `e53ed5cc…`, head `8d6a0220…`.
- A3 post-regression state verified clean (2026-08-12 13:55 UTC) → after refuting the readback candidate the tree and environment were checked rather than assumed. Repo head is exact `8d6a0220e2d6687d578a512ec9b68a67312a6c69` with only the operator-owned `HANDOFF.md` unstaged; the live queue is idle with no VM running; the sealed `bv-ppsspp-d3d11.ini` contains `GraphicsBackend = 1 (DIRECT3D11)` and `VerticalSync = False` and no `SkipGPUReadbackMode`, i.e. the real CPU framebuffer readback stays enabled as intended. `python3 scripts/render-capability-status.py` reports `capability registry: PASS (rendered 3 blocks, 24 criteria)` and the registry itself counts 22 PROVEN / 2 OPEN, which matches the evidence exactly: A3 and A11 remain the only OPEN criteria, and A11 cannot close before A3 because it must run at the final evidence head. One further check ruled out a plausible confound for the regression: the new readback executable and the previously proven private-assembly executable both contain the same embedded manifest `<file name="d3d11.dll"/>` and `<file name="dxgi.dll"/>` entries, so the 5.2-second clean exit is attributable to the readback edit itself and not to a lost DXVK DXGI pairing. Net position for the next session: the D3D11 title reaches real title-local DXVK, exact module identity passes, the host GPU path is healthy during the window, and the remaining defect is confined to PPSSPP's framebuffer readback semantics, where the size-matched staging rewrite is now a recorded dead end rather than an open question.
- A3 readback rewrite reverted and present/readback trace built (2026-08-12 14:05 UTC) → a branch-lineage check first removed a confound: the public `master` branch that the hosted workflow actually builds contains `bb2de9059` (DXGI before D3D11), `f0fcbea8f` (fail-closed swap chain) and `40d96563b` (private-assembly manifest), but not the two separately refuted loader commits, so the previous run's regression is attributable to the readback commit alone and not to a missing loader fix. Both the regressed and the proven executables were also confirmed to carry identical embedded `<file name="d3d11.dll"/>`/`<file name="dxgi.dll"/>` manifest entries, so DXVK pairing was intact in both. The size-matched staging rewrite `7ef47c487` is therefore reverted on `master` (`15522aac2`), restoring upstream readback behaviour exactly. In its place, exact commit `c6fa8fadc5892c4b3dde95050487d292dd2c61f3` adds a strictly bounded, observation-only trace so the next run identifies the stalling call instead of guessing: `BVPRESENT enter/done frame=<n> hr=<hresult>` for the first 8 presents, and `BVREADBACK enter/copied/mapped n=<n> hr=<hresult> pitch=<n>` for the first 8 readbacks, all via PPSSPP's own `INFO_LOG(Log::G3D, ...)` with fixed fields and hard caps. It changes no control flow, no thresholds, no renderer selection and cannot convert a failure into a pass; it only records the real `IDXGISwapChain::Present` and staging `Map` HRESULTs, which have never been observed on this stack. Hosted Windows ARM64 run `31603563659` was dispatched on that exact SHA and is in progress; no artifact will be sealed or run unless it completes green with recorded exact hashes. A3 remains OPEN at 1/3 with all criteria unchanged.
- A3 present/readback trace build sealed and submitted (2026-08-12 14:10 UTC) → hosted fork run `31603563659` on exact source SHA `c6fa8fadc5892c4b3dde95050487d292dd2c61f3` completed success; artifact `9144603439` downloaded as ZIP SHA-256 `f60c118460b56af0abc548a2e5fe70f7607f5c89b5cd0a522c2d9b452a197510`, yielding executable SHA-256 `004c9751414d95292f6480d38e1467c33a69b1ea2645642f8827cf6509ace60e` and PDB SHA-256 `2663afc69842a0e53afc2371b77a0cfd0b6fbc584ee4028ed3a080444483bfd1`. The instrumentation is proven present in the shipped bytes two independent ways rather than assumed: the literal format strings `BVPRESENT enter frame=%d interval=%d`, `BVREADBACK enter n=%d %dx%d at %d,%d mode=%d tag=%s` and `BVREADBACK mapped n=%d hr=0x%08x pitch=%u` are all found in the executable image, and the CRLF SHA-256 of the patched `Common/GPU/D3D11/thin3d_d3d11.cpp` (`1cd09169f641c579bf648420e089edf8b75cb6ca54e7f712c1781b71c6514799`) appears in the PDB source-hash table. This binary also carries the reverted readback (upstream behaviour restored) plus the three proven corrections on `master`. Strict-policy payload `~/BridgeVM/apps/ppsspp-a3-present-trace-20260812.zip`, mode 0600, archive SHA-256 `e3965fe1802b81f30786ccffb8f17cf70ca4a073517407cfa1598fca10b5b3f2`, 19,497,101 bytes, PDB kept host-side; the repository validator accepted it and independently reported embedded executable `004c9751…`. Private manifest `a3-title-8d6a0220e2d6687d578a512ec9b68a67312a6c69-present-trace.tsv`, mode 0600, SHA-256 `1d1128f7755ff53137c3a2aad5931ee80511ed9e285b99fcc20df115bf22898b`, changes only the `ppsspp` row; all nine hashable rows re-verified locally before submission. Exact-input T6 `20260812-100947-98930-30059` submitted on head `8d6a0220…` with the queue idle and 100 GiB free. Purpose is attribution, not a pass: the run should record the real `IDXGISwapChain::Present` HRESULT and the staging `Map` HRESULT/pitch for the first frames, so the stall after the first present is finally pinned to a specific call rather than inferred.
- A3 exit-1 regression re-attributed: wrong build base, not the readback (2026-08-12 14:30 UTC) → exact-input T6 `20260812-100947-98930-30059` with the reverted-readback trace build completed honest FAIL, `result=fail`, `exit_code=1`, receipt SHA-256 `8989e65e96b9146131ae25fb1d38a0ec1382ed7650e47ef4d64effd69c608457`, and reproduced the same early exit: `process_started ... executable_sha256=004C9751…`, `required_module_loaded module=d3d11.dll`, then `status=FAIL reason=process-exited exit_code=1 elapsed_ms=5413`, `process_exit_ntstatus=0x00000001`, `frame_tail status=absent`. Since the readback rewrite had been reverted in this binary, the previous attribution of that regression to the readback commit was WRONG and is retracted here rather than left standing. Direct byte comparison found the real difference: the proven 45-80 s build contains no `dwmapi.dll` string, while both exit-1 builds contain it. Branch inspection confirms why: the workflow builds `master`, and `git log ketchio/master..bridgevm/v1.20.4-dxgi-pair` shows `master` never carried `2a0ed1781` (dynamic dwmapi) or `4a0ac23c6` (absolute-path d3d11/dxgi loading); it only had equivalents of the DXGI-order, fail-closed and private-assembly changes. So both recent artifacts silently regressed to a static `dwmapi` import, which drags system DXGI into the process before application code and defeats the sealed DXVK pairing — exactly the failure mode previously proven and fixed. Corrective action taken: the bounded present/readback trace was rebased directly onto the proven payload commit `2956fcca8` as exact `6bcb24aa33061b5d7c5d447c19f28b4899ff6d64`, verified to retain upstream readback behaviour (`packTex = packTexture_` reuse present) and both trace hooks, and force-pushed to the workflow branch so the build base now matches the binary that actually reached 45-80 s of runtime. Hosted run `31606848719` is in progress on that exact SHA. No criterion, threshold, renderer or the 3/3 policy changed; A3 remains OPEN at 1/3.
- A3 trace-on-proven-base sealed and submitted (2026-08-12 14:44 UTC) → hosted fork run `31606848719` on exact source SHA `6bcb24aa33061b5d7c5d447c19f28b4899ff6d64` completed success; artifact `9145917183` ZIP SHA-256 `727dc4e7780d49fa8091eb1c673d06abba0910faea24e9bbf4d4a2e8f8e159ad`, executable SHA-256 `0ed365305bd0ad2dfa5010be7890922c2b0f9ee44911a1c3de89bba6ffd5a9e2`. The corrected build base is verified in the shipped bytes rather than assumed: `dwmapi.dll` string count is 0 (matching the proven 45-80 s build and unlike the two exit-1 builds), both trace format strings are present, and the embedded manifest still declares `<file name="d3d11.dll"/>` and `<file name="dxgi.dll"/>`. Strict-policy payload `~/BridgeVM/apps/ppsspp-a3-trace-proven-base-20260812.zip`, mode 0600, archive SHA-256 `6782822ae7776f01ec7a11b07842c1fd9df2acb6156c84086002f68eee0d58c5`, PDB kept host-side; the repository validator independently reported embedded executable `0ed36530…`. Private manifest `a3-title-8d6a0220e2d6687d578a512ec9b68a67312a6c69-trace-proven-base.tsv`, mode 0600, SHA-256 `344bb76b1e70a7e9d4d38700e7c645827d762a093cde722e9c41aa6e45bd9b28`, changes only the `ppsspp` row; all nine hashable rows re-verified locally. Exact-input T6 `20260812-104412-40643-14319` submitted on head `8d6a0220…` with the queue idle and 100 GiB free. This run should both restore the previously observed 45-80 s title lifetime and, for the first time, record the real `IDXGISwapChain::Present` HRESULT and staging `Map` HRESULT/pitch for the first frames.
- A3 EXACT BLOCKING CALL PROVEN: staging Map never returns, run `20260812-104412-40643-14319` (2026-08-12 14:55 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `b446ae822a7232617778c8bd0313c430e1b4d1f8978fa1a7a0bb97057d332f37`, `guest_fps samples=0 reason=no-fps-lines-in-frame-log`, `guest_fps_absent frame_log_bytes=80436 flip_lines=1`), A3 still OPEN at 1/3, but this run finally identifies the blocking instruction rather than inferring it. First, the build-base correction is confirmed by behaviour: title lifetime returned to `status=PASS pid=5160 elapsed_ms=46913` with `identity status=PASS`, versus the 5.2/5.4 s exit-1 of the two `dwmapi`-contaminated builds, which validates the retraction recorded above. Second, the bounded trace shows the last two frame-log lines are `BVREADBACK enter n=1 480x272 at 0,0 mode=0 tag=ReadbackFramebufferSync` (`thin3d_d3d11.cpp:1756`) followed by `BVREADBACK copied n=1, mapping` (`:1828`) — and no `BVREADBACK mapped` line ever appears. The trace emits `mapped` unconditionally immediately after `context_->Map(...)` returns, whether it succeeds or fails, so its permanent absence proves the thread is blocked *inside* `ID3D11DeviceContext::Map(packTex, 0, D3D11_MAP_READ, ...)` on the first `ReadbackFramebufferSync`, and never returns for the remaining ~45 s. This supersedes the earlier "stalls after the first present" framing: the block is the first blocking readback map itself. Honest limits: the frame tail is capped at 16 lines, so whether any `BVPRESENT` line occurred earlier cannot be determined from this artifact and is not claimed. Concurrent host state: `P3 Windows 3D trace gate: PASS`, `RESOURCE_CREATE_BLOB` 15, but 187 `venus: submit_cmd ... ret=22` (EINVAL) with failure groups of 151 + 1 + 35 on transfer-class commands; a previous run stalled identically with zero submit failures, so those EINVALs are correlated but not proven necessary. Process state during the block is consistent with a wait, not a crash or spin: `cpu_s=1.53`, `responding=True`, waits `Unknown 23`, `EventPairLow 5`, `ExecutionDelay 3`, `UserRequest 3`, `guest_frozen_fault status=no-application-records`, `guest_frozen_helpers count=0`. Next bounded step is to determine why DXVK's readback fence for that staging copy never retires on this Venus path.
- A3 ROOT CAUSE CHAIN PROVEN end to end (2026-08-12 15:05 UTC, analysis of run `20260812-104412-40643-14319`, no new live run) → the guest-side block and the host-side fault are now linked by timestamps and by renderer source, closing the gap left by the previous entry. Ordering: PPSSPP's frame log shows the blocking readback beginning at `58:04:927` (`BVREADBACK enter`/`BVREADBACK copied ..., mapping`), and the render server then logs, at `Aug 12 10:58:06` (seconds later, i.e. after the Map call had already started), the sequence `vkr: failed to look up object 200 of type 19`, `vkr: vkCmdBindPipeline resulted in CS error`, `vkr: ring_submit_cmd: vn_dispatch_command failed`. Renderer source turns that into a proven mechanism rather than a correlation: `VkObjectType` 19 is `VK_OBJECT_TYPE_PIPELINE`; in `src/venus/vkr_cs.h:320` an unresolvable object id makes the decoder call `vkr_cs_decoder_set_fatal()`; once fatal, `vkr_ring_submit_cmd` (`src/venus/vkr_ring.c:222`) and `vkr_context_submit_cmd` (`src/venus/vkr_context.c:167`) both early-bail returning false for every subsequent submission. That is exactly what the guest observes: after the decoder goes fatal the host stops executing any further Venus work for that context, so the GPU copy backing the staging texture never completes, its fence never retires, and `ID3D11DeviceContext::Map(..., D3D11_MAP_READ, ...)` blocks forever — matching the missing `BVREADBACK mapped` line, the idle-but-responsive process (`cpu_s=1.53`, `responding=True`), and the 187 `venus: submit_cmd ... ret=22` EINVAL replies that follow. Supporting counts from the same run: `RESOURCE_CREATE_BLOB` 15 OK, `RESOURCE_MAP_BLOB` 15 `OK_MAP_INFO`, `RESOURCE_UNMAP_BLOB` 351 `ERR_INVALID_PARAMETER` against only 19 OK, and transfer-class failure groups 151 + 1 + 35. Honest limits kept: the earlier stall observed with zero submit failures is not explained by this chain and is not claimed to be the same instance; and which component created pipeline id 200 (DXVK emitting a stale/never-created handle, versus the host losing it) is NOT yet established, so no fix is asserted. The defect is now localised to Venus pipeline-object lifetime on this stack rather than to PPSSPP, DXVK swap-chain setup, present mode, WER or frame pacing. A3 remains OPEN at 1/3 with every criterion unchanged.
- A3 fatal Venus command decoded exactly, and a trace-label defect recorded (2026-08-12 15:20 UTC, analysis only, no new live run) → the host retains an unambiguous line, `venus: dispatch CS error cmd=93 vkCmdBindPipeline`, and 93 is exactly `VK_COMMAND_TYPE_vkCmdBindPipeline_EXT` in `src/venus/venus-protocol/vn_protocol_renderer_defines.h:127`. Combined with `vkr: failed to look up object 200 of type 19` (`VK_OBJECT_TYPE_PIPELINE`) this fixes the failing operation precisely: the guest recorded a command buffer that binds pipeline id 200, the renderer has no such object registered, `vkr_cs_decoder_set_fatal()` fires (`src/venus/vkr_cs.h:320`), and from then on `vkr_ring_submit_cmd`/`vkr_context_submit_cmd` early-bail for that context, which is why the staging-copy fence never retires and the guest `Map` never returns. Separately, a real defect in our own tooling was found and must not be trusted in future analysis: `virtio-gpu-trace-report.txt` labels failing submissions with VirGL command names (`43(TRANSFER3D)`, `44(END_TRANSFERS)`) via `virgl_command_name` in `crates/bridgevm-cli/src/json_util/trace_report_print.rs:99`, but these are Venus contexts (`debug_name=venus-win32`) whose first dword decodes under the Venus table, where 43 is `vkDestroyEvent`, not `TRANSFER3D`. The retained `submit_prefix_hex` of the first failing submit (`seq 1564`, ctx 7, 114,372 bytes) decodes to `dword0=0x000d002b` -> cmd_type 43 -> `vkDestroyEvent`, confirming the report's VirGL labels are wrong for Venus and that earlier transfer-based readings of those groups were unsound. What is still NOT established, and is not claimed: whether pipeline 200 was never created (host `vkCreateGraphicsPipelines` failure leaves handles cleared and the object array finalised, see generated `vkr_graphics_pipeline_create_array`) or was created and destroyed early by the guest; no host pipeline-creation error was retained in this run, and DXVK's own logs could not be exported because the blocked process still held them open. That single question is the remaining gap between proven chain and actionable fix. A3 remains OPEN at 1/3, criteria unchanged.
- A3 trace mislabel fixed, exact `5d14c94b2b463f825f089a73f6b5ee65bc1a1f75` (`trace: do not name venus commands from the virgl table`, 2026-08-12 15:35 UTC). The analyzer printed VirGL opcode names for Venus submissions, which produced concretely wrong readings earlier in this campaign (`43(TRANSFER3D)` when the Venus table decodes 43 as `vkDestroyEvent`). `submit_command_name(command_id, venus)` now returns the VirGL name only when the trace has no Venus context, and reports `UNKNOWN` otherwise, so a wrong opcode can no longer be invented; the raw id is still printed for decoding against the correct protocol table. The verify-loop caught two real problems in my own change and both were fixed properly rather than waived: adding the guard pushed `trace_report_print.rs` to 193 lines over its fixed 190 ceiling, so the logic was extracted into the metrics module; that in turn pushed `trace_metrics.rs` to 132 over its 123 ceiling, so the 64-entry name list was compacted from one line per entry into a packed `concat!` literal, bringing the file to 81 lines. No ceiling was raised. The compaction was proven lossless rather than assumed: the pre-change and post-change name sequences were extracted and compared programmatically, giving `after count: 64` and `IDENTICAL ORDER AND CONTENT: True`. Evidence: `cargo test -p bridgevm-cli` 127 passed / 0 failed, `cargo fmt --check` clean, `git diff --check` clean, structural budgets PASS, and `scripts/check-project.sh --fast` final `project check: PASS`. This corrects an evidence-integrity defect in the tooling; it changes no A3 threshold, sampler, renderer or the 3/3 policy, and A3 remains OPEN at 1/3.
- A3 DXVK log capture unblocked, exact `e05fb1e70d969ad1cdb11474f8c98e431ab71a85` (`graphics: release the title so its dxvk logs can be read`, 2026-08-12 15:45 UTC). Every stalled run so far failed to export DXVK's own logs: the host copies them out of the share after the gate step, but PPSSPP was still blocked inside `Map` and holding them open, producing repeated `being used by another process` and leaving the decisive client-side view of pipeline 200 unavailable. The gate now refreshes the process and force-stops it if it is still running, after the identity probe and before returning, then waits 1.5 s so handles are released. Ordering was verified in the file rather than assumed: exact module identity is still captured while the title is alive (`identity status=PASS` depends on it), and only afterwards is the process released, so no existing evidence is weakened. This changes no threshold, sampler, renderer, timeout or the independent 3/3 policy, and it cannot turn a failure into a pass — the title has already been measured by that point. Evidence: structural budgets PASS with no ceiling raised (407 lines, file has no fixed ceiling entry), CRLF consistent, viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. Preceding commit `5d14c94b` is confirmed green on hosted CI `31611194381` and Security `31611192589`. Next run should finally yield `PPSSPPWindowsARM64_d3d11.log`, answering whether DXVK ever created pipeline 200 or bound a handle it never successfully created.
- A3 DXVK-log capture run submitted (2026-08-12 15:55 UTC) → hosted CI `31611532089` and Security `31611531120` completed success on exact `e05fb1e70d969ad1cdb11474f8c98e431ab71a85`. The first exact-SHA T0 `20260812-112123-77260-1260` was correctly refused by the immutable worker guard (`result=refused-free-space`, `available_gib=99`); the guard was not relaxed. Space was reclaimed only from regenerable scratch — three extracted PPSSPP artifact trees under `/tmp` (`a3-readback-art`, `a3-trace-art`, `a3-trace2`) that can be re-extracted from their sealed payload ZIPs at any time, plus two temporary file copies. No canonical image, vars, receipt, payload, manifest or live-queue evidence was touched; free space went 99 -> 100 GiB. Replacement T0 `20260812-112423-80619-19803` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe sealed at SHA-256 `4e09b8244980e3ebdbca27b35422a6c351e13206842114816223d503f3811853` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage). Private manifest `a3-title-e05fb1e70d969ad1cdb11474f8c98e431ab71a85-dxvk-logs.tsv`, mode 0600, SHA-256 `bdf0a94f3b7ee4656cc8ec6b5964ae0aafe3e9eab55c8330695ed35eebc2f6e6`, changes only the `binary` row and keeps the trace-instrumented payload `6782822a…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-113455-481-10058` submitted with the queue idle and 100 GiB free. Purpose is the one open question: with the title now released before the host copies the share, DXVK's `PPSSPPWindowsARM64_d3d11.log` should finally be exportable and show whether pipeline 200 was ever successfully created client-side, or whether DXVK bound a handle whose creation had failed.
- A3 DXVK client view captured; pipeline-loss reproduced and narrowed (2026-08-12 16:10 UTC) → exact-input T6 `20260812-113455-481-10058` completed honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `bf8389fbe92a439e1e4014c8ec2fcf33825a5b83a83a922faa3d03b5c9f34ff7`), A3 still OPEN at 1/3, but the release-before-copy change worked and produced the missing artifact: `PPSSPPWindowsARM64_d3d11.log` (268 lines) and `PPSSPPWindowsARM64_dxgi.log` are now exported for the first time in this campaign. The stall reproduced exactly — `guest_fps_absent frame_log_bytes=80435 flip_lines=1`, `BVREADBACK enter n=1 480x272 at 0,0 mode=0 tag=ReadbackFramebufferSync` then `BVREADBACK copied n=1, mapping` with no `mapped` line, `identity status=PASS` — and the host again reports `venus: dispatch CS error cmd=93 vkCmdBindPipeline` with `vkr: failed to look up object 194 of type 19` (id 194 here versus 200 previously, i.e. not a fixed handle but whichever pipeline is bound at that point). Decisive new fact from the client side: DXVK's own log contains no pipeline creation failure, no `VK_ERROR`, no device-lost and no validation complaint — it ends normally at successful swap chain creation (`Presenter: Actual swapchain properties ... 960x544`, `Image count: 2`), with only benign warnings (unknown `QueryInterface` GUID, EDID/colorimetry parse). So DXVK believes it created the pipeline successfully and legitimately binds it, while the renderer has no object registered under that id. That reverses the earlier open question: this is not DXVK binding a handle whose creation it saw fail. Two environment details are recorded as candidate mechanisms rather than conclusions: the device reports `graphicsPipelineLibrary: 0` with `Graphics pipeline libraries not supported`, and `pipelineCreationCacheControl: 1` with `DXVK: Using 4 compiler threads` plus a warm on-disk cache file, so pipelines are created asynchronously on compiler threads. A pipeline created on one Venus ring/thread but bound from another is a plausible loss path, but no evidence yet proves the renderer dropped it versus never receiving its creation, so no fix is asserted. A3 criteria unchanged.
- A3 pipeline-loss narrowed to one context and one submission; size and two mechanisms refuted (2026-08-12 16:25 UTC, analysis of run `20260812-113455-481-10058`, no new live run). Per-context accounting shows the failure is not diffuse: of 16 contexts only ctx 9 ever fails, and it does so only after 244 consecutive successful submissions, with the 245th (`seq 1754`, 114,372 bytes) being the first `ERR_UNSPEC`; all 183 subsequent failures are on that same context. The apparent size correlation was tested and REFUTED rather than adopted: although every OK submission on ctx 9 was <= 39,412 bytes and no submission >= 64 KiB ever succeeded, 158 of the 183 failing submissions are themselves smaller than 39,412 bytes, so small buffers fail too once the context is poisoned; and the host cap `MAX_SUBMIT_3D_BYTES` is 4 MiB (`crates/bridgevm-hvf/src/virtio_gpu_3d/protocol.rs`), which 114 KB passes, with rejection there returning `ERR_INVALID_PARAMETER` rather than the observed `ERR_UNSPEC`. So the host transport did not truncate or reject the large buffer; the renderer accepted it and then went fatal inside dispatch. Two further mechanisms were checked in source and ruled out. DXVK cannot be destroying the pipeline early: `mustTrackPipelineLifetime()` (`src/dxvk/dxvk_device.cpp:133`) returns `canUseGraphicsPipelineLibrary()` for `True` and false otherwise, and this device reports `graphicsPipelineLibrary: 0` with `Graphics pipeline libraries not supported`, so lifetime tracking is inactive regardless of the stock `dxvk.trackPipelineLifetime` option. A renderer object-table race is also not supported: insertion (`vkr_context_add_object`), validation (`vkr_context_validate_object_id`, `vkr_context.h:172`) and lookup (`vkr_cs.h:311`) all take the same per-context `object_mutex`, and ring decoders are initialised with that same table and mutex (`vkr_cs_decoder_init`, `vkr_cs.c:57-58`), so ring and context submissions share one consistent view. What remains open, and is not claimed: why that specific 114 KB command stream references a pipeline id the context never registered. A3 remains OPEN at 1/3 with all criteria unchanged.
- A3 concurrency hypothesis under test via stock DXVK config, exact `8819f0bcb741898221723f544a1b1eb5f74a4b02` (`graphics: serialise dxvk pipeline creation`, 2026-08-12 16:35 UTC). With size, host truncation, DXVK lifetime tracking and renderer object-table locking all refuted, the surviving candidate is concurrent pipeline creation: DXVK starts background compiler workers (`DxvkPipelineWorkers::startWorkers`, `src/dxvk/dxvk_pipemanager.cpp:81-110`) and the run logged `DXVK: Using 4 compiler threads`, so a pipeline can be created on a worker thread while the command stream that binds it is recorded on another. The gate now exports `DXVK_CONFIG=dxvk.numCompilerThreads=1` for the title, which `Config::getUserConfig()` reads from the environment (`src/util/config/config.cpp:1751-1752`) and which clamps the worker pool to a single thread (`numCompilerThreads > 0` overrides the core-count default). This is a stock, documented DXVK option applied purely through the environment: no sealed DXVK or PPSSPP byte changes, no new unsealed input, and the manifest identities are untouched. It also changes no A3 threshold, sampler, renderer selection, timeout or the independent 3/3 policy, and it cannot convert a failure into a pass — if the bind still references an unregistered pipeline the run fails exactly as before. Evidence: `bash -n` syntax OK, structural budgets PASS, viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. Prediction being tested: if serialising creation removes `vkr: failed to look up object ... of type 19` and the `vkCmdBindPipeline` CS error, the defect is an ordering/visibility problem between DXVK worker threads and the Venus ring; if the fatal persists unchanged, concurrency is refuted too and the next step is instrumenting pipeline create/bind ids directly.
- A3 serialised-pipeline run submitted (2026-08-12 16:45 UTC) → hosted CI `31615285429` and Security `31615285367` completed success on exact `8819f0bcb741898221723f544a1b1eb5f74a4b02`; exact-SHA T0 `20260812-120258-50367-25446` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe sealed at SHA-256 `dda9af9e8ed85fefc34f8246ef16719d3357565182237fa4c104246f3b01f3c7` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, sealed virglrenderer linkage). Private manifest `a3-title-8819f0bcb741898221723f544a1b1eb5f74a4b02-serial-pipelines.tsv`, mode 0600, SHA-256 `59b773ff09de147d2d11f0ebc7c6617494a651add9f18052b31b40d6339762a1`, changes only the `binary` row and keeps the trace-instrumented payload `6782822a…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-121410-70689-27666` submitted with the queue idle and 100 GiB free. This run is a clean discriminator on the last surviving hypothesis, since the only behavioural difference from the immediately preceding run is DXVK compiling pipelines on one worker instead of four.
- A3 concurrency hypothesis REFUTED with proof the setting applied, run `20260812-121410-70689-27666` (2026-08-12 16:55 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `b4a11b4dabca3e7a9fc7546311ace727c09a278903d1521ac0eed8a81a7f6511`), A3 still OPEN at 1/3. The stock DXVK option demonstrably took effect: its log now reports `Using 1 compiler threads` instead of the previous `Using 4 compiler threads`, so pipeline creation was serialised onto a single worker. Despite that, the failure is bit-for-bit the same behaviour: `guest_fps_absent frame_log_bytes=80436 flip_lines=1`, `BVREADBACK enter n=1 480x272 at 0,0 mode=0 tag=ReadbackFramebufferSync` then `BVREADBACK copied n=1, mapping` with no `mapped` line, `identity status=PASS`, and on the host `vkr: failed to look up object 200 of type 19`, `vkr: vkCmdBindPipeline resulted in CS error`, `venus: dispatch CS error cmd=93 vkCmdBindPipeline`, `vkr: ring_submit_cmd: vn_dispatch_command failed`. Multi-threaded pipeline creation is therefore refuted as the cause, and the `DXVK_CONFIG` serialisation will be reverted rather than kept as unjustified configuration. Cumulative refutation list for this defect now stands at: blocking staging readback semantics, vsync/present interval, WER hold, the size-matched staging rewrite, submission size and host truncation, DXVK pipeline lifetime tracking, renderer object-table locking, ring-versus-context decoder split, and DXVK worker concurrency. Note the object id recurred as 200 (as in the first occurrence) rather than 194, so the id tracks a deterministic creation order rather than a thread race. The remaining unexplained fact is precise and narrow: a `vkCmdBindPipeline` in a recorded command buffer references a pipeline id that the renderer context never registered, on a single ring, after 244 successful submissions. The next step is direct instrumentation of pipeline create/bind ids on both sides rather than another configuration experiment.
- A3 refuted setting reverted, exact `b482a8d834e4065b84a349f8e2b7986551e0988e` (`graphics: drop the refuted compiler-thread setting`, 2026-08-12 17:00 UTC). Since serialising DXVK pipeline creation demonstrably applied (`Using 1 compiler threads`) and demonstrably did not change the failure, `DXVK_CONFIG=dxvk.numCompilerThreads=1` was removed from the gate rather than left behind as unjustified configuration that would silently alter every future measurement. The revert was verified to be exact: `git diff` shows the guest command restored to its previous form with no residue, and `grep -c DXVK_CONFIG` is 0. Evidence: `bash -n` syntax OK, live gate policy smoke 83 checks PASS, structural budgets PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. Working tree now carries only the operator-owned `HANDOFF.md`, and the live queue is idle. Net state for the next session: the blocking call is proven (`Map` on the readback staging texture never returns because the Venus decoder goes fatal on `vkCmdBindPipeline` for an unregistered pipeline id), nine candidate mechanisms have been tested and eliminated, and the single remaining question is why that bind references an id the context never registered. The next step is direct instrumentation of pipeline create/bind ids on both the DXVK and renderer sides, not another configuration experiment.
- A3 renderer-side pipeline diagnostic added and submitted (2026-08-12 17:15 UTC) → source reading located a silent-drop path that exactly matches the observed signature "DXVK created it, renderer never registered it". In the generated `vkr_pipeline_add_array`, each pipeline whose driver handle comes back as `VK_NULL_HANDLE` is freed and its id is NOT inserted into the context object table, with no log at all; and `vkr_graphics_pipeline_create_array` likewise returns early after clearing handles when driver creation fails. Either path leaves an id the guest believes exists, so the later `vkCmdBindPipeline` kills the ring with no prior diagnostic — which is precisely why the host logs showed the fatal bind but never a creation error. The sealed renderer is BridgeVM's own patched virglrenderer, so a bounded, observation-only diagnostic was added in `src/venus/vkr_pipeline.c`: one fixed-field line when the whole create call fails (`BVPIPE graphics create failed ret=<n> count=<n>`), and one per individually dropped pipeline (`BVPIPE graphics pipeline <i> of <n> unregistered ret=<n>`). It changes no control flow, registers nothing new and cannot turn a failure into a pass. Built with `ninja -C build-venus` (clean link) and both strings verified present in the shipped artifacts, including `virgl_render_server`, which is the process that emits the existing `vkr:` lines. Installed into the sealed prefix after backing up the previous dylib as `libvirglrenderer.1.dylib.pre-bvpipe`; the renderer hash therefore changed from `2ed8c9a1…` to `8b3673ca98391d1da6b2bd9eefb0293c1932e79bec5b2c44b1600363f169f2ac` and the probe was rebuilt against it and re-sealed at `6d40526a4a6b0582df2746861253e57f6f10c35897ec7d0d302227cd72b1090e` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, sealed linkage confirmed). Manifest `a3-title-b482a8d8-bvpipe-diagnostic.tsv`, mode 0600, SHA-256 `5dc164cd78004fd2f861ff3a4c6b822ed1081ed1255803c861f458b6eedb2f07`, updates exactly the `virglrenderer` and `binary` rows; all nine hashable rows re-verified locally. Exact-input T6 `20260812-124009-32951-24468` submitted with the queue idle and 100 GiB free. This run should state directly whether pipeline creation failed host-side and with which VkResult, or whether creation was never attempted for that id.
- A3 TRUE ROOT CAUSE CAPTURED: host pipeline creation fails with VK_ERROR_INITIALIZATION_FAILED, run `20260812-124009-32951-24468` (2026-08-12 17:30 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `5ea9aeee1eab35c51c5f985c067fc490d6b5919580370db816d2a88ed8bc06c5`, `guest_fps samples=0`), A3 still OPEN at 1/3, but the new renderer diagnostic answered the one open question directly and in order: `vkr: BVPIPE graphics create failed ret=-3 count=1`, then `vkr: failed to look up object 167 of type 19`, then `venus: dispatch CS error cmd=93 vkCmdBindPipeline`, `vkr: vkCmdBindPipeline resulted in CS error`, `vkr: ring_submit_cmd: vn_dispatch_command failed`. VkResult -3 is `VK_ERROR_INITIALIZATION_FAILED`, and `count=1` shows the whole single-pipeline create call failed rather than one entry of a batch being dropped. The complete chain is now evidence-backed end to end: MoltenVK/Metal fails to create that one graphics pipeline -> `vkr_graphics_pipeline_create_array` clears the handles and returns without registering the id (previously silent) -> DXVK, which saw no error on its side, records and submits a command buffer binding that id -> the Venus decoder cannot resolve it and goes fatal -> `vkr_ring_submit_cmd`/`vkr_context_submit_cmd` early-bail for that context forever -> the readback staging copy never completes -> `ID3D11DeviceContext::Map` never returns -> exactly one flip and zero FPS samples. This supersedes the "renderer lost a pipeline it was given" framing: the renderer never had it, because the host driver refused to create it. The object id varies run to run (200, 194, 167) because it is whichever pipeline the title compiles at that point, which is consistent with a specific shader/state combination failing rather than a race. Note also `vkr: destroying device with valid objects` at teardown, consistent with the poisoned context never draining. Still NOT established, and not claimed: which pipeline state or shader MoltenVK rejects, and whether it is a MoltenVK limitation, a Metal capability gap or a translation defect. That is now a single, well-posed question rather than a search across the whole stack. A3 remains OPEN with every criterion unchanged.
- A3 MoltenVK rejection reason instrumented, exact `44fac487044d1e478c9cccbf74dc41a7b50fb94b` (`graphics: log why moltenvk rejects a pipeline`, 2026-08-12 17:45 UTC). With the failure now pinned to `vkCreateGraphicsPipelines` returning `VK_ERROR_INITIALIZATION_FAILED` on the host, the remaining question is which pipeline state MoltenVK refuses, and MoltenVK reports that itself at a higher log level. The A3 tier now exports `MVK_CONFIG_LOG_LEVEL` defaulting to 3 (info) alongside the existing sealed-input exports; the render server is forked from the probe and inherits it, so no code path, sealed byte or manifest identity changes and the only effect is additional host log output. The environment override is preserved (`${MVK_CONFIG_LOG_LEVEL:-3}`), and the exports were verified to resolve identically before and after the edit by executing the exact statement in isolation (`prefix=/a/b lib=/opt/mvk.dylib mvk=3`, and `mvk=1` when overridden). This adds no A3 permissiveness: thresholds, sampler, renderer selection, timeouts and the independent 3/3 policy are untouched, and a run that still fails still fails. The edit initially pushed the file to 93 lines over its fixed 90 ceiling; rather than raise the ceiling the two new export lines were folded into the existing `export` statement, bringing the file back to exactly 90/90. Evidence: `bash -n` syntax OK, structural budgets PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`.
- A3 MoltenVK-reason run submitted (2026-08-12 17:55 UTC) → hosted CI `31621541847` and Security `31621542012` completed success on exact `44fac487044d1e478c9cccbf74dc41a7b50fb94b`; exact-SHA T0 `20260812-131647-19611-8859` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe sealed at SHA-256 `2b8620c42385d00dfc804edcf88226811743d3d8f22eea5d2f3cf6ade80b9b5a` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, linkage to the BVPIPE-instrumented renderer confirmed). Private manifest `a3-title-44fac487-mvk-reason.tsv`, mode 0600, SHA-256 `11c203d6719c2be5ce3b7ac456d900b8aee68d0f986040347db291d4193b44da`, changes only the `binary` row and retains the instrumented renderer `8b3673ca…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-132756-39880-18405` submitted with the queue idle. Expected outcome is not a pass but an identification: with `BVPIPE` naming the failing create and `MVK_CONFIG_LOG_LEVEL=3` now active, the run should record MoltenVK's own explanation for the `VK_ERROR_INITIALIZATION_FAILED`, turning "some pipeline is rejected" into a specific shader or pipeline-state limitation that can then be addressed.
- A3 DEFINITIVE ROOT CAUSE: Metal shader compile fails on undeclared gl_CullDistance, run `20260812-132756-39880-18405` (2026-08-12 18:00 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `9b5c68a615cd86497b6024760e6eb0778785650e198239176902beb7ef1b6aa3`), A3 still OPEN at 1/3, but the campaign's blocking defect is now identified exactly, with the compiler's own words. MoltenVK 1.4.1 (Vulkan 1.4.334) logged: `VK_ERROR_INITIALIZATION_FAILED: Shader library compile failed (Error code 3)` followed by the Metal compiler diagnostics `program_source:259:9: error: no member named 'gl_CullDistance' in 'main0_out'` and the same at line 260 (`out.gl_CullDistance[0u] = _259;` / `[1u] = _260;`), then `Vertex shader function could not be compiled into pipeline`, and immediately after it the renderer diagnostic added earlier this session, `vkr: BVPIPE graphics create failed ret=-3 count=1`. So the generated Metal Shading Language source writes to `gl_CullDistance` members that were never declared in the output struct: SPIRV-Cross emits the stores without emitting the declaration, Metal rejects the source, `vkCreateGraphicsPipelines` returns `VK_ERROR_INITIALIZATION_FAILED`, the renderer registers no object for that id, DXVK later binds the id it believes valid, the Venus decoder goes fatal, every subsequent submission on that context early-bails, the readback fence never retires, `Map` never returns, and the title yields one flip and zero FPS samples. Every link in that chain is now backed by a captured log line rather than inference, and it explains the run-to-run variation in the failing object id (200/194/167): it is simply whichever pipeline the title compiles when it first needs a cull-distance-writing vertex shader. This is a host-side shader-translation defect in the MoltenVK/SPIRV-Cross path, not a BridgeVM renderer, DXVK, PPSSPP, present-mode, readback-semantics or measurement problem — all of which were tested and refuted earlier. The concrete fix direction is to stop that shader from writing cull distance on this stack: DXVK exposes the Vulkan `shaderCullDistance` feature to D3D11 clip planes, so the next step is to determine whether the feature can be withheld or the emitted MSL corrected, rather than to run the same payload again. A3 remains OPEN with all criteria unchanged.
- A3 defect ownership established: MoltenVK advertises shaderCullDistance then fails to compile it (2026-08-12 18:15 UTC, analysis, no new live run). Four checks were made before proposing any fix, and they move the defect off BridgeVM and off DXVK. (1) DXVK does not opportunistically enable the feature: `dxvk_device_info.cpp:846` lists `ENABLE_FEATURE(core.features, shaderCullDistance, true)` in its REQUIRED set, so a device that cannot honour it cannot be used by DXVK at all; withholding the feature is therefore not a usable workaround, it would break device creation outright. (2) DXVK only emits the built-in when the application's own shader uses it — the references in `dxvk_shader_io.cpp:126`, `dxvk_shader_spirv.cpp:587` and `dxvk_shader_ir.cpp:1837/2412` are pass-through translation of an existing SPIR-V built-in, not an injected workaround, so PPSSPP's shader legitimately writes two cull distances. (3) MoltenVK exposes no configuration knob for this: the full `MVK_CONFIG_*` string table in the shipped 1.4.1 dylib contains nothing cull- or clip-distance related, so there is no supported toggle to disable or work around it. (4) Decisively, MoltenVK claims the capability. A minimal host program was written that dlopens the exact sealed `/opt/homebrew/lib/libMoltenVK.dylib`, creates an instance, enumerates the physical device and reads `VkPhysicalDeviceFeatures`; it reports `shaderClipDistance=1 shaderCullDistance=1`. So MoltenVK 1.4.1 advertises cull-distance support, DXVK reasonably relies on that advertisement, and MoltenVK then emits Metal source that stores to `out.gl_CullDistance[0u]`/`[1u]` without declaring the member in `main0_out`, which its own Metal compile rejects with error code 3. That is a defect in the MoltenVK/SPIRV-Cross MSL emission path, in a Homebrew-installed binary with no local source tree, and it is the single remaining blocker for A3. It is not a BridgeVM renderer, transport, PPSSPP, DXVK, present-mode, readback or measurement problem — all previously tested and refuted. A3 remains OPEN at 1/3 with every criterion unchanged.
- A3 blocker reproduced standalone and 1.4.2 upgrade REFUTED as a fix (2026-08-12 18:30 UTC, host analysis, no live run). The Metal compile failure was reproduced outside the whole VM stack, which removes any doubt that it is BridgeVM-specific. A four-line GLSL vertex shader declaring `gl_CullDistance[2]` was compiled to SPIR-V with `glslangValidator` and translated with the installed `MoltenVKShaderConverter` (1.4.1): the emitted `main0_out` struct declares only `gl_CullDistance_0 [[user(cull0)]]` and `gl_CullDistance_1 [[user(cull1)]]`, yet the body writes `out.gl_CullDistance[0]` and `out.gl_CullDistance[1]` — an array member that was never declared. That is byte-for-byte the live failure (`no member named 'gl_CullDistance' in 'main0_out'` at the two store lines). A second shader using cull distance WITHOUT clip distance reproduces identically, so the defect is unconditional rather than a clip+cull interaction. The obvious remedy was then tested and refuted rather than assumed: MoltenVK 1.4.2 is available upstream, its Homebrew bottle was downloaded to a scratch directory (SHA-256 `5a83a9421fb588695613464a75ea469443f9aef72db475c3b2ae64dccc97c98a`, matching Homebrew's published digest exactly, extracted `libMoltenVK.dylib` `480a857b63f62e1ecf0e0a314d07b42614e112b6ad746c128a706699b775d765`), and its bundled converter produces the SAME broken output — `gl_CullDistance_0`/`_1` declared, `out.gl_CullDistance[0]`/`[1]` written. So upgrading MoltenVK does not fix A3, and the sealed 1.4.1 was deliberately left untouched rather than swapped on a hopeful basis. Ownership is therefore confirmed upstream: MoltenVK advertises `shaderCullDistance=1` (verified directly against the sealed dylib) while its MSL emitter cannot compile a shader that uses it. The remaining options are to patch the emission path in a locally built MoltenVK, or to prevent this shader reaching it; neither is asserted as chosen yet. A3 remains OPEN at 1/3, all criteria unchanged.
- A3 upstream defect fully characterised; reproducer preserved (2026-08-12 18:40 UTC). Additional checks closed the last alternatives. The MSL target version is irrelevant: converting the cull-only shader with `-mv 2.0`, `2.3` and `3.0` each produce zero declarations of a `gl_CullDistance` array member and two writes to `out.gl_CullDistance[...]`, so there is no MSL level at which the emitter is correct. The standalone converter is not a red herring either: the shipped runtime `libMoltenVK.dylib` contains the same emitter strings (`[[user(cull`, `cull_distance`, `GL_EXT_clip_cull_distance`, and four `gl_CullDistance` references), and the live run's Metal compiler error is textually identical to the standalone output, so the tool and the runtime share the defective path. The reproducer and its outputs are preserved as durable evidence under `~/BridgeVM/evidence/mvk-cull-distance/` (`cullonly.vert`, `cullonly.metal` SHA-256 `45d8b3956a6aab792624f56f6c7c34765648bce958a36116c7d8cd4d1021b384`, plus the clip+cull variant and both 1.4.1/1.4.2 translations). Final status of this investigation: A3's blocker is a MoltenVK MSL emission bug affecting every shader that writes `gl_CullDistance`, present in 1.4.1 and unchanged in 1.4.2, in a component that advertises the capability it cannot compile. BridgeVM's own stack is exonerated by evidence at every layer — host GPU transport healthy during the window, `P3 ... PASS`, renderer bookkeeping consistent, DXVK behaving per spec, PPSSPP reaching real title-local D3D11 with exact module identity `identity status=PASS`. The remaining work is upstream-facing (patch and locally build MoltenVK's emitter, or prevent cull-distance shaders reaching it), not a BridgeVM correctness fix, and it must still be proven by the unchanged 3/3 live gate before A3 can close. A3 remains OPEN at 1/3.
- A3 fix built at the correct layer: PPSSPP stops using cull distance on Venus (2026-08-12 18:55 UTC). Because the MoltenVK emitter defect cannot be configured away and exists in both 1.4.1 and 1.4.2, the remaining honest lever is to stop generating the shader that triggers it. Reading exact v1.20.4 source shows PPSSPP already gates this correctly in principle: `GPUCommonHW.cpp:602` sets `GPU_USE_CULL_DISTANCE` only when `draw_->GetDeviceCaps().cullDistanceSupported`, and the D3D11 backend infers that capability purely from feature level (`thin3d_d3d11.cpp:326`, `featureLevel_ >= D3D_FEATURE_LEVEL_10_0`), which is true for real D3D11 but wrong for a translation layer that accepts the feature level and then cannot compile the shader. An earlier attempt to prove the capability by compiling a probe `SV_CullDistance0` shader at init was written and then DELETED rather than shipped, because inspection of DXVK's `D3D11Device::CreateVertexShader` shows it only builds a shader module; MoltenVK does not translate to MSL until pipeline creation, so the probe would always succeed and prove nothing. The shipped change instead uses PPSSPP's own documented mechanism for exactly this situation: a new `Bugs::CULL_DISTANCE_BROKEN` flag (with its name string), infested when the adapter description contains "Venus" (live adapter is `Virtio-GPU Venus (Apple M4 Max) (venus 26.1.0)`), which clears `caps_.cullDistanceSupported`. It is applied after adapter detection, since the capability is assigned at line 326 before the adapter is queried at line 373 — verified by line ordering rather than assumed. Correctness of the fallback was checked in source rather than hoped for: cull distance is only an optimisation that discards triangles fully outside the depth range, clip distance still provides correctness (`VertexShaderGenerator.cpp:1264`), and `SoftwareTransformCommon.cpp:471` contains an explicit CPU path guarded by `!gstate_c.Use(GPU_USE_CULL_DISTANCE)` for precisely this case. Scope is minimal and safe for ordinary installs: only adapters named Venus are affected. Fork commit `51b3ace1984b6e2d760cfb87621d26bbc102701b`, hosted ARM64 build `31625434528` in progress. No artifact will be sealed or run unless that build is green with recorded exact hashes, and A3 still requires the unchanged 3/3 live gate.
- A3 cull-distance fix sealed and submitted (2026-08-12 19:20 UTC) → hosted fork run `31625434528` on exact source SHA `51b3ace1984b6e2d760cfb87621d26bbc102701b` completed success; artifact `9153363379` yields executable SHA-256 `1341d6f773dc87e3c07e16676b94ad3e676983ec3f4313232408a13cf0d6d09f`. The change is proven present in the shipped bytes two independent ways rather than assumed: the new bug-name string `CULL_DISTANCE_BROKEN` is found in the executable image, and the CRLF SHA-256 of the patched `Common/GPU/D3D11/thin3d_d3d11.cpp` appears in the PDB source-hash table. The build also retains the proven loader corrections and the bounded present/readback trace, so the run remains diagnosable. Strict-policy payload `~/BridgeVM/apps/ppsspp-a3-cull-distance-fix-20260812.zip`, mode 0600, archive SHA-256 `5db0f49e3c00a5f8a7d6a29438d036544de942abea1bd1bf415a8cce69bdc8f1`, PDB kept host-side; the repository validator accepted it and independently reported the same embedded executable hash. Private manifest `a3-title-44fac487-cull-distance-fix.tsv`, mode 0600, SHA-256 `35fbdba309616ff97cd4b6c4e22a84219d2d20ad59595495f6c34ee02ee07909`, changes only the `ppsspp` row and keeps the BVPIPE-instrumented renderer `8b3673ca…` so a recurrence would still be attributable; all nine hashable rows re-verified locally. Exact-input T6 `20260812-142101-37275-19759` submitted on the T0-passed head `44fac487…` with the queue idle. Prediction under test: with no `SV_CullDistance0` in the generated vertex shader, MoltenVK should compile the pipeline, `vkCreateGraphicsPipelines` should succeed, no `BVPIPE graphics create failed` or `vkCmdBindPipeline` CS error should appear, the readback `Map` should return, and the title should produce many `setframebuf-intervals` samples. It is A3 evidence only if samples>0, exact identity and p50>=30 all hold, and only if that then repeats independently 3/3.
- A3 BREAKTHROUGH: cull-distance fix restores real frame output; identity now the only unmet criterion, run `20260812-142418-40711-15470` (2026-08-12 19:35 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `caa31c24f52cda36bf862647dce0a74acb9f9993e1f64fb653d370d99140654d`), A3 still OPEN at 1/3, but the blocking defect is fixed and the campaign has moved to a different, final obstacle. With `Bugs::CULL_DISTANCE_BROKEN` set for Venus adapters the title no longer emits `SV_CullDistance0`, and every downstream symptom disappears: no `BVPIPE graphics create failed`, no `vkr: failed to look up object ... of type 19`, no `vkCmdBindPipeline` CS error, and no readback stall. Instead the sampler recorded `guest_fps samples=2161 p50=250.00 p05=13.70 mean=282.40 min=1.29 max=1000.00 source=setframebuf-intervals` — 2,161 real per-frame samples against exactly one flip in every previous run, from the exact fixed executable (`process_sha256=1341D6F773DC87E3C07E16676B94AD3E676983EC3F4313232408A13CF0D6D09F`) on the exact sealed content (`content_sha256=24D3AEAA…`). That decisively confirms the whole proven causal chain and the fix at its root. The gate nonetheless fails, correctly and without any threshold being touched: `module_identity_verified=false` because `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll sha256=3D1DF347…` is the system DXGI rather than the sealed title-local copy, and the verifier requires both DXVK modules to be the sealed pair. `d3d11.dll` is correct (`C:\BridgeVM\a2-title\ppsspp\d3d11.dll`, `DAAC69E6…`) and the Venus ICD is correct. So the remaining work is the previously known KnownDLL/private-assembly DXGI pairing, now isolated as the single unmet criterion rather than one of several. Also note p50 250.00 with max 1000.00 indicates the title is running far above real-time on a 480x272 homebrew scene, so the p50>=30 threshold is met with large margin but the distribution must be sanity-checked before it is ever cited as a pass. A3 remains OPEN with all criteria unchanged.
- A3 identity probe made non-collapsing, exact `fe03c4871b9fbeff878b6fce923acdecc65cbffe` (`graphics: report every module sharing a base name`, 2026-08-12 19:50 UTC). The last run left one criterion unmet, `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll`, but that evidence was ambiguous: DXVK's own DXGI demonstrably initialised inside the same process (it wrote `PPSSPPWindowsARM64_dxgi.log` with `DXVK: 3.0.2`, `Build: aarch64 clang 22.1.8`), while the probe used `Select-Object -First 1` and could therefore report only one of two modules sharing the base name. That distinction matters: it separates "the sealed DXGI is not loaded at all" from "both are loaded and the probe printed the wrong one", and those demand different fixes. The probe now enumerates every module with each base name and prints one `identity module=... path=... sha256=...` line per instance, still failing closed with `exit 3` if none is found. The verifier was updated in step so the added lines cannot be gamed: it selects the title-local instance explicitly (`grep '^identity module=dxgi.dll .*a2-title'`) instead of `tail -1` over a mixed set, and every existing assertion is unchanged — exact path containment, exact expected SHA-256 for `d3d11.dll`/`dxgi.dll`/`vulkan_virtio.dll`, exact process and content hashes, and `identity status=PASS`. This deliberately cannot loosen the gate, and that was proven rather than asserted: feeding a log containing only the system DXGI leaves `MODULE2_LINE` empty, the path assertion false and the extracted SHA empty, so the run still fails. The probe stayed within its fixed 24-line ceiling by folding the guard onto one line rather than raising the ceiling. Evidence: structural budgets PASS, CRLF consistent, `bash -n` OK, viogpu3d asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. A3 remains OPEN at 1/3 with all criteria unchanged.
- A3 module-enumeration run submitted (2026-08-12 20:01 UTC) → hosted CI `31629341229` and Security `31629341120` completed success on exact `fe03c4871b9fbeff878b6fce923acdecc65cbffe`; exact-SHA T0 `20260812-144955-16254-22992` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe sealed at SHA-256 `751c1493799be0872ad2d1f5b5734e4cda1c159569af1bdee42c16c37e07ad7b` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, linkage to the BVPIPE-instrumented renderer confirmed). Private manifest `a3-title-fe03c487-module-enum.tsv`, mode 0600, SHA-256 `5cb449d2633d3f97b79e5ed2bcf678b921d40ce603bb7c96d6821ac6cb006ab5`, changes only the `binary` row and keeps the cull-distance-fixed payload `5db0f49e…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-150115-36606-9707` submitted with the queue idle and 100 GiB free. This run settles the one remaining ambiguity: if the sealed DXVK `dxgi.dll` is in fact mapped alongside the system copy, the enumerating probe will now print both and the verifier will match the title-local one, and with frames already proven (2,161 samples last run) all A3 criteria could be met for run 1 of 3; if only the system DXGI is mapped, the run fails honestly and the private-assembly pairing is confirmed as the real remaining defect.
- A3 DXGI pairing PROVEN correct; last mismatch isolated to a stale Venus ICD expectation, run `20260812-150115-36606-9707` (2026-08-12 20:10 UTC) → honest FAIL (`result=fail`, `exit_code=1`, receipt SHA-256 `ae8e53707beeca93b2ec2382d88c1b7351ae889095e197ab823c4bbf235e8456`, `module_identity_verified=false`), A3 still OPEN at 1/3, but two things are now settled. First, the enumerating probe shows the process maps BOTH DXGI modules: `identity module=dxgi.dll path=C:\Windows\system32\dxgi.dll sha256=3D1DF347…` AND `identity module=dxgi.dll path=C:\BridgeVM\a2-title\ppsspp\dxgi.dll sha256=100F22B8CCA2C81B08FE49898F6B753744F1556114637D277B61B7432950BBA3`, which exactly matches `expected_dxgi_sha256`. So the private-assembly pairing has been working; the previous `first match` probe simply printed the system copy and hid it. The earlier conclusion that title-local DXGI was not loaded is therefore retracted. With the cull-distance fix also holding (`guest_fps samples=2160 p50=333.33 source=setframebuf-intervals`), four of the five identity assertions now match sealed values exactly: `d3d11.dll` `DAAC69E6…`, `dxgi.dll` `100F22B8…`, process `1341D6F7…`, content `24D3AEAA…`. Second, the single remaining mismatch is the Venus ICD: the guest loads `vulkan_virtio.dll` `A9356F9F837F82F288B3A8DB6F3BA4784C241316CA77AA0EA261D0B53CCA3429` from `C:\Windows\System32\DriverStore\FileRepository\viogpu3d.inf_arm64_6435ce2e01767d8f\`, while the verifier derives `expected_venus_sha256` from the host staging package at `~/BridgeVM/work/download-120.45-backing-only/vulkan_virtio.dll` = `bb201685e040dd7f05b0322dc5738d9cea62a718dd4d23ea697c3c90a0ed586a`. Evidence says this is a stale expectation rather than a wrong guest: the guest has loaded `A9356F9F…` consistently across every run in this campaign (checked 2026-08-12 runs and the 2026-08-11 private-assembly run), and the staging package file dates from 25 July whereas the canonical A2 image was sealed 8 August, so the image's installed DriverStore driver is simply newer than the loose package the verifier hashes. This is NOT resolved by editing the expectation to match observation — that would defeat the check — so the correct next step is to establish which artifact is authoritative for the sealed image and make the expectation derive from it, without weakening exact-hash verification. A3 remains OPEN with all criteria unchanged.
- A3 Venus ICD expectation corrected to the installed driver, exact `b4a58d79dbed4a7cb899e8a3e6a35912cf0e734a` (`graphics: pin the icd identity to the installed driver`, 2026-08-12 20:25 UTC). The final identity mismatch was traced to its source rather than patched over. The verifier derived `expected_venus_sha256` from the loose host build directory `~/BridgeVM/work/download-120.45-backing-only/vulkan_virtio.dll` (`bb201685…`), but the guest loads `A9356F9F…`. Byte comparison shows these are genuinely different builds, not a signing difference: same 3,995,048-byte length but 903 differing bytes spread from offset 129 to 3,995,045. The run log then identified where the guest copy comes from: the agent exports the driver files the image actually installed back to the host with `BVAGENT SHARE guest->host viogpu3d.sys bytes=151976` and `vulkan_virtio.dll bytes=3995048`, and `viogpu3d.sys` likewise differs from the host package (`4ce6fbb2…` exported versus `d95e4279…` packaged) while `virtio_icd.arm64.json` matches. So the sealed A2 image ships and installs its own driver package, and the loose host directory is a build input that this image never used — consistent with the package file dating from 25 July and the image being sealed on 8 August. The expectation is now computed from the guest-exported copy in the run share, with a fail-closed guard if the guest exports nothing. This is not circular self-certification, and that was checked rather than assumed: the exported file is the guest's install package under `C:\BridgeVM\viogpu3d`, whereas the identity probe reports the separately loaded module under `C:\Windows\System32\DriverStore\FileRepository\viogpu3d.inf_arm64_6435ce2e01767d8f\`; the two are independent locations and were verified to hash identically (`a9356f9f…` both), so the assertion still proves the loaded ICD is the one the image installed. Every other identity assertion is untouched. Evidence: `bash -n` OK, structural budgets PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. A3 remains OPEN at 1/3.
- A3 first full-criteria attempt submitted (2026-08-12 20:41 UTC) → hosted CI `31632658306` and Security `31632658320` completed success on exact `b4a58d79dbed4a7cb899e8a3e6a35912cf0e734a`; exact-SHA T0 `20260812-152940-87528-14232` completed PASS (`result=pass`, `exit_code=0`). Fresh exact-SHA release+Venus probe sealed at SHA-256 `07e4377d0c2f3d6abf77778952444c25aa081a6e10f116476080a07bb815fa60` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, linkage to the BVPIPE-instrumented renderer confirmed). Private manifest `a3-title-b4a58d79-icd-identity.tsv`, mode 0600, SHA-256 `e18e525a2b92272ce35bfb993b9369ab92f471ccf337ea9db401fc754d6153aa`, changes only the `binary` row and keeps the cull-distance-fixed payload `5db0f49e…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-154114-8586-18948` submitted with the queue idle. This is the first run in the campaign where every known obstacle has a proven fix in place simultaneously: the MoltenVK cull-distance trigger removed at the PPSSPP layer (2,161 and 2,160 samples in the last two runs), the DXGI private-assembly pairing shown to be working and now visible to the probe, and the ICD expectation pinned to the driver the image actually installed. It counts as A3 evidence only if this run reports samples>0, exact identity for all five artifacts and p50>=30, and only if two further independent runs then do the same for 3/3.
- A3 run `20260812-154114-8586-18948` → honest FAIL with a NEW, different failure mode (2026-08-12 20:55 UTC): `result=fail`, `exit_code=1`, receipt SHA-256 `7ea92cf85defa404fd488a6c82cecf65f1286fcdc09a128838d36b87e29c7dcb`, `sample_count=0`, A3 still OPEN at 1/3. The ICD correction worked as intended — `identity status=PASS` was reached, meaning all five exact-hash assertions including the Venus ICD now match — but the title produced no frames at all this time: `guest_fps samples=0 reason=no-fps-lines-in-frame-log`, `guest_fps_absent frame_log_bytes=76657 flip_lines=0 fps_lines=0`, i.e. ZERO flips rather than the 2,161 and 2,160 samples of the two preceding runs on the same exact executable `1341D6F7…`. This is not the old pipeline defect: there is no `BVPIPE graphics create failed`, no `vkr: failed to look up object`, no `vkCmdBindPipeline` CS error, `SUBMIT_3D failure groups: none`, and `P3 Windows 3D trace gate: PASS`. The bounded frame tail ends on `Display disabled, displaying only black` (`FramebufferManagerCommon.cpp`), which appears twice in the run, and the title still survived the full window (`status=PASS pid=3436 elapsed_ms=48814`) with `required_module_loaded module=d3d11.dll` and clean launch state. A further distinguishing fact: DXVK wrote no `PPSSPPWindowsARM64_d3d11.log` content for this run, unlike the previous runs where the log was exported and readable. So the emulator reached its display path but never latched a framebuffer, meaning the title did not get as far as running the content, and the receipt correctly reports `module_identity_verified=false` because the gate requires samples as well. Since the two immediately preceding runs with the identical payload and executable produced thousands of samples, this looks like run-to-run nondeterminism in reaching content execution rather than a regression introduced by the ICD expectation change, which only affects a host-side comparison and cannot influence guest behaviour. That reasoning is recorded as a hypothesis, not a conclusion: the next run must establish whether frame production is reliable, because the 3/3 policy requires three independent passes and an intermittent no-flip state would block it regardless of the fixes already proven. A3 remains OPEN with all criteria unchanged.
- A3 zero-flip run diagnosed as late-boot truncation, not a new defect (2026-08-12 21:13 UTC, analysis of `20260812-154114-8586-18948`). Reading the bounded frame tail in order shows the title did NOT fail to run the content: it called `sceDisplaySetMode(0, 480, 272)`, logged `Display disabled, displaying only black` while no framebuffer was latched yet, then `N[BOOT]: UI\EmuScreen.cpp:391 Booted C:/BridgeVMShare/cube.iso...`, then began GE work with `sceGeListUpdateStallAddr` and `Starting DL execution at 0880d7a0`. So content boot succeeded and the emulator started submitting display lists; the last tail line is stamped `55:25:438`, roughly seven seconds after `status=START utc=2026-08-12T19:55:18`, while the process itself survived the whole window (`elapsed_ms=48814`, `responding=True`, `cpu_s=1.25`). The earlier reading that the title "never got as far as running the content" is therefore retracted — what the evidence supports is that the title booted late within the 45-second window and had not yet reached steady-state `sceDisplaySetFrameBuf` flips when the measurement ended, which is consistent with `flip_lines=0` and with DXVK never writing its log for this run. The two immediately preceding runs on the identical payload produced 2,161 and 2,160 samples, so this is variance in how quickly the guest reaches title steady state, not a regression from the ICD expectation change, which is host-side only and cannot affect guest behaviour. No threshold, sampler or timeout was altered in response. An unchanged rerun on the same exact head `b4a58d79…`, probe `07e4377d…` and manifest `e18e525a…` was submitted as T6 `20260812-161348-48399-4906` with the queue idle and 100 GiB free, to establish whether frame production is reliable enough for the independent 3/3 requirement. A3 remains OPEN at 1/3.
- A3 FIRST PASSING RUN, `20260812-161348-48399-4906` run-1 (2026-08-12 21:30 UTC): `a3_d3d11_fps=pass`, `samples=2158`, `p50=250.00`, with every exact-identity assertion satisfied — `identity module=d3d11.dll path=C:\BridgeVM\a2-title\ppsspp\d3d11.dll sha256=DAAC69E6…` (sealed DXVK D3D11), `identity module=dxgi.dll path=C:\BridgeVM\a2-title\ppsspp\dxgi.dll sha256=100F22B8…` (sealed DXVK DXGI, title-local), and `identity module=vulkan_virtio.dll` from the installed DriverStore package, plus exact process and content hashes and `identity status=PASS`. This is the first time in the entire campaign that samples, module identity and the p50>=30 threshold have all been met in one run, and it was produced by an unchanged rerun of the same head `b4a58d79…`, probe `07e4377d…` and manifest `e18e525a…` used by the preceding zero-flip run, which confirms that run was late-boot variance rather than a defect. Importantly this is still only run 1 of 3: the tier is executing the remaining independent runs, and A3 closes only if all three pass. No criterion, threshold, sampler, renderer or timeout was relaxed to reach this point; the result came from fixing the MoltenVK cull-distance trigger at the PPSSPP layer, exposing the already-working DXVK DXGI pairing to the probe, and pinning the ICD expectation to the driver the sealed image actually installs. A3 remains OPEN until the campaign reports 3/3.
- A3 campaign `20260812-161348-48399-4906` completed 2/3 — honest FAIL, but the two passes are the first real A3 evidence (2026-08-12 21:55 UTC). Receipt SHA-256 `55710434e8a22459fb1b89eee053845714b6ffddfee7c2c8445897b3d1b6dc89`, `run_count=3`, `passes=2`, `failures=1`, `fps_samples=[2158, 5122, 0]`, `fps_p50=[250.0, 62.5, 0.0]`, `pass=false`, `outcome=failed`. Runs 1 and 2 both reported `a3_d3d11_fps=pass` with full exact identity (title-local DXVK `d3d11.dll` `DAAC69E6…` and `dxgi.dll` `100F22B8…`, installed DriverStore ICD, exact process `1341D6F7…` and content `24D3AEAA…`, `identity status=PASS`) and p50 of 250.00 and 62.50 against the unchanged >=30 threshold. Run 3 failed with `samples=0`, and its cause is the same late-boot variance already seen once before rather than a new defect: `guest_fps_absent frame_log_bytes=70800 flip_lines=0`, process alive and responsive (`cpu_s=1.06`, `responding=True`), no pipeline error, and the bounded frame tail ends inside startup at `Core\System.cpp:590 Starting graphics...`, `NotifyDisplayResized`, `ScreenManager::resized(dp: 1046x593)` — the title was still initialising graphics when the 45-second window closed. Timing confirms the pattern: the tail stamps show run 3 had only reached `57:53:236` from a `20:57:46` start, i.e. roughly seven seconds in, whereas the passing runs were already producing flips. So the remaining obstacle is not correctness but startup latency inside the measurement window: three of the last five runs reached steady state and produced thousands of samples, two did not. The gate correctly refuses to call this a pass, and no threshold, timeout, sampler or the 3/3 policy will be changed to accommodate it; per the standing rule the campaign stopped at the first failure and this receipt is preserved as an honest partial. The legitimate next step is to remove the startup race itself — for example by requiring the title to have reached steady state before the measurement window begins — rather than by extending or weakening the window. A3 remains OPEN.
- A3 startup race removed at its source, exact `31368a1fc55abc8f3421d5b11b2e537c4f96a6f2` (`graphics: measure from the first presented frame`, 2026-08-12 22:10 UTC). Two of the last five runs produced zero samples purely because the 45-second window began at process launch and was consumed by title startup — evidence showed both stopping inside initialisation (`Starting graphics...`, `ScreenManager::resized`) with the process alive, responsive and error-free. The window now starts at the first presented frame instead: the gate polls a bounded 64-KiB tail of the title's own log for `=sceDisplaySetFrameBuf(` via a new `Test-FrameLogHasFlip`, holds the deadline one second ahead while no flip has been seen, and on the first flip logs `first_flip_observed after_ms=<n> window_restarted=true` and restarts the full `MinimumSeconds` measurement from that instant. Crucially this does not weaken the gate, and that was verified rather than asserted: the extension is capped by a new `StartupGraceSeconds` (default 60, `ValidateRange(0,300)`), so a title that never renders still fails. The timing was proven by simulating the exact loop before any live run — first flip at 2 s, 7 s, 40 s and 44.9 s each yield a full 45.0 s measured window after that flip, while a title that never flips terminates the loop at ~105.75 s and then fails through the unchanged zero-sample path. The measured duration, the p50>=30 threshold, the sampler source, exact identity, the renderer and the independent 3/3 policy are all untouched; the only change is that the 45 seconds are now spent on a running title rather than partly on its boot. The guest command timeout already accommodates this (`TITLE_SECONDS + STEP_TIMEOUT` with `STEP_TIMEOUT=300`), so no timeout was altered either. Evidence: structural budgets PASS, CRLF consistent, viogpu3d firstboot asset smoke PASS, live gate policy smoke 83 checks PASS, `git diff --check` clean, `scripts/check-project.sh --fast` = `project check: PASS`. A3 remains OPEN; the next campaign must still pass 3/3 on its own merits.
- A3 3/3 attempt with flip-anchored window submitted (2026-08-12 22:32 UTC) → hosted CI `31641755825` and Security `31641755687` completed success on exact `31368a1fc55abc8f3421d5b11b2e537c4f96a6f2`; exact-SHA T0 `20260812-172042-71030-2123` completed PASS (`result=pass`, `exit_code=0`). The first T0 attempt `20260812-171944-69976-21222` was correctly refused by the immutable worker guard (`refused-free-space`, `available_gib=99`); the guard was not relaxed. Space was reclaimed only from regenerable scratch: the single content-addressed `prepared-*` directory under `~/BridgeVM/work/p1gate-cache` (49 GB), which `scripts/p1-boot-gate.sh` rebuilds automatically from the retained `base-image.raw` + `injector.raw` and which belongs to the already-closed A1 gate. No canonical image, vars, receipt, payload, manifest or live-queue evidence was touched; free space went 99 -> 100 GiB. Fresh exact-SHA release+Venus probe sealed at SHA-256 `94114e21d9a2fa3c270896d73a0fa960da116f98a163463d50c221f651bc2d8a` (strict codesign PASS, `virtio_gpu_3d_compiled=true`, linkage to the BVPIPE-instrumented renderer confirmed). Private manifest `a3-title-31368a1f-flip-window.tsv`, mode 0600, SHA-256 `4f9787db6063f09f6ec2c9f415f27a4047207ffb737372f9b650f1004fa19708`, changes only the `binary` row and keeps the cull-distance-fixed payload `5db0f49e…`; all nine hashable rows re-verified locally. Exact-input T6 `20260812-173200-91503-26516` submitted with the queue idle and 100 GiB free. This campaign tests whether the two known-good runs now reproduce three times independently, with the measured 45 seconds spent on a running title rather than partly on its startup.
- **A3 GATE PASSED 3/3** — campaign `20260812-173200-91503-26516` completed (2026-08-12 23:05 UTC): `result=pass`, `exit_code=0`, receipt SHA-256 `8430c7d682c81330da3c2d969eb57ddd1c381c2dc59edc2fe094657a9f93a21e` (private and public identical), `run_count=3`, `passes=3`, `failures=0`, `pass=true`, `outcome=completed`, `required_run_count=3`, `threshold_fps=30.0`. The three independent runs measured `fps_samples=[2633, 5275, 5369]` with `fps_p50=[250.0, 62.5, 62.5]`, every one above the unchanged 30 fps threshold, and each reported `identity status=PASS` with exact sealed hashes: PPSSPP executable `1341d6f7…`, title-local DXVK `d3d11.dll` `daac69e6…` and `dxgi.dll` `100f22b8…`, canonical image `eed9aa9b…`, content `24d3aeaa…`, plus the installed DriverStore Venus ICD. The flip-anchored window behaved exactly as simulated, logging `first_flip_observed after_ms=7664`, `7100` and `6731` with `window_restarted=true`, so each measurement covered a running title rather than its startup. Nothing was relaxed to achieve this: the p50>=30 threshold, the `setframebuf-intervals` sampler, exact-hash identity for all five artifacts, the shipping `aggressive` renderer, the timeouts and the independent 3/3 policy are all unchanged from the start of the campaign, and the same gate produced honest failures for dozens of runs before the underlying defects were fixed. The chain of real fixes that made this possible: the MoltenVK `gl_CullDistance` MSL emission defect worked around at the PPSSPP layer via `Bugs::CULL_DISTANCE_BROKEN` for Venus adapters, the DXVK private-assembly DXGI pairing shown to have been working and exposed by the non-collapsing module probe, the Venus ICD expectation pinned to the driver the sealed image actually installs, and the measurement window anchored to the first presented frame. A3 is now PROVEN by live evidence on exact head `31368a1fc55abc8f3421d5b11b2e537c4f96a6f2`.
- A3 recorded as PROVEN in tracked evidence and the registry (2026-08-12 23:20 UTC). New evidence document `docs/windows-arm/evidence/a3-d3d11-title-fps-3of3-20260812.md` records the campaign, receipt hash, per-run samples and p50 values, the flip-anchored window timings, the exact sealed identities and the four defects fixed to get there. The verify-loop caught that the document was unclassified and it was registered in `docs/document-manifest.tsv` as `current / windows-graphics` rather than left dangling. `capabilities/windows-hvf.json` now carries `A3 state=PROVEN` with the live measurement summary and three evidence paths, and `scripts/render-capability-status.py` regenerated `README.md`, `STATUS.md` and `docs/windows-arm/capability-matrix.md`, which now show A3 as `proven`. Registry counts moved from 22 PROVEN / 2 OPEN to **23 PROVEN / 1 OPEN**, the sole remaining item being A11, which by design must be verified at the final evidence head. Committed as `8649b9981d74874617873d35a65737f4cad3d477` (`graphics: record the a3 title frame rate gate as proven`) and `0683663d8c0efd8843146ca22706e47f797469b0` (`docs: regenerate capability status for a3`), with `scripts/check-project.sh --fast` = `project check: PASS` before each push.
- **A11 PROVEN and registry 24/24 at final head `3f6631ea3461d98875aab513dccb099be522ac19`** (2026-08-12 23:55 UTC). A11 was first verified at `0683663d…`, but recording A11 itself moved the head, so the entire criterion was re-verified at the true final head rather than relying on the earlier run: `scripts/run-xctest-shim-suites.sh` = 403 + 188 (1 skip) + 62 = **653 passed, 0 failed**, `PASS: all three shim suites`; full non-fast `scripts/check-project.sh` = `project check: PASS` (log SHA-256 `e5bbb65a45090862adadd5987f48be436e03cbd27899a823298987baa46b1596`); hosted CI `31647926528` and Security and quality `31647926522` both completed success on that exact SHA; the shipped app was rebuilt from that head and passed `codesign --verify --deep --strict`, with `third_party_notice_gate=pass` across 12 bundled frameworks and notice SHA-256 `354bb9a5…`. The registry now reports **24 PROVEN / 0 OPEN** and `render-capability-status.py` = `capability registry: PASS (rendered 3 blocks, 24 criteria)`. The suites remain honestly labelled as measured under the shim rather than Apple XCTest, exactly as the suite output states. Final done-criteria status: A2 Vulkan gate PASS, A3 D3D11 gate PASS 3/3 (receipt `8430c7d6…`), A1 boot reliability 10/10, A11 final-head verification PASS, registry 24/24, app packaged and codesigned, third-party notices closed, and hosted CI green on the final head.
