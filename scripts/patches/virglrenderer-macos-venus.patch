diff --git a/src/meson.build b/src/meson.build
index 6207a74..dd535d1 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -180,6 +180,10 @@ virgl_depends = [
    dl_dep,
 ]
 
+if with_host_darwin
+   virgl_depends += [dependency('appleframeworks', modules: ['OpenGL'], required: true)]
+endif
+
 if with_tracing == 'perfetto'
    virgl_depends += [vperfetto_min_dep]
 endif
diff --git a/src/venus/venus-protocol/vn_protocol_renderer_dispatches.h b/src/venus/venus-protocol/vn_protocol_renderer_dispatches.h
index a29a4fc..d1a1200 100644
--- a/src/venus/venus-protocol/vn_protocol_renderer_dispatches.h
+++ b/src/venus/venus-protocol/vn_protocol_renderer_dispatches.h
@@ -7,6 +7,7 @@
 
 #ifndef VN_PROTOCOL_RENDERER_DISPATCHES_H
 #define VN_PROTOCOL_RENDERER_DISPATCHES_H
+#include <stdio.h>
 
 #include "vn_protocol_renderer_structs.h"
 #include "vn_protocol_renderer_transport.h"
@@ -736,8 +737,12 @@ static inline void vn_dispatch_command(struct vn_dispatch_context *ctx)
             vn_cs_decoder_set_fatal(ctx->decoder);
     }
 
-    if (vn_cs_decoder_get_fatal(ctx->decoder))
+    if (vn_cs_decoder_get_fatal(ctx->decoder)) {
+        /* BridgeVM: always surface CS errors — silent ring death is undebuggable. */
+        fprintf(stderr, "venus: dispatch CS error cmd=%d %s\n", (int)cmd_type,
+                vn_dispatch_command_name(cmd_type));
         vn_dispatch_debug_log(ctx, "%s resulted in CS error", vn_dispatch_command_name(cmd_type));
+    }
 }
 
 #endif /* VN_PROTOCOL_RENDERER_DISPATCHES_H */
diff --git a/src/venus/vkr_context.c b/src/venus/vkr_context.c
index 2ea5898..bf56607 100644
--- a/src/venus/vkr_context.c
+++ b/src/venus/vkr_context.c
@@ -372,6 +372,17 @@ vkr_context_create_resource(struct vkr_context *ctx,
    if (!blob_id && blob_flags == VIRGL_RENDERER_BLOB_FLAG_USE_MAPPABLE)
       return vkr_context_create_resource_from_shm(ctx, res_id, blob_size, out_blob);
 
+   /* BridgeVM: the Windows Venus winsys uses the guest-vram-style ordering
+    * where the mappable blob is created BEFORE vkAllocateMemory and the
+    * allocation then imports the resource via VkImportMemoryResourceInfoMESA.
+    * When the referenced memory object does not exist yet, service the create
+    * as a shm allocation exactly like blob_id == 0; the later import binds
+    * these pages to the VkDeviceMemory via VK_EXT_external_memory_host.
+    */
+   if ((blob_flags & VIRGL_RENDERER_BLOB_FLAG_USE_MAPPABLE) &&
+       !vkr_context_get_object(ctx, blob_id))
+      return vkr_context_create_resource_from_shm(ctx, res_id, blob_size, out_blob);
+
    return vkr_context_create_resource_from_device_memory(ctx, res_id, blob_id, blob_size,
                                                          blob_flags, out_blob);
 }
diff --git a/src/venus/vkr_device.c b/src/venus/vkr_device.c
index 3e943bf..22439b2 100644
--- a/src/venus/vkr_device.c
+++ b/src/venus/vkr_device.c
@@ -132,6 +132,7 @@ vkr_dispatch_vkCreateDevice(struct vn_dispatch_context *dispatch,
    const char **exts = NULL;
    uint32_t ext_count = args->pCreateInfo->enabledExtensionCount;
    ext_count += physical_dev->EXT_external_memory_metal;
+   ext_count += physical_dev->EXT_external_memory_host;
    ext_count += physical_dev->EXT_metal_objects;
    ext_count += physical_dev->KHR_portability_subset;
    ext_count += physical_dev->KHR_external_memory_fd;
@@ -150,6 +151,8 @@ vkr_dispatch_vkCreateDevice(struct vn_dispatch_context *dispatch,
 
       if (physical_dev->EXT_external_memory_metal)
          exts[ext_count++] = "VK_EXT_external_memory_metal";
+      if (physical_dev->EXT_external_memory_host)
+         exts[ext_count++] = "VK_EXT_external_memory_host";
       if (physical_dev->EXT_metal_objects)
          exts[ext_count++] = "VK_EXT_metal_objects";
       if (physical_dev->KHR_portability_subset)
diff --git a/src/venus/vkr_device_memory.c b/src/venus/vkr_device_memory.c
index 57a108d..206602e 100644
--- a/src/venus/vkr_device_memory.c
+++ b/src/venus/vkr_device_memory.c
@@ -14,9 +14,12 @@
 #include "vkr_physical_device.h"
 
 static bool
-vkr_get_fd_info_from_resource_info(struct vkr_context *ctx,
-                                   const VkImportMemoryResourceInfoMESA *res_info,
-                                   VkImportMemoryFdInfoKHR *out)
+vkr_get_import_info_from_resource_info(struct vkr_context *ctx,
+                                       const VkImportMemoryResourceInfoMESA *res_info,
+                                       VkImportMemoryFdInfoKHR *out_fd,
+                                       VkImportMemoryHostPointerInfoEXT *out_host_ptr,
+                                       const VkBaseInStructure **out_import,
+                                       VkDeviceSize *inout_alloc_size)
 {
    struct vkr_resource *res = vkr_context_get_resource(ctx, res_info->resourceId);
    if (!res) {
@@ -33,6 +36,24 @@ vkr_get_fd_info_from_resource_info(struct vkr_context *ctx,
    case VIRGL_RESOURCE_FD_OPAQUE:
       handle_type = VK_EXTERNAL_MEMORY_HANDLE_TYPE_OPAQUE_FD_BIT;
       break;
+   case VIRGL_RESOURCE_FD_SHM:
+      /* BridgeVM: shm-backed mappable blob created ahead of the allocation
+       * (guest-vram-style ordering).  Import its pages as a host pointer;
+       * MoltenVK wraps them NoCopy so GPU writes land in the same shm the
+       * guest maps through the shared-memory BAR. */
+      if (!res->u.data)
+         return false;
+      *out_host_ptr = (VkImportMemoryHostPointerInfoEXT){
+         .sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_HOST_POINTER_INFO_EXT,
+         .pNext = res_info->pNext,
+         .handleType = VK_EXTERNAL_MEMORY_HANDLE_TYPE_HOST_ALLOCATION_BIT_EXT,
+         .pHostPointer = res->u.data,
+      };
+      /* Host-pointer imports must cover whole imported pages. */
+      if (*inout_alloc_size < res->size)
+         *inout_alloc_size = res->size;
+      *out_import = (const VkBaseInStructure *)out_host_ptr;
+      return true;
    default:
       return false;
    }
@@ -41,12 +62,13 @@ vkr_get_fd_info_from_resource_info(struct vkr_context *ctx,
    if (fd < 0)
       return false;
 
-   *out = (VkImportMemoryFdInfoKHR){
+   *out_fd = (VkImportMemoryFdInfoKHR){
       .sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_FD_INFO_KHR,
       .pNext = res_info->pNext,
       .fd = fd,
       .handleType = handle_type,
    };
+   *out_import = (const VkBaseInStructure *)out_fd;
    return true;
 }
 
@@ -260,19 +282,23 @@ vkr_dispatch_vkAllocateMemory(struct vn_dispatch_context *dispatch,
       return;
    }
 
-   /* translate VkImportMemoryResourceInfoMESA into VkImportMemoryFdInfoKHR in place */
+   /* translate VkImportMemoryResourceInfoMESA into an import chain in place */
    VkImportMemoryFdInfoKHR local_import_info = { .fd = -1 };
+   VkImportMemoryHostPointerInfoEXT local_host_ptr_info;
    VkImportMemoryResourceInfoMESA *res_info = NULL;
    VkBaseInStructure *prev_of_res_info = vkr_find_prev_struct(
       alloc_info, VK_STRUCTURE_TYPE_IMPORT_MEMORY_RESOURCE_INFO_MESA);
    if (prev_of_res_info) {
+      const VkBaseInStructure *import_struct = NULL;
       res_info = (VkImportMemoryResourceInfoMESA *)prev_of_res_info->pNext;
-      if (!vkr_get_fd_info_from_resource_info(ctx, res_info, &local_import_info)) {
+      if (!vkr_get_import_info_from_resource_info(ctx, res_info, &local_import_info,
+                                                  &local_host_ptr_info, &import_struct,
+                                                  &alloc_info->allocationSize)) {
          args->ret = VK_ERROR_INVALID_EXTERNAL_HANDLE;
          return;
       }
 
-      prev_of_res_info->pNext = (const struct VkBaseInStructure *)&local_import_info;
+      prev_of_res_info->pNext = import_struct;
    }
 
    VkExportMemoryAllocateInfo *export_info =
@@ -281,6 +307,7 @@ vkr_dispatch_vkAllocateMemory(struct vn_dispatch_context *dispatch,
    /* track if driver has requested export allocation */
    const bool might_export = export_info && export_info->handleTypes;
 
+
    /* XXX Force dma_buf/opaque fd export or gbm bo import until a new extension that
     * supports direct export from host visible memory
     *
@@ -299,7 +326,7 @@ vkr_dispatch_vkAllocateMemory(struct vn_dispatch_context *dispatch,
    void *gbm_bo = NULL;
    struct vkr_mtl_shm *mtl_shm = NULL;
    VkExportMemoryAllocateInfo local_export_info;
-   VkImportMemoryMetalHandleInfoEXT local_metal_import;
+   VkImportMemoryHostPointerInfoEXT local_host_import;
 
    if ((property_flags & VK_MEMORY_PROPERTY_HOST_VISIBLE_BIT) && !res_info) {
       /* An implementation can support dma_buf import along with opaque fd export/import.
@@ -357,13 +384,18 @@ vkr_dispatch_vkAllocateMemory(struct vn_dispatch_context *dispatch,
             return;
          }
 
-         local_metal_import = (VkImportMemoryMetalHandleInfoEXT){
-            .sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_METAL_HANDLE_INFO_EXT,
+         /* BridgeVM: MoltenVK 1.4.1's MTLBuffer import does not alias the
+          * imported buffer (GPU writes land elsewhere; proven by a minimal
+          * repro). Import the shm HOST POINTER via
+          * VK_EXT_external_memory_host instead; MoltenVK wraps it NoCopy
+          * internally and GPU writes then land in the shm pages. */
+         local_host_import = (VkImportMemoryHostPointerInfoEXT){
+            .sType = VK_STRUCTURE_TYPE_IMPORT_MEMORY_HOST_POINTER_INFO_EXT,
             .pNext = alloc_info->pNext,
-            .handleType = VK_EXTERNAL_MEMORY_HANDLE_TYPE_MTLBUFFER_BIT_EXT,
-            .handle = mtl_shm->mtl_buffer,
+            .handleType = VK_EXTERNAL_MEMORY_HANDLE_TYPE_HOST_ALLOCATION_BIT_EXT,
+            .pHostPointer = mtl_shm->shm_ptr,
          };
-         alloc_info->pNext = &local_metal_import;
+         alloc_info->pNext = &local_host_import;
          alloc_info->allocationSize = mtl_shm->shm_size;
          valid_fd_types = 1 << VIRGL_RESOURCE_FD_SHM;
       } else if (physical_dev->EXT_external_memory_dma_buf) {
diff --git a/src/venus/vkr_library.c b/src/venus/vkr_library.c
index dc1f42c..41add3e 100644
--- a/src/venus/vkr_library.c
+++ b/src/venus/vkr_library.c
@@ -6,6 +6,7 @@
 #include "vkr_common.h"
 
 #include <dlfcn.h>
+#include <stdlib.h>
 
 #include "vkr_library.h"
 
@@ -43,7 +44,14 @@ vkr_library_load(struct vulkan_library *lib)
       return true;
 
 #ifdef __APPLE__
-   lib->handle = dlopen("libvulkan.dylib", RTLD_NOW | RTLD_LOCAL);
+   /* BridgeVM: absolute-path override that survives DYLD env stripping in
+    * entitled host processes (loads MoltenVK directly, bypassing the Vulkan
+    * loader's portability filtering). */
+   const char *bv_lib = getenv("BRIDGEVM_VULKAN_LIB");
+   if (bv_lib)
+      lib->handle = dlopen(bv_lib, RTLD_NOW | RTLD_LOCAL);
+   if (lib->handle == NULL)
+      lib->handle = dlopen("libvulkan.dylib", RTLD_NOW | RTLD_LOCAL);
    if (lib->handle == NULL)
       lib->handle = dlopen("libvulkan.1.dylib", RTLD_NOW | RTLD_LOCAL);
    if (lib->handle == NULL)
diff --git a/src/venus/vkr_physical_device.c b/src/venus/vkr_physical_device.c
index 65fdf4c..38e8462 100644
--- a/src/venus/vkr_physical_device.c
+++ b/src/venus/vkr_physical_device.c
@@ -284,6 +284,8 @@ vkr_physical_device_init_extensions(struct vkr_physical_device *physical_dev)
          physical_dev->KHR_external_fence_fd = true;
       else if (!strcmp(props->extensionName, "VK_EXT_external_memory_metal"))
          physical_dev->EXT_external_memory_metal = true;
+      else if (!strcmp(props->extensionName, "VK_EXT_external_memory_host"))
+         physical_dev->EXT_external_memory_host = true;
       else if (!strcmp(props->extensionName, "VK_EXT_metal_objects"))
          physical_dev->EXT_metal_objects = true;
       else if (!strcmp(props->extensionName, "VK_KHR_portability_subset"))
diff --git a/src/venus/vkr_physical_device.h b/src/venus/vkr_physical_device.h
index 139b11a..6e0d08a 100644
--- a/src/venus/vkr_physical_device.h
+++ b/src/venus/vkr_physical_device.h
@@ -29,6 +29,7 @@ struct vkr_physical_device {
    bool KHR_external_semaphore_fd;
 
    bool EXT_external_memory_metal;
+   bool EXT_external_memory_host;
    bool EXT_metal_objects;
 
    VkPhysicalDeviceMemoryProperties memory_properties;
diff --git a/src/venus/vkr_queue.c b/src/venus/vkr_queue.c
index 61b94be..68ed8ee 100644
--- a/src/venus/vkr_queue.c
+++ b/src/venus/vkr_queue.c
@@ -586,6 +586,33 @@ vkr_dispatch_vkImportSemaphoreResourceMESA(
    /* resourceId 0 is for importing a signaled payload to sync_fd fence */
    assert(!res_info->resourceId);
 
+   /* BridgeVM: MoltenVK has no sync-fd semaphore import.  Signal the binary
+    * semaphore with an empty queue submit instead; ring execution is
+    * sequential, so the signal lands before the guest's next submission
+    * waits on it (WSI acquire-semaphore flow).
+    */
+   if (!vk->ImportSemaphoreFdKHR) {
+      if (list_is_empty(&dev->queues)) {
+         vkr_log("no queue available to signal imported semaphore");
+         vkr_context_set_fatal(ctx);
+         return;
+      }
+      struct vkr_queue *queue =
+         list_first_entry(&dev->queues, struct vkr_queue, base.track_head);
+      const VkSubmitInfo submit = {
+         .sType = VK_STRUCTURE_TYPE_SUBMIT_INFO,
+         .signalSemaphoreCount = 1,
+         .pSignalSemaphores = &res_info->semaphore,
+      };
+      mtx_lock(&queue->vk_mutex);
+      VkResult result =
+         vk->QueueSubmit(queue->base.handle.queue, 1, &submit, VK_NULL_HANDLE);
+      mtx_unlock(&queue->vk_mutex);
+      if (result != VK_SUCCESS)
+         vkr_context_set_fatal(ctx);
+      return;
+   }
+
    const VkImportSemaphoreFdInfoKHR import_info = {
       .sType = VK_STRUCTURE_TYPE_IMPORT_SEMAPHORE_FD_INFO_KHR,
       .semaphore = res_info->semaphore,
diff --git a/src/vrend/vrend_blitter.c b/src/vrend/vrend_blitter.c
index eed56e4..afbde6f 100644
--- a/src/vrend/vrend_blitter.c
+++ b/src/vrend/vrend_blitter.c
@@ -792,6 +792,7 @@ void vrend_renderer_blit_gl(ASSERTED struct vrend_context *ctx,
                             const struct vrend_blit_info *info)
 {
    struct vrend_blitter_ctx *blit_ctx = &vrend_blit_ctx;
+   GLint saved_swizzle[4];
 
    int dst_z;
    struct blit_point src0, src1;
@@ -852,6 +853,13 @@ void vrend_renderer_blit_gl(ASSERTED struct vrend_context *ctx,
    glDrawBuffers(1, &buffers);
 
    glBindTexture(src_res->target, info->src_view);
+   /* Texture parameters are shared between the blit and renderer contexts.
+    * vrend_set_tex_param() may change the source object's swizzle, while the
+    * renderer context keeps a cached copy and therefore will not necessarily
+    * program it again on the next draw.  Preserve the shared object state so
+    * a format-converting blit cannot poison later sampler alpha reads. */
+   glGetTexParameteriv(src_res->target, GL_TEXTURE_SWIZZLE_RGBA,
+                       saved_swizzle);
    vrend_set_tex_param(src_res, &info->b,
                        info->has_texture_srgb_decode &&
                        !info->needs_manual_srgb_decode);
@@ -903,6 +911,8 @@ void vrend_renderer_blit_gl(ASSERTED struct vrend_context *ctx,
                              GL_TEXTURE_2D, 0, 0);
    glFramebufferTexture2D(GL_FRAMEBUFFER, GL_COLOR_ATTACHMENT0,
                              GL_TEXTURE_2D, 0, 0);
+   glTexParameteriv(src_res->target, GL_TEXTURE_SWIZZLE_RGBA,
+                    saved_swizzle);
    glBindTexture(src_res->target, 0);
 }
 
@@ -919,4 +929,3 @@ void vrend_blitter_fini(void)
    vrend_clicbs->destroy_gl_context(vrend_blit_ctx.gl_context);
    memset(&vrend_blit_ctx, 0, sizeof(vrend_blit_ctx));
 }
-
diff --git a/src/vrend/vrend_decode.c b/src/vrend/vrend_decode.c
index 7774a25..1909b4f 100644
--- a/src/vrend/vrend_decode.c
+++ b/src/vrend/vrend_decode.c
@@ -28,6 +28,10 @@
 #include <epoxy/gl.h>
 #include <fcntl.h>
 
+#ifdef __APPLE__
+#include <OpenGL/OpenGL.h>
+#endif
+
 #include "util/u_memory.h"
 #include "pipe/p_defines.h"
 #include "pipe/p_state.h"
@@ -2053,7 +2057,10 @@ static int vrend_decode_ctx_submit_cmd(struct virgl_context *ctx,
    TRACE_FUNC();
    struct vrend_decode_ctx *gdctx = (struct vrend_decode_ctx *)ctx;
    bool bret;
-   int ret;
+   int ret = 0;
+#ifdef __APPLE__
+   CGLContextObj cgl_ctx = NULL;
+#endif
 
 #define TRANSFER_HEADER_SIZE 4096
 
@@ -2061,6 +2068,16 @@ static int vrend_decode_ctx_submit_cmd(struct virgl_context *ctx,
    if (bret == false)
       return EINVAL;
 
+#ifdef __APPLE__
+   /* CGL contexts are thread-local, while BridgeVM may service a serialized
+    * virtio-gpu queue notification on any vCPU thread.  Apple requires the
+    * complete use of a context to be bracketed when it can migrate between
+    * threads; merely calling CGLSetCurrentContext is not sufficient. */
+   cgl_ctx = CGLGetCurrentContext();
+   if (!cgl_ctx || CGLLockContext(cgl_ctx) != kCGLNoError)
+      return EINVAL;
+#endif
+
    if (VREND_DEBUG_ENABLED &&
        vrend_debug(gdctx->grctx, dbg_dump_cmd_streams) &&
        size > TRANSFER_HEADER_SIZE) {
@@ -2078,8 +2095,10 @@ static int vrend_decode_ctx_submit_cmd(struct virgl_context *ctx,
       uint32_t len = *buf >> 16;
       uint32_t cmd = *buf & 0xff;
 
-      if (cmd >= VIRGL_MAX_COMMANDS)
-         return EINVAL;
+      if (cmd >= VIRGL_MAX_COMMANDS) {
+         ret = EINVAL;
+         goto out;
+      }
 
       buf_offset += len + 1;
 
@@ -2103,10 +2122,15 @@ static int vrend_decode_ctx_submit_cmd(struct virgl_context *ctx,
                gdctx->base.ctx_id, vrend_get_comand_name(cmd), ret);
          if (ret == EINVAL)
             vrend_report_buffer_error(gdctx->grctx, *buf);
-         return ret;
+         goto out;
       }
    }
-   return 0;
+
+out:
+#ifdef __APPLE__
+   CGLUnlockContext(cgl_ctx);
+#endif
+   return ret;
 }
 
 static int vrend_decode_ctx_get_fencing_fd(UNUSED struct virgl_context *ctx)
diff --git a/src/vrend/vrend_renderer.c b/src/vrend/vrend_renderer.c
index 4bccc44..b3cc434 100644
--- a/src/vrend/vrend_renderer.c
+++ b/src/vrend/vrend_renderer.c
@@ -409,13 +409,77 @@ struct sysval_uniform_block {
    GLfloat clipp[VIRGL_NUM_CLIP_PLANES][4];
    GLuint stipple_pattern[VREND_POLYGON_STIPPLE_SIZE][4];
    GLfloat winsys_adjust_y;
+   GLint clip_halfz_fixup;
    GLfloat alpha_ref_val;
    GLfloat clip_plane_enabled;
    GLint drawid_base;
+   GLint padding[3];
 };
 
 static struct global_renderer_state vrend_state;
 
+#ifdef __APPLE__
+/* Apple's OpenGL driver can fault when glBufferData creates a fresh sysval UBO
+ * after a program has been linked.  Allocate one share-group buffer while ctx0
+ * is pristine, then refresh it before every serialized draw. */
+static GLuint vrend_apple_sysval_buffer_id;
+
+struct vrend_apple_buffer_texture_bindings {
+   GLint active_texture;
+   GLuint *textures;
+};
+
+/* Apple's legacy GL driver dereferences a null buffer-texture bind-point while
+ * updating a buffer object's shared-context stamp.  The fault is triggered by
+ * modifying (and, in practice, sometimes merely rebinding) any buffer while a
+ * GL_TEXTURE_BUFFER is bound on any texture unit.  Temporarily unbind every
+ * buffer texture around such operations, then restore the exact GL state so
+ * vrend's sampler binding cache remains truthful for the current draw. */
+static bool
+vrend_apple_buffer_modify_begin(struct vrend_apple_buffer_texture_bindings *saved)
+{
+   const GLuint count = vrend_state.max_texture_units;
+
+   memset(saved, 0, sizeof(*saved));
+   glGetIntegerv(GL_ACTIVE_TEXTURE, &saved->active_texture);
+   if (!count)
+      return true;
+
+   saved->textures = calloc(count, sizeof(*saved->textures));
+   if (!saved->textures) {
+      virgl_error("Unable to save Apple buffer-texture bindings\n");
+      return false;
+   }
+
+   for (GLuint unit = 0; unit < count; unit++) {
+      glActiveTexture(GL_TEXTURE0 + unit);
+      glGetIntegerv(GL_TEXTURE_BINDING_BUFFER,
+                    (GLint *)&saved->textures[unit]);
+      if (saved->textures[unit])
+         glBindTexture(GL_TEXTURE_BUFFER, 0);
+   }
+   glActiveTexture(saved->active_texture);
+   return true;
+}
+
+static void
+vrend_apple_buffer_modify_end(struct vrend_apple_buffer_texture_bindings *saved)
+{
+   if (!saved->textures)
+      return;
+
+   for (GLuint unit = 0; unit < vrend_state.max_texture_units; unit++) {
+      if (!saved->textures[unit])
+         continue;
+      glActiveTexture(GL_TEXTURE0 + unit);
+      glBindTexture(GL_TEXTURE_BUFFER, saved->textures[unit]);
+   }
+   glActiveTexture(saved->active_texture);
+   free(saved->textures);
+   saved->textures = NULL;
+}
+#endif
+
 static inline bool has_feature(enum features_id feature_id)
 {
    int slot = feature_id / 64;
@@ -1760,8 +1824,12 @@ static void bind_virgl_block_loc(struct vrend_linked_shader_program *sprog,
       if (sprog->virgl_block_bind == GL_INVALID_INDEX) {
          sprog->virgl_block_bind = virgl_block_ubo_id;
          if (sprog->ubo_sysval_buffer_id == GL_INVALID_INDEX) {
+#ifdef __APPLE__
+             sprog->ubo_sysval_buffer_id = vrend_apple_sysval_buffer_id;
+#else
              glGenBuffers(1, &sprog->ubo_sysval_buffer_id);
              created_virgl_block_buffer = true;
+#endif
          }
       }
 
@@ -2247,7 +2315,9 @@ static void vrend_destroy_program(struct vrend_linked_shader_program *ent)
       ent->ref_context->prog = NULL;
 
    if (ent->ubo_sysval_buffer_id != GL_INVALID_INDEX) {
+#ifndef __APPLE__
        glDeleteBuffers(1, &ent->ubo_sysval_buffer_id);
+#endif
    }
 
    if (ent->is_pipeline)
@@ -5075,7 +5145,8 @@ static GLenum get_xfb_mode(GLenum mode)
 }
 
 static void vrend_draw_bind_vertex_legacy(struct vrend_context *ctx,
-                                          struct vrend_vertex_element_array *va)
+                                          struct vrend_vertex_element_array *va,
+                                          uint32_t base_instance)
 {
    uint32_t enable_bitmask;
    uint32_t disable_bitmask;
@@ -5149,12 +5220,20 @@ static void vrend_draw_bind_vertex_legacy(struct vrend_context *ctx,
          disable_bitmask |= (1 << loc);
       } else {
          GLint size = !vrend_state.use_gles && (va->zyxw_bitmask & (1 << i)) ? GL_BGRA : ve->nr_chan;
+         GLintptr attrib_offset = (GLintptr)ve->base.src_offset + (GLintptr)vbo->base.buffer_offset;
+
+         /* base_instance emulation: hosts without feat_base_instance (e.g. Apple
+          * GL 4.1) reach this legacy path; offset instanced attributes by
+          * base_instance elements so the divisor-based fetch reads from the
+          * right element, matching glDraw*BaseInstance semantics. */
+         if (base_instance && ve->base.instance_divisor)
+            attrib_offset += (GLintptr)base_instance * (GLintptr)vbo->base.stride;
 
          enable_bitmask |= (1 << loc);
          if (util_format_is_pure_integer(ve->base.src_format)) {
-            glVertexAttribIPointer(loc, size, ve->type, vbo->base.stride, (void *)(uintptr_t)(ve->base.src_offset + vbo->base.buffer_offset));
+            glVertexAttribIPointer(loc, size, ve->type, vbo->base.stride, (void *)(uintptr_t)attrib_offset);
          } else {
-            glVertexAttribPointer(loc, size, ve->type, ve->norm, vbo->base.stride, (void *)(uintptr_t)(ve->base.src_offset + vbo->base.buffer_offset));
+            glVertexAttribPointer(loc, size, ve->type, ve->norm, vbo->base.stride, (void *)(uintptr_t)attrib_offset);
          }
          glVertexAttribDivisorARB(loc, ve->base.instance_divisor);
       }
@@ -5556,11 +5635,26 @@ vrend_fill_sysval_uniform_block (struct vrend_sub_context *sub_ctx)
    if (sub_ctx->prog->virgl_block_bind == GL_INVALID_INDEX)
       return;
 
+#ifdef __APPLE__
+   {
+#else
    if (sub_ctx->sysvalue_data_cookie != sub_ctx->prog->sysvalue_data_cookie) {
+#endif
+#ifdef __APPLE__
+      struct vrend_apple_buffer_texture_bindings saved;
+      if (!vrend_apple_buffer_modify_begin(&saved))
+         return;
+      glBindBuffer(GL_ARRAY_BUFFER, vrend_apple_sysval_buffer_id);
+      glBufferSubData(GL_ARRAY_BUFFER, 0, sizeof(struct sysval_uniform_block),
+                      &sub_ctx->sysvalue_data);
+      glBindBuffer(GL_ARRAY_BUFFER, 0);
+      vrend_apple_buffer_modify_end(&saved);
+#else
       glBindBuffer(GL_UNIFORM_BUFFER, sub_ctx->prog->ubo_sysval_buffer_id);
       glBufferSubData(GL_UNIFORM_BUFFER, 0, sizeof(struct sysval_uniform_block),
                       &sub_ctx->sysvalue_data);
       glBindBuffer(GL_UNIFORM_BUFFER, 0);
+#endif
       sub_ctx->prog->sysvalue_data_cookie = sub_ctx->sysvalue_data_cookie;
    }
 }
@@ -5895,6 +5989,7 @@ int vrend_draw_vbo(struct vrend_context *ctx,
    struct vrend_resource *indirect_res = NULL;
    struct vrend_resource *indirect_params_res = NULL;
    struct vrend_sub_context *sub_ctx = ctx->sub;
+   bool emulate_base_instance = info->start_instance && !has_feature(feat_base_instance);
 
    if (ctx->in_error)
       return ENOTRECOVERABLE;
@@ -5902,9 +5997,6 @@ int vrend_draw_vbo(struct vrend_context *ctx,
    if (info->instance_count && !has_feature(feat_draw_instance))
       return EINVAL;
 
-   if (info->start_instance && !has_feature(feat_base_instance))
-      return EINVAL;
-
    if (info->indirect.draw_count > 1 && !has_feature(feat_multi_draw_indirect))
       return EINVAL;
 
@@ -6000,11 +6092,11 @@ int vrend_draw_vbo(struct vrend_context *ctx,
       }
    } else {
       if (sub_ctx->ve) {
-         vrend_draw_bind_vertex_legacy(ctx, sub_ctx->ve);
+         vrend_draw_bind_vertex_legacy(ctx, sub_ctx->ve, emulate_base_instance ? info->start_instance : 0);
       } else {
          struct vrend_vertex_element_array va;
          va.count = 0;
-         vrend_draw_bind_vertex_legacy(ctx, &va);
+         vrend_draw_bind_vertex_legacy(ctx, &va, emulate_base_instance ? info->start_instance : 0);
       }
    }
 
@@ -6103,7 +6195,7 @@ int vrend_draw_vbo(struct vrend_context *ctx,
          else
             glDrawArraysIndirect(mode, (GLvoid const *)(uintptr_t)info->indirect.offset);
       } else if (info->instance_count > 0) {
-         if (info->start_instance > 0)
+         if (info->start_instance > 0 && !emulate_base_instance)
             glDrawArraysInstancedBaseInstance(mode, start, count, info->instance_count, info->start_instance);
          else
             glDrawArraysInstancedARB(mode, start, count, info->instance_count);
@@ -6135,7 +6227,7 @@ int vrend_draw_vbo(struct vrend_context *ctx,
             glDrawElementsIndirect(mode, elsz, (GLvoid const *)(uintptr_t)info->indirect.offset);
       } else if (info->index_bias) {
          if (info->instance_count > 0) {
-            if (info->start_instance > 0)
+            if (info->start_instance > 0 && !emulate_base_instance)
                glDrawElementsInstancedBaseVertexBaseInstance(mode, info->count, elsz, (void *)(uintptr_t)sub_ctx->ib.offset,
                                                              info->instance_count, info->index_bias, info->start_instance);
             else
@@ -6147,7 +6239,7 @@ int vrend_draw_vbo(struct vrend_context *ctx,
          else
             glDrawElementsBaseVertex(mode, info->count, elsz, (void *)(uintptr_t)sub_ctx->ib.offset, info->index_bias);
       } else if (info->instance_count > 0) {
-         if (info->start_instance > 0) {
+         if (info->start_instance > 0 && !emulate_base_instance) {
             glDrawElementsInstancedBaseInstance(mode, info->count, elsz, (void *)(uintptr_t)sub_ctx->ib.offset, info->instance_count, info->start_instance);
          } else
             glDrawElementsInstancedARB(mode, info->count, elsz, (void *)(uintptr_t)sub_ctx->ib.offset, info->instance_count);
@@ -6835,10 +6927,11 @@ static void vrend_hw_emit_rs(struct vrend_context *ctx)
            * already handled via Gallium */
           GLenum depthrule = state->clip_halfz ? GL_ZERO_TO_ONE : GL_NEGATIVE_ONE_TO_ONE;
           glClipControl(GL_LOWER_LEFT, depthrule);
-          ctx->sub->hw_rs_state.clip_halfz = state->clip_halfz;
        } else {
-          virgl_warn("No clip control supported\n");
+          ctx->sub->sysvalue_data.clip_halfz_fixup = state->clip_halfz;
+          ctx->sub->sysvalue_data_cookie++;
        }
+       ctx->sub->hw_rs_state.clip_halfz = state->clip_halfz;
    }
    if (state->flatshade_first != ctx->sub->hw_rs_state.flatshade_first) {
       ctx->sub->hw_rs_state.flatshade_first = state->flatshade_first;
@@ -7732,6 +7825,13 @@ int vrend_renderer_init(const struct vrend_if_cbs *cbs, uint32_t flags)
       goto fail;
    }
 
+#ifdef __APPLE__
+   glGenBuffers(1, &vrend_apple_sysval_buffer_id);
+   glBindBuffer(GL_ARRAY_BUFFER, vrend_apple_sysval_buffer_id);
+   glBufferData(GL_ARRAY_BUFFER, sizeof(struct sysval_uniform_block), NULL, GL_DYNAMIC_DRAW);
+   glBindBuffer(GL_ARRAY_BUFFER, 0);
+#endif
+
    vrend_state.eventfd = -1;
    if (flags & VREND_USE_THREAD_SYNC) {
       if (flags & VREND_USE_ASYNC_FENCE_CB)
@@ -7795,6 +7895,14 @@ vrend_renderer_fini(void)
    vrend_video_fini();
 #endif
 
+#ifdef __APPLE__
+   if (vrend_apple_sysval_buffer_id) {
+      vrend_clicbs->make_current(vrend_state.ctx0->sub->gl_context);
+      glDeleteBuffers(1, &vrend_apple_sysval_buffer_id);
+      vrend_apple_sysval_buffer_id = 0;
+   }
+#endif
+
    vrend_destroy_context(vrend_state.ctx0);
 
    vrend_state.current_ctx = NULL;
@@ -8031,6 +8139,7 @@ struct vrend_context *vrend_create_context(int id, uint32_t nlen, const char *de
    grctx->shader_cfg.has_texture_shadow_lod = has_feature(feat_texture_shadow_lod);
    grctx->shader_cfg.has_vs_layer = has_feature(feat_vs_layer_viewport);
    grctx->shader_cfg.has_vs_viewport_index = has_feature(feat_vs_viewport_index);
+   grctx->shader_cfg.has_draw_parameters = has_feature(feat_draw_parameters);
 
    vrend_renderer_create_sub_ctx(grctx, 0);
    vrend_renderer_set_sub_ctx(grctx, 0);
@@ -8290,6 +8399,11 @@ static void vrend_create_buffer(struct vrend_resource *gr, uint32_t width, uint3
 {
 
    GLbitfield buffer_storage_flags = 0;
+#ifdef __APPLE__
+   struct vrend_apple_buffer_texture_bindings saved;
+   if (!vrend_apple_buffer_modify_begin(&saved))
+      return;
+#endif
    if (flags & VIRGL_RESOURCE_FLAG_MAP_PERSISTENT) {
       buffer_storage_flags |= GL_MAP_PERSISTENT_BIT;
       /* Gallium's storage_flags_to_buffer_flags seems to drop some information, but we have to
@@ -8343,7 +8457,7 @@ static void vrend_create_buffer(struct vrend_resource *gr, uint32_t width, uint3
 #endif
       else {
          virgl_error("Missing buffer storage and interop extensions\n");
-         return;
+         goto out;
       }
 
       gr->storage_bits |= VREND_STORAGE_GL_IMMUTABLE;
@@ -8352,7 +8466,11 @@ static void vrend_create_buffer(struct vrend_resource *gr, uint32_t width, uint3
    } else
       glBufferData(gr->target, width, NULL, GL_STREAM_DRAW);
 
+out:
    glBindBufferARB(gr->target, 0);
+#ifdef __APPLE__
+   vrend_apple_buffer_modify_end(&saved);
+#endif
 }
 
 static int
@@ -8943,6 +9061,7 @@ void vrend_renderer_resource_destroy(struct vrend_resource *res)
    free(res);
 }
 
+#ifndef __APPLE__
 struct virgl_sub_upload_data {
    GLenum target;
    struct pipe_box *box;
@@ -8953,6 +9072,7 @@ static void iov_buffer_upload(void *cookie, uint32_t doff, void *src, int len)
    struct virgl_sub_upload_data *d = cookie;
    glBufferSubData(d->target, d->box->x + doff, len, src);
 }
+#endif
 
 static void vrend_scale_depth(void *ptr, int size, float scale_val)
 {
@@ -9301,6 +9421,49 @@ static int vrend_renderer_transfer_write_iov(struct vrend_context *ctx,
    }
 
    if (has_bit(res->storage_bits, VREND_STORAGE_GL_BUFFER)) {
+#ifdef __APPLE__
+      /* Apple's legacy OpenGL implementation can crash while updating its
+       * internal buffer binding state from glMapBufferRange.  A CPU staging
+       * allocation plus glBufferSubData exercises the same transfer semantics
+       * without mapping the GL buffer into the process. */
+      if (!info->box->width)
+         return 0;
+
+      data = malloc(info->box->width);
+      if (!data) {
+         virgl_error("Memory allocation failed for %d-byte buffer upload\n",
+                     info->box->width);
+         return ENOMEM;
+      }
+
+      if (vrend_read_from_iovec(iov, num_iovs, info->offset, data,
+                                info->box->width) != (size_t)info->box->width) {
+         free(data);
+         return EINVAL;
+      }
+
+      struct vrend_apple_buffer_texture_bindings saved;
+      if (!vrend_apple_buffer_modify_begin(&saved)) {
+         free(data);
+         return ENOMEM;
+      }
+      glBindBuffer(GL_COPY_WRITE_BUFFER, res->gl_id);
+      glBufferSubData(GL_COPY_WRITE_BUFFER, info->box->x,
+                      info->box->width, data);
+      glBindBuffer(GL_COPY_WRITE_BUFFER, 0);
+      vrend_apple_buffer_modify_end(&saved);
+      /* Apple's legacy GL loses same-context ordering between this buffer
+       * upload and a later draw that sources the buffer: the first D3D
+       * context after a guest boot reliably rasterizes from the buffer's
+       * stale (zeroed) GPU-side copy even though a CPU readback returns
+       * the fresh data, so its first draw silently produces nothing
+       * (the per-boot first-draw black-readback wall, live-proven
+       * 2026-07-14).  Flushing after the upload publishes the write to
+       * the GPU-side copy; it is the only probe-independent remedy found
+       * live, and a fresh-boot first-process draw smoke passes with it. */
+      glFlush();
+      free(data);
+#else
       GLuint map_flags = GL_MAP_INVALIDATE_RANGE_BIT | GL_MAP_WRITE_BIT;
       struct virgl_sub_upload_data d;
       d.box = info->box;
@@ -9319,6 +9482,7 @@ static int vrend_renderer_transfer_write_iov(struct vrend_context *ctx,
          glUnmapBuffer(res->target);
       }
       glBindBufferARB(res->target, 0);
+#endif
    } else {
       GLenum glformat;
       GLenum gltype;
@@ -9970,6 +10134,34 @@ static int vrend_renderer_transfer_send_iov(struct vrend_context *ctx,
    }
 
    if (has_bit(res->storage_bits, VREND_STORAGE_GL_BUFFER)) {
+#ifdef __APPLE__
+      if (!info->box->width)
+         return 0;
+
+      void *data = malloc(info->box->width);
+      if (!data) {
+         virgl_error("Memory allocation failed for %d-byte buffer readback\n",
+                     info->box->width);
+         return ENOMEM;
+      }
+
+      struct vrend_apple_buffer_texture_bindings saved;
+      if (!vrend_apple_buffer_modify_begin(&saved)) {
+         free(data);
+         return ENOMEM;
+      }
+      glBindBuffer(GL_COPY_READ_BUFFER, res->gl_id);
+      glGetBufferSubData(GL_COPY_READ_BUFFER, info->box->x,
+                         info->box->width, data);
+      glBindBuffer(GL_COPY_READ_BUFFER, 0);
+      vrend_apple_buffer_modify_end(&saved);
+
+      size_t written = vrend_write_to_iovec(iov, num_iovs, info->offset,
+                                            data, info->box->width);
+      free(data);
+      if (written != (size_t)info->box->width)
+         return EINVAL;
+#else
       glBindBufferARB(res->target, res->gl_id);
       void *data = glMapBufferRange(res->target, info->box->x, info->box->width, GL_MAP_READ_BIT);
       if (!data)
@@ -9978,11 +10170,19 @@ static int vrend_renderer_transfer_send_iov(struct vrend_context *ctx,
          vrend_write_to_iovec(iov, num_iovs, info->offset, data, info->box->width);
       glUnmapBuffer(res->target);
       glBindBufferARB(res->target, 0);
+#endif
    } else {
       int ret = -1;
       bool can_readpixels = true;
 
       can_readpixels = vrend_format_can_render(res->base.format) || vrend_format_is_ds(res->base.format);
+#ifdef __APPLE__
+      /* Apple's core profile rejects GL_BGRA readback from these renderer
+       * textures with GL_INVALID_ENUM.  The desktop glGetTexImage fallback
+       * below supports the native format and is the path that actually
+       * succeeds, so avoid a noisy and redundant framebuffer read first. */
+      can_readpixels = false;
+#endif
 
       if (can_readpixels)
          ret = vrend_transfer_send_readpixels(ctx, res, iov, num_iovs, info);
@@ -10421,12 +10621,43 @@ static void vrend_resource_buffer_copy(UNUSED struct vrend_context *ctx,
                                        uint32_t dstx, uint32_t srcx,
                                        uint32_t width)
 {
+#ifdef __APPLE__
+   /* Apple's OpenGL-on-Metal implementation reports
+    * `gldCopyBufferSubData: NEEDS IMPLEMENTATION` and can leave buffer binding
+    * state corrupted before the next uniform-block bind.  Stage the bytes
+    * through CPU memory instead.  This is deliberately Apple-only: native
+    * glCopyBufferSubData remains the fast path everywhere else. */
+   if (width == 0)
+      return;
+
+   void *data = malloc(width);
+   if (!data) {
+      virgl_error("Buffer copy fallback allocation failed for %u bytes\n", width);
+      return;
+   }
+
+   struct vrend_apple_buffer_texture_bindings saved;
+   if (!vrend_apple_buffer_modify_begin(&saved)) {
+      free(data);
+      return;
+   }
+   glBindBuffer(GL_COPY_READ_BUFFER, src_res->gl_id);
+   glGetBufferSubData(GL_COPY_READ_BUFFER, srcx, width, data);
+   glBindBuffer(GL_COPY_READ_BUFFER, 0);
+
+   glBindBuffer(GL_COPY_WRITE_BUFFER, dst_res->gl_id);
+   glBufferSubData(GL_COPY_WRITE_BUFFER, dstx, width, data);
+   glBindBuffer(GL_COPY_WRITE_BUFFER, 0);
+   vrend_apple_buffer_modify_end(&saved);
+   free(data);
+#else
    glBindBuffer(GL_COPY_READ_BUFFER, src_res->gl_id);
    glBindBuffer(GL_COPY_WRITE_BUFFER, dst_res->gl_id);
 
    glCopyBufferSubData(GL_COPY_READ_BUFFER, GL_COPY_WRITE_BUFFER, srcx, dstx, width);
    glBindBuffer(GL_COPY_READ_BUFFER, 0);
    glBindBuffer(GL_COPY_WRITE_BUFFER, 0);
+#endif
 }
 
 static void vrend_resource_copy_fallback(struct vrend_resource *src_res,
@@ -11201,8 +11432,8 @@ static void vrend_renderer_blit_int(struct vrend_context *ctx,
       VREND_DEBUG(dbg_blit, ctx, "BLIT_INT: use FBO blit\n");
       vrend_renderer_blit_fbo(ctx, src_res, dst_res, &blit_info);
    } else {
-      blit_info.has_srgb_write_control = has_feature(feat_texture_srgb_decode);
-      blit_info.has_texture_srgb_decode = has_feature(feat_srgb_write_control);
+      blit_info.has_srgb_write_control = has_feature(feat_srgb_write_control);
+      blit_info.has_texture_srgb_decode = has_feature(feat_texture_srgb_decode);
 
       VREND_DEBUG(dbg_blit, ctx, "BLIT_INT: use GL fallback\n");
       vrend_renderer_blit_gl(ctx, src_res, dst_res, &blit_info);
@@ -11626,8 +11857,14 @@ bool vrend_hw_switch_context(struct vrend_context *ctx, bool now)
    if (!ctx)
       return false;
 
-   if (ctx == vrend_state.current_ctx && ctx->ctx_switch_pending == false)
+   if (ctx == vrend_state.current_ctx && ctx->ctx_switch_pending == false) {
+#ifdef __APPLE__
+      /* Rebind the thread-local CGL context even when vrend's logical context
+       * did not change; MMIO may now be executing on another vCPU thread. */
+      vrend_clicbs->make_current(ctx->sub->gl_context);
+#endif
       return true;
+   }
 
    if (ctx->ctx_id != 0 && ctx->in_error) {
       return false;
diff --git a/src/vrend/vrend_shader.c b/src/vrend/vrend_shader.c
index 4fe673c..fd06705 100644
--- a/src/vrend/vrend_shader.c
+++ b/src/vrend/vrend_shader.c
@@ -92,6 +92,7 @@
 
 enum vrend_sysval_uniform {
    UNIFORM_WINSYS_ADJUST_Y,
+   UNIFORM_CLIP_HALFZ_FIXUP,
    UNIFORM_CLIP_PLANE,
    UNIFORM_ALPHA_REF_VAL,
    UNIFORM_PSTIPPLE_SAMPLER,
@@ -1988,7 +1989,14 @@ iter_declaration(struct tgsi_iterate_context *iter,
             virgl_error("Unsupported system value %d\n", decl->Semantic.Name);
             return false;
          }
-         ctx->shader_req_bits |= svmap->required_ext;
+         if (decl->Semantic.Name == TGSI_SEMANTIC_VERTEXID_NOBASE &&
+             !ctx->cfg->has_draw_parameters) {
+            name_prefix = "gl_VertexID";
+            ctx->shader_req_bits |= svmap->required_ext &
+                                    ~SHADER_REQ_SHADER_DRAW_PARAMETERS;
+         } else {
+            ctx->shader_req_bits |= svmap->required_ext;
+         }
          ctx->system_values[i].override_no_wm = svmap->override_no_wm;
          snprintf(ctx->system_values[i].glsl_name, 64, "%s", name_prefix);
          if (decl->Semantic.Name == TGSI_SEMANTIC_DRAWID)
@@ -2271,7 +2279,10 @@ static void emit_color_select(const struct dump_ctx *ctx,
 static void emit_prescale(struct vrend_glsl_strbufs *glsl_strbufs)
 {
    emit_buf(glsl_strbufs, "gl_Position.y = gl_Position.y * winsys_adjust_y;\n");
-   glsl_strbufs->required_sysval_uniform_decls |= BIT(UNIFORM_WINSYS_ADJUST_Y);
+   emit_buf(glsl_strbufs, "if (clip_halfz_fixup)\n");
+   emit_buf(glsl_strbufs, "   gl_Position.z = 2.0 * gl_Position.z - gl_Position.w;\n");
+   glsl_strbufs->required_sysval_uniform_decls |=
+      BIT(UNIFORM_WINSYS_ADJUST_Y) | BIT(UNIFORM_CLIP_HALFZ_FIXUP);
 }
 
 // TODO Consider exposing non-const ctx-> members as args to make *ctx const
@@ -2550,6 +2561,20 @@ static void handle_vertex_proc_exit(const struct dump_ctx *ctx,
        emit_fog_fixup_write(ctx, glsl_strbufs);
 }
 
+static void handle_tess_eval_proc_exit(const struct dump_ctx *ctx,
+                                       struct vrend_glsl_strbufs *glsl_strbufs,
+                                       bool *has_clipvertex_so)
+{
+   if (ctx->so && !ctx->key->gs_present)
+      emit_so_movs(ctx, glsl_strbufs, has_clipvertex_so);
+
+   if (ctx->cfg->has_cull_distance)
+      emit_clip_dist_movs(ctx, glsl_strbufs);
+
+   if (!ctx->key->gs_present)
+      emit_prescale(glsl_strbufs);
+}
+
 static void emit_fragment_logicop(const struct dump_ctx *ctx,
                                   struct vrend_glsl_strbufs *glsl_strbufs)
 {
@@ -5956,13 +5981,9 @@ iter_instruction(struct tgsi_iterate_context *iter,
          handle_vertex_proc_exit(ctx, &ctx->glsl_strbufs, &ctx->has_clipvertex_so);
       } else if (iter->processor.Processor == TGSI_PROCESSOR_TESS_CTRL && ctx->cfg->has_cull_distance) {
          emit_clip_dist_movs(ctx, &ctx->glsl_strbufs);
-      } else if (iter->processor.Processor == TGSI_PROCESSOR_TESS_EVAL && ctx->cfg->has_cull_distance) {
-         if (ctx->so && !ctx->key->gs_present)
-            emit_so_movs(ctx, &ctx->glsl_strbufs, &ctx->has_clipvertex_so);
-         emit_clip_dist_movs(ctx, &ctx->glsl_strbufs);
-         if (!ctx->key->gs_present) {
-            emit_prescale(&ctx->glsl_strbufs);
-         }
+      } else if (iter->processor.Processor == TGSI_PROCESSOR_TESS_EVAL) {
+         handle_tess_eval_proc_exit(ctx, &ctx->glsl_strbufs,
+                                    &ctx->has_clipvertex_so);
       } else if (iter->processor.Processor == TGSI_PROCESSOR_FRAGMENT) {
          handle_fragment_proc_exit(ctx, &ctx->glsl_strbufs);
       }
@@ -5971,6 +5992,9 @@ iter_instruction(struct tgsi_iterate_context *iter,
    case TGSI_OPCODE_RET:
       if (iter->processor.Processor == TGSI_PROCESSOR_VERTEX) {
          handle_vertex_proc_exit(ctx, &ctx->glsl_strbufs, &ctx->has_clipvertex_so);
+      } else if (iter->processor.Processor == TGSI_PROCESSOR_TESS_EVAL) {
+         handle_tess_eval_proc_exit(ctx, &ctx->glsl_strbufs,
+                                    &ctx->has_clipvertex_so);
       } else if (iter->processor.Processor == TGSI_PROCESSOR_FRAGMENT) {
          handle_fragment_proc_exit(ctx, &ctx->glsl_strbufs);
       }
@@ -5980,7 +6004,16 @@ iter_instruction(struct tgsi_iterate_context *iter,
       emit_buff(&ctx->glsl_strbufs, "%s = int(floor(%s)%s);\n", dsts[0], srcs[0], writemask);
       break;
    case TGSI_OPCODE_UARL:
-      emit_buff(&ctx->glsl_strbufs, "%s = int(%s);\n", dsts[0], srcs[0]);
+      /* BridgeVM/viogpu3d: the Windows D3D->virgl UMD emits UARL where the address
+       * source is a FLOAT-typed vertex-attribute VALUE (e.g. a palette index like
+       * 10.0), not an integer bit-pattern. The stock `int(floatBitsToInt(src))`
+       * bit-reinterprets 10.0 -> 1092616192, blowing every indirect UBO/const index
+       * out of bounds -> undefined color (0). This silently wiped Windows 11 Quick
+       * Settings tile icons (D2D palette-indexed vector fills). Converting the value
+       * (int(intBitsToFloat(floatBitsToInt(src))) == int(src_as_float)) yields the
+       * intended index. Verified: all QS icons render, and desktop/taskbar/Edge
+       * (chrome + HTML/CSS/SVG/text) are unaffected. */
+      emit_buff(&ctx->glsl_strbufs, "%s = int(intBitsToFloat(%s));\n", dsts[0], srcs[0]);
       break;
    case TGSI_OPCODE_XPD:
       emit_buff(&ctx->glsl_strbufs, "%s = %s(cross(vec3(%s), vec3(%s)));\n", dsts[0], get_string(dinfo.dstconv), srcs[0], srcs[1]);
@@ -6347,7 +6380,11 @@ static void emit_header(const struct dump_ctx *ctx, struct vrend_glsl_strbufs *g
       if (ctx->prog_type == TGSI_PROCESSOR_FRAGMENT && fs_emit_layout(ctx))
          emit_ext(glsl_strbufs, "ARB_fragment_coord_conventions", "require");
 
-      if (ctx->ubo_used_mask)
+      /* Uniform blocks are core in GLSL 1.40. Apple's core-profile compiler
+       * deliberately does not expose GL_ARB_uniform_buffer_object as an
+       * extension and rejects a redundant `#extension ... : require`, even
+       * though the same UBO syntax is valid in the selected language version. */
+      if (ctx->ubo_used_mask && ctx->glsl_ver_required < 140)
          emit_ext(glsl_strbufs, "ARB_uniform_buffer_object", "require");
 
       if (ctx->num_cull_dist_prop || ctx->key->num_in_cull || ctx->key->num_out_cull)
@@ -6361,10 +6398,19 @@ static void emit_header(const struct dump_ctx *ctx, struct vrend_glsl_strbufs *g
       }
 
       for (uint32_t i = 0; i < ARRAY_SIZE(shader_req_table); i++) {
-         if (shader_req_table[i].key == SHADER_REQ_SAMPLER_RECT && ctx->glsl_ver_required >= 140)
+         const uint64_t key = shader_req_table[i].key;
+
+         /* These features are core in GLSL 1.40. Apple's core-profile GL 4.1
+          * compiler exposes the core operation (gl_InstanceID, sampler rect)
+          * but not the redundant ARB extension name, and rejects a
+          * `#extension ... : require` for it. Emitting it poisons DWM's virgl
+          * composition context (DRAW_VBO EINVAL -> ctx in_error), which
+          * silently drops all later per-window composition. */
+         if (ctx->glsl_ver_required >= 140 &&
+             (key == SHADER_REQ_SAMPLER_RECT || key == SHADER_REQ_INSTANCE_ID))
             continue;
 
-         if (ctx->shader_req_bits & shader_req_table[i].key) {
+         if (ctx->shader_req_bits & key) {
             emit_ext(glsl_strbufs, shader_req_table[i].string, "require");
          }
       }
@@ -8010,6 +8056,7 @@ static void emit_required_sysval_uniforms(struct vrend_strbuf *block, uint32_t m
    strbuf_append(block, "\tvec4 clipp[8];\n");
    strbuf_appendf(block, "\tuint stipple_pattern[%d];\n", VREND_POLYGON_STIPPLE_SIZE);
    strbuf_append(block, "\tfloat winsys_adjust_y;\n");
+   strbuf_append(block, "\tbool clip_halfz_fixup;\n");
    strbuf_append(block, "\tfloat alpha_ref_val;\n");
    strbuf_append(block, "\tbool clip_plane_enabled;\n");
    strbuf_append(block, "\tint drawid_base;\n");
@@ -8583,4 +8630,3 @@ bool vrend_shader_needs_alpha_func(const struct vrend_shader_key *key) {
       return true;
    }
 }
-
diff --git a/src/vrend/vrend_shader.h b/src/vrend/vrend_shader.h
index 7f5897b..25cbed3 100644
--- a/src/vrend/vrend_shader.h
+++ b/src/vrend/vrend_shader.h
@@ -259,6 +259,7 @@ struct vrend_shader_cfg {
    uint32_t has_texture_shadow_lod : 1;
    uint32_t has_vs_layer : 1;
    uint32_t has_vs_viewport_index : 1;
+   uint32_t has_draw_parameters : 1;
 };
 
 struct vrend_context;
