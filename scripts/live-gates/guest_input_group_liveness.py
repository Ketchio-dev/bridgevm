"""Only ESRCH proves absence; permission denial leaves a group potentially alive."""
import os


def group_alive(pgid):
    try:
        os.killpg(pgid, 0)
        return True
    except ProcessLookupError:
        return False
    except PermissionError:
        return True
